// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::integration_status_type::IntegrationStatus;
use super::integration_type_type::IntegrationType;
use super::sync_status_type::SyncStatus;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateIntegrationStatusArgs {
    pub organization_id: u64,
    pub integration_id: u64,
    pub integration_type: IntegrationType,
    pub status: IntegrationStatus,
    pub sync_status: SyncStatus,
    pub error_message: Option<String>,
}

impl From<UpdateIntegrationStatusArgs> for super::Reducer {
    fn from(args: UpdateIntegrationStatusArgs) -> Self {
        Self::UpdateIntegrationStatus {
            organization_id: args.organization_id,
            integration_id: args.integration_id,
            integration_type: args.integration_type,
            status: args.status,
            sync_status: args.sync_status,
            error_message: args.error_message,
        }
    }
}

impl __sdk::InModule for UpdateIntegrationStatusArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_integration_status`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_integration_status {
    /// Request that the remote module invoke the reducer `update_integration_status` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_integration_status:update_integration_status_then`] to run a callback after the reducer completes.
    fn update_integration_status(
        &self,
        organization_id: u64,
        integration_id: u64,
        integration_type: IntegrationType,
        status: IntegrationStatus,
        sync_status: SyncStatus,
        error_message: Option<String>,
    ) -> __sdk::Result<()> {
        self.update_integration_status_then(
            organization_id,
            integration_id,
            integration_type,
            status,
            sync_status,
            error_message,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_integration_status` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_integration_status_then(
        &self,
        organization_id: u64,
        integration_id: u64,
        integration_type: IntegrationType,
        status: IntegrationStatus,
        sync_status: SyncStatus,
        error_message: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_integration_status for super::RemoteReducers {
    fn update_integration_status_then(
        &self,
        organization_id: u64,
        integration_id: u64,
        integration_type: IntegrationType,
        status: IntegrationStatus,
        sync_status: SyncStatus,
        error_message: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateIntegrationStatusArgs {
                organization_id,
                integration_id,
                integration_type,
                status,
                sync_status,
                error_message,
            },
            callback,
        )
    }
}
