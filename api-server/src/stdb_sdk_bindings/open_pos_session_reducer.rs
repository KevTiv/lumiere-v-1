// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct OpenPosSessionArgs {
    pub organization_id: u64,
    pub config_id: u64,
    pub cash_register_balance_start: f64,
}

impl From<OpenPosSessionArgs> for super::Reducer {
    fn from(args: OpenPosSessionArgs) -> Self {
        Self::OpenPosSession {
            organization_id: args.organization_id,
            config_id: args.config_id,
            cash_register_balance_start: args.cash_register_balance_start,
        }
    }
}

impl __sdk::InModule for OpenPosSessionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `open_pos_session`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait open_pos_session {
    /// Request that the remote module invoke the reducer `open_pos_session` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`open_pos_session:open_pos_session_then`] to run a callback after the reducer completes.
    fn open_pos_session(
        &self,
        organization_id: u64,
        config_id: u64,
        cash_register_balance_start: f64,
    ) -> __sdk::Result<()> {
        self.open_pos_session_then(
            organization_id,
            config_id,
            cash_register_balance_start,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `open_pos_session` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn open_pos_session_then(
        &self,
        organization_id: u64,
        config_id: u64,
        cash_register_balance_start: f64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl open_pos_session for super::RemoteReducers {
    fn open_pos_session_then(
        &self,
        organization_id: u64,
        config_id: u64,
        cash_register_balance_start: f64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            OpenPosSessionArgs {
                organization_id,
                config_id,
                cash_register_balance_start,
            },
            callback,
        )
    }
}
