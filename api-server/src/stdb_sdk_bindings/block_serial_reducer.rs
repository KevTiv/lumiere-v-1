// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BlockSerialArgs {
    pub organization_id: u64,
    pub serial_id: u64,
    pub reason: Option<String>,
}

impl From<BlockSerialArgs> for super::Reducer {
    fn from(args: BlockSerialArgs) -> Self {
        Self::BlockSerial {
            organization_id: args.organization_id,
            serial_id: args.serial_id,
            reason: args.reason,
        }
    }
}

impl __sdk::InModule for BlockSerialArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `block_serial`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait block_serial {
    /// Request that the remote module invoke the reducer `block_serial` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`block_serial:block_serial_then`] to run a callback after the reducer completes.
    fn block_serial(
        &self,
        organization_id: u64,
        serial_id: u64,
        reason: Option<String>,
    ) -> __sdk::Result<()> {
        self.block_serial_then(organization_id, serial_id, reason, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `block_serial` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn block_serial_then(
        &self,
        organization_id: u64,
        serial_id: u64,
        reason: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl block_serial for super::RemoteReducers {
    fn block_serial_then(
        &self,
        organization_id: u64,
        serial_id: u64,
        reason: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            BlockSerialArgs {
                organization_id,
                serial_id,
                reason,
            },
            callback,
        )
    }
}
