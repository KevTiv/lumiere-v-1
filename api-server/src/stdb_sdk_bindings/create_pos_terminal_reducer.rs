// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreatePosTerminalArgs {
    pub organization_id: u64,
    pub company_id: Option<u64>,
    pub name: String,
    pub location_label: Option<String>,
    pub latitude: Option<f64>,
    pub longitude: Option<f64>,
}

impl From<CreatePosTerminalArgs> for super::Reducer {
    fn from(args: CreatePosTerminalArgs) -> Self {
        Self::CreatePosTerminal {
            organization_id: args.organization_id,
            company_id: args.company_id,
            name: args.name,
            location_label: args.location_label,
            latitude: args.latitude,
            longitude: args.longitude,
        }
    }
}

impl __sdk::InModule for CreatePosTerminalArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_pos_terminal`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_pos_terminal {
    /// Request that the remote module invoke the reducer `create_pos_terminal` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_pos_terminal:create_pos_terminal_then`] to run a callback after the reducer completes.
    fn create_pos_terminal(
        &self,
        organization_id: u64,
        company_id: Option<u64>,
        name: String,
        location_label: Option<String>,
        latitude: Option<f64>,
        longitude: Option<f64>,
    ) -> __sdk::Result<()> {
        self.create_pos_terminal_then(
            organization_id,
            company_id,
            name,
            location_label,
            latitude,
            longitude,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_pos_terminal` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_pos_terminal_then(
        &self,
        organization_id: u64,
        company_id: Option<u64>,
        name: String,
        location_label: Option<String>,
        latitude: Option<f64>,
        longitude: Option<f64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_pos_terminal for super::RemoteReducers {
    fn create_pos_terminal_then(
        &self,
        organization_id: u64,
        company_id: Option<u64>,
        name: String,
        location_label: Option<String>,
        latitude: Option<f64>,
        longitude: Option<f64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreatePosTerminalArgs {
                organization_id,
                company_id,
                name,
                location_label,
                latitude,
                longitude,
            },
            callback,
        )
    }
}
