// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::dispose_account_asset_params_type::DisposeAccountAssetParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DisposeAccountAssetArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub asset_id: u64,
    pub params: DisposeAccountAssetParams,
}

impl From<DisposeAccountAssetArgs> for super::Reducer {
    fn from(args: DisposeAccountAssetArgs) -> Self {
        Self::DisposeAccountAsset {
            organization_id: args.organization_id,
            company_id: args.company_id,
            asset_id: args.asset_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for DisposeAccountAssetArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `dispose_account_asset`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait dispose_account_asset {
    /// Request that the remote module invoke the reducer `dispose_account_asset` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`dispose_account_asset:dispose_account_asset_then`] to run a callback after the reducer completes.
    fn dispose_account_asset(
        &self,
        organization_id: u64,
        company_id: u64,
        asset_id: u64,
        params: DisposeAccountAssetParams,
    ) -> __sdk::Result<()> {
        self.dispose_account_asset_then(organization_id, company_id, asset_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `dispose_account_asset` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn dispose_account_asset_then(
        &self,
        organization_id: u64,
        company_id: u64,
        asset_id: u64,
        params: DisposeAccountAssetParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl dispose_account_asset for super::RemoteReducers {
    fn dispose_account_asset_then(
        &self,
        organization_id: u64,
        company_id: u64,
        asset_id: u64,
        params: DisposeAccountAssetParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DisposeAccountAssetArgs {
                organization_id,
                company_id,
                asset_id,
                params,
            },
            callback,
        )
    }
}
