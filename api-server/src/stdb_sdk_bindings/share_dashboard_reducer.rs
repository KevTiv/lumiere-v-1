// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_dashboard_share_params_type::UpdateDashboardShareParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ShareDashboardArgs {
    pub organization_id: u64,
    pub dashboard_id: u64,
    pub params: UpdateDashboardShareParams,
}

impl From<ShareDashboardArgs> for super::Reducer {
    fn from(args: ShareDashboardArgs) -> Self {
        Self::ShareDashboard {
            organization_id: args.organization_id,
            dashboard_id: args.dashboard_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for ShareDashboardArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `share_dashboard`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait share_dashboard {
    /// Request that the remote module invoke the reducer `share_dashboard` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`share_dashboard:share_dashboard_then`] to run a callback after the reducer completes.
    fn share_dashboard(
        &self,
        organization_id: u64,
        dashboard_id: u64,
        params: UpdateDashboardShareParams,
    ) -> __sdk::Result<()> {
        self.share_dashboard_then(organization_id, dashboard_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `share_dashboard` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn share_dashboard_then(
        &self,
        organization_id: u64,
        dashboard_id: u64,
        params: UpdateDashboardShareParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl share_dashboard for super::RemoteReducers {
    fn share_dashboard_then(
        &self,
        organization_id: u64,
        dashboard_id: u64,
        params: UpdateDashboardShareParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ShareDashboardArgs {
                organization_id,
                dashboard_id,
                params,
            },
            callback,
        )
    }
}
