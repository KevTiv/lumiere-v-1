// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct LinkDeviceToPosArgs {
    pub organization_id: u64,
    pub device_id: u64,
    pub pos_config_id: u64,
}

impl From<LinkDeviceToPosArgs> for super::Reducer {
    fn from(args: LinkDeviceToPosArgs) -> Self {
        Self::LinkDeviceToPos {
            organization_id: args.organization_id,
            device_id: args.device_id,
            pos_config_id: args.pos_config_id,
        }
    }
}

impl __sdk::InModule for LinkDeviceToPosArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `link_device_to_pos`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait link_device_to_pos {
    /// Request that the remote module invoke the reducer `link_device_to_pos` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`link_device_to_pos:link_device_to_pos_then`] to run a callback after the reducer completes.
    fn link_device_to_pos(
        &self,
        organization_id: u64,
        device_id: u64,
        pos_config_id: u64,
    ) -> __sdk::Result<()> {
        self.link_device_to_pos_then(organization_id, device_id, pos_config_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `link_device_to_pos` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn link_device_to_pos_then(
        &self,
        organization_id: u64,
        device_id: u64,
        pos_config_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl link_device_to_pos for super::RemoteReducers {
    fn link_device_to_pos_then(
        &self,
        organization_id: u64,
        device_id: u64,
        pos_config_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            LinkDeviceToPosArgs {
                organization_id,
                device_id,
                pos_config_id,
            },
            callback,
        )
    }
}
