// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ApplyReconciliationRulesArgs {
    pub organization_id: u64,
    pub line_id: u64,
    pub rule_id: Option<u64>,
}

impl From<ApplyReconciliationRulesArgs> for super::Reducer {
    fn from(args: ApplyReconciliationRulesArgs) -> Self {
        Self::ApplyReconciliationRules {
            organization_id: args.organization_id,
            line_id: args.line_id,
            rule_id: args.rule_id,
        }
    }
}

impl __sdk::InModule for ApplyReconciliationRulesArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `apply_reconciliation_rules`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait apply_reconciliation_rules {
    /// Request that the remote module invoke the reducer `apply_reconciliation_rules` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`apply_reconciliation_rules:apply_reconciliation_rules_then`] to run a callback after the reducer completes.
    fn apply_reconciliation_rules(
        &self,
        organization_id: u64,
        line_id: u64,
        rule_id: Option<u64>,
    ) -> __sdk::Result<()> {
        self.apply_reconciliation_rules_then(organization_id, line_id, rule_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `apply_reconciliation_rules` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn apply_reconciliation_rules_then(
        &self,
        organization_id: u64,
        line_id: u64,
        rule_id: Option<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl apply_reconciliation_rules for super::RemoteReducers {
    fn apply_reconciliation_rules_then(
        &self,
        organization_id: u64,
        line_id: u64,
        rule_id: Option<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ApplyReconciliationRulesArgs {
                organization_id,
                line_id,
                rule_id,
            },
            callback,
        )
    }
}
