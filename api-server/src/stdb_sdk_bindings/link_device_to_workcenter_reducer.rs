// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct LinkDeviceToWorkcenterArgs {
    pub organization_id: u64,
    pub device_id: u64,
    pub workcenter_id: u64,
}

impl From<LinkDeviceToWorkcenterArgs> for super::Reducer {
    fn from(args: LinkDeviceToWorkcenterArgs) -> Self {
        Self::LinkDeviceToWorkcenter {
            organization_id: args.organization_id,
            device_id: args.device_id,
            workcenter_id: args.workcenter_id,
        }
    }
}

impl __sdk::InModule for LinkDeviceToWorkcenterArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `link_device_to_workcenter`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait link_device_to_workcenter {
    /// Request that the remote module invoke the reducer `link_device_to_workcenter` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`link_device_to_workcenter:link_device_to_workcenter_then`] to run a callback after the reducer completes.
    fn link_device_to_workcenter(
        &self,
        organization_id: u64,
        device_id: u64,
        workcenter_id: u64,
    ) -> __sdk::Result<()> {
        self.link_device_to_workcenter_then(organization_id, device_id, workcenter_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `link_device_to_workcenter` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn link_device_to_workcenter_then(
        &self,
        organization_id: u64,
        device_id: u64,
        workcenter_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl link_device_to_workcenter for super::RemoteReducers {
    fn link_device_to_workcenter_then(
        &self,
        organization_id: u64,
        device_id: u64,
        workcenter_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            LinkDeviceToWorkcenterArgs {
                organization_id,
                device_id,
                workcenter_id,
            },
            callback,
        )
    }
}
