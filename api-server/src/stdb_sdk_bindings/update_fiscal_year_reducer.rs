// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_fiscal_year_params_type::UpdateFiscalYearParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateFiscalYearArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub fiscal_year_id: u64,
    pub params: UpdateFiscalYearParams,
}

impl From<UpdateFiscalYearArgs> for super::Reducer {
    fn from(args: UpdateFiscalYearArgs) -> Self {
        Self::UpdateFiscalYear {
            organization_id: args.organization_id,
            company_id: args.company_id,
            fiscal_year_id: args.fiscal_year_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateFiscalYearArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_fiscal_year`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_fiscal_year {
    /// Request that the remote module invoke the reducer `update_fiscal_year` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_fiscal_year:update_fiscal_year_then`] to run a callback after the reducer completes.
    fn update_fiscal_year(
        &self,
        organization_id: u64,
        company_id: u64,
        fiscal_year_id: u64,
        params: UpdateFiscalYearParams,
    ) -> __sdk::Result<()> {
        self.update_fiscal_year_then(
            organization_id,
            company_id,
            fiscal_year_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_fiscal_year` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_fiscal_year_then(
        &self,
        organization_id: u64,
        company_id: u64,
        fiscal_year_id: u64,
        params: UpdateFiscalYearParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_fiscal_year for super::RemoteReducers {
    fn update_fiscal_year_then(
        &self,
        organization_id: u64,
        company_id: u64,
        fiscal_year_id: u64,
        params: UpdateFiscalYearParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateFiscalYearArgs {
                organization_id,
                company_id,
                fiscal_year_id,
                params,
            },
            callback,
        )
    }
}
