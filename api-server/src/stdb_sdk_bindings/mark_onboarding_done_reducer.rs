// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MarkOnboardingDoneArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub employee_id: u64,
}

impl From<MarkOnboardingDoneArgs> for super::Reducer {
    fn from(args: MarkOnboardingDoneArgs) -> Self {
        Self::MarkOnboardingDone {
            organization_id: args.organization_id,
            company_id: args.company_id,
            employee_id: args.employee_id,
        }
    }
}

impl __sdk::InModule for MarkOnboardingDoneArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `mark_onboarding_done`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait mark_onboarding_done {
    /// Request that the remote module invoke the reducer `mark_onboarding_done` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`mark_onboarding_done:mark_onboarding_done_then`] to run a callback after the reducer completes.
    fn mark_onboarding_done(
        &self,
        organization_id: u64,
        company_id: u64,
        employee_id: u64,
    ) -> __sdk::Result<()> {
        self.mark_onboarding_done_then(organization_id, company_id, employee_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `mark_onboarding_done` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn mark_onboarding_done_then(
        &self,
        organization_id: u64,
        company_id: u64,
        employee_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl mark_onboarding_done for super::RemoteReducers {
    fn mark_onboarding_done_then(
        &self,
        organization_id: u64,
        company_id: u64,
        employee_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            MarkOnboardingDoneArgs {
                organization_id,
                company_id,
                employee_id,
            },
            callback,
        )
    }
}
