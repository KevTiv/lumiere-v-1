// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdatePaymentTermArgs {
    pub organization_id: u64,
    pub term_id: u64,
    pub name: Option<String>,
    pub note: Option<String>,
    pub is_active: Option<bool>,
}

impl From<UpdatePaymentTermArgs> for super::Reducer {
    fn from(args: UpdatePaymentTermArgs) -> Self {
        Self::UpdatePaymentTerm {
            organization_id: args.organization_id,
            term_id: args.term_id,
            name: args.name,
            note: args.note,
            is_active: args.is_active,
        }
    }
}

impl __sdk::InModule for UpdatePaymentTermArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_payment_term`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_payment_term {
    /// Request that the remote module invoke the reducer `update_payment_term` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_payment_term:update_payment_term_then`] to run a callback after the reducer completes.
    fn update_payment_term(
        &self,
        organization_id: u64,
        term_id: u64,
        name: Option<String>,
        note: Option<String>,
        is_active: Option<bool>,
    ) -> __sdk::Result<()> {
        self.update_payment_term_then(organization_id, term_id, name, note, is_active, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_payment_term` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_payment_term_then(
        &self,
        organization_id: u64,
        term_id: u64,
        name: Option<String>,
        note: Option<String>,
        is_active: Option<bool>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_payment_term for super::RemoteReducers {
    fn update_payment_term_then(
        &self,
        organization_id: u64,
        term_id: u64,
        name: Option<String>,
        note: Option<String>,
        is_active: Option<bool>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdatePaymentTermArgs {
                organization_id,
                term_id,
                name,
                note,
                is_active,
            },
            callback,
        )
    }
}
