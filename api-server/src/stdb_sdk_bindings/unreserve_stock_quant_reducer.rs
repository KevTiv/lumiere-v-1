// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::stock_quant_unreserve_params_type::StockQuantUnreserveParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UnreserveStockQuantArgs {
    pub organization_id: u64,
    pub quant_id: u64,
    pub params: StockQuantUnreserveParams,
}

impl From<UnreserveStockQuantArgs> for super::Reducer {
    fn from(args: UnreserveStockQuantArgs) -> Self {
        Self::UnreserveStockQuant {
            organization_id: args.organization_id,
            quant_id: args.quant_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UnreserveStockQuantArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `unreserve_stock_quant`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait unreserve_stock_quant {
    /// Request that the remote module invoke the reducer `unreserve_stock_quant` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`unreserve_stock_quant:unreserve_stock_quant_then`] to run a callback after the reducer completes.
    fn unreserve_stock_quant(
        &self,
        organization_id: u64,
        quant_id: u64,
        params: StockQuantUnreserveParams,
    ) -> __sdk::Result<()> {
        self.unreserve_stock_quant_then(organization_id, quant_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `unreserve_stock_quant` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn unreserve_stock_quant_then(
        &self,
        organization_id: u64,
        quant_id: u64,
        params: StockQuantUnreserveParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl unreserve_stock_quant for super::RemoteReducers {
    fn unreserve_stock_quant_then(
        &self,
        organization_id: u64,
        quant_id: u64,
        params: StockQuantUnreserveParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UnreserveStockQuantArgs {
                organization_id,
                quant_id,
                params,
            },
            callback,
        )
    }
}
