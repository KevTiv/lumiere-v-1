// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::seed_pack_holidays_params_type::SeedPackHolidaysParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SeedPackHolidaysArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub params: SeedPackHolidaysParams,
}

impl From<SeedPackHolidaysArgs> for super::Reducer {
    fn from(args: SeedPackHolidaysArgs) -> Self {
        Self::SeedPackHolidays {
            organization_id: args.organization_id,
            company_id: args.company_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for SeedPackHolidaysArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `seed_pack_holidays`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait seed_pack_holidays {
    /// Request that the remote module invoke the reducer `seed_pack_holidays` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`seed_pack_holidays:seed_pack_holidays_then`] to run a callback after the reducer completes.
    fn seed_pack_holidays(
        &self,
        organization_id: u64,
        company_id: u64,
        params: SeedPackHolidaysParams,
    ) -> __sdk::Result<()> {
        self.seed_pack_holidays_then(organization_id, company_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `seed_pack_holidays` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn seed_pack_holidays_then(
        &self,
        organization_id: u64,
        company_id: u64,
        params: SeedPackHolidaysParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl seed_pack_holidays for super::RemoteReducers {
    fn seed_pack_holidays_then(
        &self,
        organization_id: u64,
        company_id: u64,
        params: SeedPackHolidaysParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SeedPackHolidaysArgs {
                organization_id,
                company_id,
                params,
            },
            callback,
        )
    }
}
