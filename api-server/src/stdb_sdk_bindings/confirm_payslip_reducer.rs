// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::confirm_payslip_params_type::ConfirmPayslipParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ConfirmPayslipArgs {
    pub organization_id: u64,
    pub payslip_id: u64,
    pub params: ConfirmPayslipParams,
}

impl From<ConfirmPayslipArgs> for super::Reducer {
    fn from(args: ConfirmPayslipArgs) -> Self {
        Self::ConfirmPayslip {
            organization_id: args.organization_id,
            payslip_id: args.payslip_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for ConfirmPayslipArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `confirm_payslip`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait confirm_payslip {
    /// Request that the remote module invoke the reducer `confirm_payslip` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`confirm_payslip:confirm_payslip_then`] to run a callback after the reducer completes.
    fn confirm_payslip(
        &self,
        organization_id: u64,
        payslip_id: u64,
        params: ConfirmPayslipParams,
    ) -> __sdk::Result<()> {
        self.confirm_payslip_then(organization_id, payslip_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `confirm_payslip` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn confirm_payslip_then(
        &self,
        organization_id: u64,
        payslip_id: u64,
        params: ConfirmPayslipParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl confirm_payslip for super::RemoteReducers {
    fn confirm_payslip_then(
        &self,
        organization_id: u64,
        payslip_id: u64,
        params: ConfirmPayslipParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ConfirmPayslipArgs {
                organization_id,
                payslip_id,
                params,
            },
            callback,
        )
    }
}
