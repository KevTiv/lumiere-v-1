// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecordAiAgentRunPolicySnapshotArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub run_id: u64,
    pub expected_release_id: Option<u64>,
}

impl From<RecordAiAgentRunPolicySnapshotArgs> for super::Reducer {
    fn from(args: RecordAiAgentRunPolicySnapshotArgs) -> Self {
        Self::RecordAiAgentRunPolicySnapshot {
            organization_id: args.organization_id,
            company_id: args.company_id,
            run_id: args.run_id,
            expected_release_id: args.expected_release_id,
        }
    }
}

impl __sdk::InModule for RecordAiAgentRunPolicySnapshotArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `record_ai_agent_run_policy_snapshot`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait record_ai_agent_run_policy_snapshot {
    /// Request that the remote module invoke the reducer `record_ai_agent_run_policy_snapshot` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`record_ai_agent_run_policy_snapshot:record_ai_agent_run_policy_snapshot_then`] to run a callback after the reducer completes.
    fn record_ai_agent_run_policy_snapshot(
        &self,
        organization_id: u64,
        company_id: u64,
        run_id: u64,
        expected_release_id: Option<u64>,
    ) -> __sdk::Result<()> {
        self.record_ai_agent_run_policy_snapshot_then(
            organization_id,
            company_id,
            run_id,
            expected_release_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `record_ai_agent_run_policy_snapshot` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn record_ai_agent_run_policy_snapshot_then(
        &self,
        organization_id: u64,
        company_id: u64,
        run_id: u64,
        expected_release_id: Option<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl record_ai_agent_run_policy_snapshot for super::RemoteReducers {
    fn record_ai_agent_run_policy_snapshot_then(
        &self,
        organization_id: u64,
        company_id: u64,
        run_id: u64,
        expected_release_id: Option<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecordAiAgentRunPolicySnapshotArgs {
                organization_id,
                company_id,
                run_id,
                expected_release_id,
            },
            callback,
        )
    }
}
