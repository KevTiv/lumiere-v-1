// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::apply_expense_advance_params_type::ApplyExpenseAdvanceParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ApplyExpenseAdvanceToSheetArgs {
    pub organization_id: u64,
    pub advance_id: u64,
    pub sheet_id: u64,
    pub params: ApplyExpenseAdvanceParams,
}

impl From<ApplyExpenseAdvanceToSheetArgs> for super::Reducer {
    fn from(args: ApplyExpenseAdvanceToSheetArgs) -> Self {
        Self::ApplyExpenseAdvanceToSheet {
            organization_id: args.organization_id,
            advance_id: args.advance_id,
            sheet_id: args.sheet_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for ApplyExpenseAdvanceToSheetArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `apply_expense_advance_to_sheet`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait apply_expense_advance_to_sheet {
    /// Request that the remote module invoke the reducer `apply_expense_advance_to_sheet` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`apply_expense_advance_to_sheet:apply_expense_advance_to_sheet_then`] to run a callback after the reducer completes.
    fn apply_expense_advance_to_sheet(
        &self,
        organization_id: u64,
        advance_id: u64,
        sheet_id: u64,
        params: ApplyExpenseAdvanceParams,
    ) -> __sdk::Result<()> {
        self.apply_expense_advance_to_sheet_then(
            organization_id,
            advance_id,
            sheet_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `apply_expense_advance_to_sheet` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn apply_expense_advance_to_sheet_then(
        &self,
        organization_id: u64,
        advance_id: u64,
        sheet_id: u64,
        params: ApplyExpenseAdvanceParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl apply_expense_advance_to_sheet for super::RemoteReducers {
    fn apply_expense_advance_to_sheet_then(
        &self,
        organization_id: u64,
        advance_id: u64,
        sheet_id: u64,
        params: ApplyExpenseAdvanceParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ApplyExpenseAdvanceToSheetArgs {
                organization_id,
                advance_id,
                sheet_id,
                params,
            },
            callback,
        )
    }
}
