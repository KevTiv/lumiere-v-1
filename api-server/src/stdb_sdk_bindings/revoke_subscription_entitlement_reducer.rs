// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RevokeSubscriptionEntitlementArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub entitlement_id: u64,
}

impl From<RevokeSubscriptionEntitlementArgs> for super::Reducer {
    fn from(args: RevokeSubscriptionEntitlementArgs) -> Self {
        Self::RevokeSubscriptionEntitlement {
            organization_id: args.organization_id,
            company_id: args.company_id,
            entitlement_id: args.entitlement_id,
        }
    }
}

impl __sdk::InModule for RevokeSubscriptionEntitlementArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `revoke_subscription_entitlement`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait revoke_subscription_entitlement {
    /// Request that the remote module invoke the reducer `revoke_subscription_entitlement` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`revoke_subscription_entitlement:revoke_subscription_entitlement_then`] to run a callback after the reducer completes.
    fn revoke_subscription_entitlement(
        &self,
        organization_id: u64,
        company_id: u64,
        entitlement_id: u64,
    ) -> __sdk::Result<()> {
        self.revoke_subscription_entitlement_then(
            organization_id,
            company_id,
            entitlement_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `revoke_subscription_entitlement` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn revoke_subscription_entitlement_then(
        &self,
        organization_id: u64,
        company_id: u64,
        entitlement_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl revoke_subscription_entitlement for super::RemoteReducers {
    fn revoke_subscription_entitlement_then(
        &self,
        organization_id: u64,
        company_id: u64,
        entitlement_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RevokeSubscriptionEntitlementArgs {
                organization_id,
                company_id,
                entitlement_id,
            },
            callback,
        )
    }
}
