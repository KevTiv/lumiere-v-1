// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SubscribeToRecordArgs {
    pub organization_id: u64,
    pub res_model: String,
    pub res_id: u64,
    pub subtypes: Vec<String>,
}

impl From<SubscribeToRecordArgs> for super::Reducer {
    fn from(args: SubscribeToRecordArgs) -> Self {
        Self::SubscribeToRecord {
            organization_id: args.organization_id,
            res_model: args.res_model,
            res_id: args.res_id,
            subtypes: args.subtypes,
        }
    }
}

impl __sdk::InModule for SubscribeToRecordArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `subscribe_to_record`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait subscribe_to_record {
    /// Request that the remote module invoke the reducer `subscribe_to_record` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`subscribe_to_record:subscribe_to_record_then`] to run a callback after the reducer completes.
    fn subscribe_to_record(
        &self,
        organization_id: u64,
        res_model: String,
        res_id: u64,
        subtypes: Vec<String>,
    ) -> __sdk::Result<()> {
        self.subscribe_to_record_then(organization_id, res_model, res_id, subtypes, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `subscribe_to_record` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn subscribe_to_record_then(
        &self,
        organization_id: u64,
        res_model: String,
        res_id: u64,
        subtypes: Vec<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl subscribe_to_record for super::RemoteReducers {
    fn subscribe_to_record_then(
        &self,
        organization_id: u64,
        res_model: String,
        res_id: u64,
        subtypes: Vec<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SubscribeToRecordArgs {
                organization_id,
                res_model,
                res_id,
                subtypes,
            },
            callback,
        )
    }
}
