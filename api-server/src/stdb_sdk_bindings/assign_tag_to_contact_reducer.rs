// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AssignTagToContactArgs {
    pub organization_id: u64,
    pub contact_id: u64,
    pub tag_id: u64,
    pub metadata: Option<String>,
}

impl From<AssignTagToContactArgs> for super::Reducer {
    fn from(args: AssignTagToContactArgs) -> Self {
        Self::AssignTagToContact {
            organization_id: args.organization_id,
            contact_id: args.contact_id,
            tag_id: args.tag_id,
            metadata: args.metadata,
        }
    }
}

impl __sdk::InModule for AssignTagToContactArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `assign_tag_to_contact`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait assign_tag_to_contact {
    /// Request that the remote module invoke the reducer `assign_tag_to_contact` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`assign_tag_to_contact:assign_tag_to_contact_then`] to run a callback after the reducer completes.
    fn assign_tag_to_contact(
        &self,
        organization_id: u64,
        contact_id: u64,
        tag_id: u64,
        metadata: Option<String>,
    ) -> __sdk::Result<()> {
        self.assign_tag_to_contact_then(organization_id, contact_id, tag_id, metadata, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `assign_tag_to_contact` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn assign_tag_to_contact_then(
        &self,
        organization_id: u64,
        contact_id: u64,
        tag_id: u64,
        metadata: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl assign_tag_to_contact for super::RemoteReducers {
    fn assign_tag_to_contact_then(
        &self,
        organization_id: u64,
        contact_id: u64,
        tag_id: u64,
        metadata: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AssignTagToContactArgs {
                organization_id,
                contact_id,
                tag_id,
                metadata,
            },
            callback,
        )
    }
}
