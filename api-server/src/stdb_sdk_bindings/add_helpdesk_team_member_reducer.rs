// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddHelpdeskTeamMemberArgs {
    pub organization_id: u64,
    pub team_id: u64,
    pub identity: __sdk::Identity,
}

impl From<AddHelpdeskTeamMemberArgs> for super::Reducer {
    fn from(args: AddHelpdeskTeamMemberArgs) -> Self {
        Self::AddHelpdeskTeamMember {
            organization_id: args.organization_id,
            team_id: args.team_id,
            identity: args.identity,
        }
    }
}

impl __sdk::InModule for AddHelpdeskTeamMemberArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_helpdesk_team_member`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_helpdesk_team_member {
    /// Request that the remote module invoke the reducer `add_helpdesk_team_member` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_helpdesk_team_member:add_helpdesk_team_member_then`] to run a callback after the reducer completes.
    fn add_helpdesk_team_member(
        &self,
        organization_id: u64,
        team_id: u64,
        identity: __sdk::Identity,
    ) -> __sdk::Result<()> {
        self.add_helpdesk_team_member_then(organization_id, team_id, identity, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `add_helpdesk_team_member` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_helpdesk_team_member_then(
        &self,
        organization_id: u64,
        team_id: u64,
        identity: __sdk::Identity,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl add_helpdesk_team_member for super::RemoteReducers {
    fn add_helpdesk_team_member_then(
        &self,
        organization_id: u64,
        team_id: u64,
        identity: __sdk::Identity,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddHelpdeskTeamMemberArgs {
                organization_id,
                team_id,
                identity,
            },
            callback,
        )
    }
}
