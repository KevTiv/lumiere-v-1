// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RemoveHelpdeskTeamMemberArgs {
    pub organization_id: u64,
    pub team_id: u64,
    pub identity: __sdk::Identity,
}

impl From<RemoveHelpdeskTeamMemberArgs> for super::Reducer {
    fn from(args: RemoveHelpdeskTeamMemberArgs) -> Self {
        Self::RemoveHelpdeskTeamMember {
            organization_id: args.organization_id,
            team_id: args.team_id,
            identity: args.identity,
        }
    }
}

impl __sdk::InModule for RemoveHelpdeskTeamMemberArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `remove_helpdesk_team_member`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait remove_helpdesk_team_member {
    /// Request that the remote module invoke the reducer `remove_helpdesk_team_member` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`remove_helpdesk_team_member:remove_helpdesk_team_member_then`] to run a callback after the reducer completes.
    fn remove_helpdesk_team_member(
        &self,
        organization_id: u64,
        team_id: u64,
        identity: __sdk::Identity,
    ) -> __sdk::Result<()> {
        self.remove_helpdesk_team_member_then(organization_id, team_id, identity, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `remove_helpdesk_team_member` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn remove_helpdesk_team_member_then(
        &self,
        organization_id: u64,
        team_id: u64,
        identity: __sdk::Identity,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl remove_helpdesk_team_member for super::RemoteReducers {
    fn remove_helpdesk_team_member_then(
        &self,
        organization_id: u64,
        team_id: u64,
        identity: __sdk::Identity,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RemoveHelpdeskTeamMemberArgs {
                organization_id,
                team_id,
                identity,
            },
            callback,
        )
    }
}
