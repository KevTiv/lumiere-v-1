// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::task_state_type::TaskState;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateTaskStateArgs {
    pub organization_id: u64,
    pub task_id: u64,
    pub state: TaskState,
}

impl From<UpdateTaskStateArgs> for super::Reducer {
    fn from(args: UpdateTaskStateArgs) -> Self {
        Self::UpdateTaskState {
            organization_id: args.organization_id,
            task_id: args.task_id,
            state: args.state,
        }
    }
}

impl __sdk::InModule for UpdateTaskStateArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_task_state`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_task_state {
    /// Request that the remote module invoke the reducer `update_task_state` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_task_state:update_task_state_then`] to run a callback after the reducer completes.
    fn update_task_state(
        &self,
        organization_id: u64,
        task_id: u64,
        state: TaskState,
    ) -> __sdk::Result<()> {
        self.update_task_state_then(organization_id, task_id, state, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_task_state` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_task_state_then(
        &self,
        organization_id: u64,
        task_id: u64,
        state: TaskState,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_task_state for super::RemoteReducers {
    fn update_task_state_then(
        &self,
        organization_id: u64,
        task_id: u64,
        state: TaskState,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateTaskStateArgs {
                organization_id,
                task_id,
                state,
            },
            callback,
        )
    }
}
