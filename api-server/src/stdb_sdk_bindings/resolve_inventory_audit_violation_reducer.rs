// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ResolveInventoryAuditViolationArgs {
    pub violation_id: u64,
}

impl From<ResolveInventoryAuditViolationArgs> for super::Reducer {
    fn from(args: ResolveInventoryAuditViolationArgs) -> Self {
        Self::ResolveInventoryAuditViolation {
            violation_id: args.violation_id,
        }
    }
}

impl __sdk::InModule for ResolveInventoryAuditViolationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `resolve_inventory_audit_violation`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait resolve_inventory_audit_violation {
    /// Request that the remote module invoke the reducer `resolve_inventory_audit_violation` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`resolve_inventory_audit_violation:resolve_inventory_audit_violation_then`] to run a callback after the reducer completes.
    fn resolve_inventory_audit_violation(&self, violation_id: u64) -> __sdk::Result<()> {
        self.resolve_inventory_audit_violation_then(violation_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `resolve_inventory_audit_violation` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn resolve_inventory_audit_violation_then(
        &self,
        violation_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl resolve_inventory_audit_violation for super::RemoteReducers {
    fn resolve_inventory_audit_violation_then(
        &self,
        violation_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ResolveInventoryAuditViolationArgs { violation_id },
            callback,
        )
    }
}
