// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PassQualityCheckArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub check_id: u64,
    pub measure: Option<f64>,
    pub note: Option<String>,
    pub picture: Option<String>,
}

impl From<PassQualityCheckArgs> for super::Reducer {
    fn from(args: PassQualityCheckArgs) -> Self {
        Self::PassQualityCheck {
            organization_id: args.organization_id,
            company_id: args.company_id,
            check_id: args.check_id,
            measure: args.measure,
            note: args.note,
            picture: args.picture,
        }
    }
}

impl __sdk::InModule for PassQualityCheckArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `pass_quality_check`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait pass_quality_check {
    /// Request that the remote module invoke the reducer `pass_quality_check` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`pass_quality_check:pass_quality_check_then`] to run a callback after the reducer completes.
    fn pass_quality_check(
        &self,
        organization_id: u64,
        company_id: u64,
        check_id: u64,
        measure: Option<f64>,
        note: Option<String>,
        picture: Option<String>,
    ) -> __sdk::Result<()> {
        self.pass_quality_check_then(
            organization_id,
            company_id,
            check_id,
            measure,
            note,
            picture,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `pass_quality_check` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn pass_quality_check_then(
        &self,
        organization_id: u64,
        company_id: u64,
        check_id: u64,
        measure: Option<f64>,
        note: Option<String>,
        picture: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl pass_quality_check for super::RemoteReducers {
    fn pass_quality_check_then(
        &self,
        organization_id: u64,
        company_id: u64,
        check_id: u64,
        measure: Option<f64>,
        note: Option<String>,
        picture: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PassQualityCheckArgs {
                organization_id,
                company_id,
                check_id,
                measure,
                note,
                picture,
            },
            callback,
        )
    }
}
