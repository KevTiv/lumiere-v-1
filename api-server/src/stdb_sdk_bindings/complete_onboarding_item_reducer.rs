// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::complete_onboarding_item_params_type::CompleteOnboardingItemParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CompleteOnboardingItemArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub employee_id: u64,
    pub params: CompleteOnboardingItemParams,
}

impl From<CompleteOnboardingItemArgs> for super::Reducer {
    fn from(args: CompleteOnboardingItemArgs) -> Self {
        Self::CompleteOnboardingItem {
            organization_id: args.organization_id,
            company_id: args.company_id,
            employee_id: args.employee_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for CompleteOnboardingItemArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `complete_onboarding_item`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait complete_onboarding_item {
    /// Request that the remote module invoke the reducer `complete_onboarding_item` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`complete_onboarding_item:complete_onboarding_item_then`] to run a callback after the reducer completes.
    fn complete_onboarding_item(
        &self,
        organization_id: u64,
        company_id: u64,
        employee_id: u64,
        params: CompleteOnboardingItemParams,
    ) -> __sdk::Result<()> {
        self.complete_onboarding_item_then(
            organization_id,
            company_id,
            employee_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `complete_onboarding_item` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn complete_onboarding_item_then(
        &self,
        organization_id: u64,
        company_id: u64,
        employee_id: u64,
        params: CompleteOnboardingItemParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl complete_onboarding_item for super::RemoteReducers {
    fn complete_onboarding_item_then(
        &self,
        organization_id: u64,
        company_id: u64,
        employee_id: u64,
        params: CompleteOnboardingItemParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CompleteOnboardingItemArgs {
                organization_id,
                company_id,
                employee_id,
                params,
            },
            callback,
        )
    }
}
