// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AssignQualityAlertArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub alert_id: u64,
    pub user_id: __sdk::Identity,
}

impl From<AssignQualityAlertArgs> for super::Reducer {
    fn from(args: AssignQualityAlertArgs) -> Self {
        Self::AssignQualityAlert {
            organization_id: args.organization_id,
            company_id: args.company_id,
            alert_id: args.alert_id,
            user_id: args.user_id,
        }
    }
}

impl __sdk::InModule for AssignQualityAlertArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `assign_quality_alert`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait assign_quality_alert {
    /// Request that the remote module invoke the reducer `assign_quality_alert` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`assign_quality_alert:assign_quality_alert_then`] to run a callback after the reducer completes.
    fn assign_quality_alert(
        &self,
        organization_id: u64,
        company_id: u64,
        alert_id: u64,
        user_id: __sdk::Identity,
    ) -> __sdk::Result<()> {
        self.assign_quality_alert_then(organization_id, company_id, alert_id, user_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `assign_quality_alert` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn assign_quality_alert_then(
        &self,
        organization_id: u64,
        company_id: u64,
        alert_id: u64,
        user_id: __sdk::Identity,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl assign_quality_alert for super::RemoteReducers {
    fn assign_quality_alert_then(
        &self,
        organization_id: u64,
        company_id: u64,
        alert_id: u64,
        user_id: __sdk::Identity,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AssignQualityAlertArgs {
                organization_id,
                company_id,
                alert_id,
                user_id,
            },
            callback,
        )
    }
}
