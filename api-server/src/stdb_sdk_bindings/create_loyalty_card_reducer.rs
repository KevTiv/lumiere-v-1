// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateLoyaltyCardArgs {
    pub organization_id: u64,
    pub partner_id: Option<u64>,
    pub program_id: u64,
    pub code: String,
    pub points: f64,
}

impl From<CreateLoyaltyCardArgs> for super::Reducer {
    fn from(args: CreateLoyaltyCardArgs) -> Self {
        Self::CreateLoyaltyCard {
            organization_id: args.organization_id,
            partner_id: args.partner_id,
            program_id: args.program_id,
            code: args.code,
            points: args.points,
        }
    }
}

impl __sdk::InModule for CreateLoyaltyCardArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_loyalty_card`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_loyalty_card {
    /// Request that the remote module invoke the reducer `create_loyalty_card` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_loyalty_card:create_loyalty_card_then`] to run a callback after the reducer completes.
    fn create_loyalty_card(
        &self,
        organization_id: u64,
        partner_id: Option<u64>,
        program_id: u64,
        code: String,
        points: f64,
    ) -> __sdk::Result<()> {
        self.create_loyalty_card_then(
            organization_id,
            partner_id,
            program_id,
            code,
            points,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_loyalty_card` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_loyalty_card_then(
        &self,
        organization_id: u64,
        partner_id: Option<u64>,
        program_id: u64,
        code: String,
        points: f64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_loyalty_card for super::RemoteReducers {
    fn create_loyalty_card_then(
        &self,
        organization_id: u64,
        partner_id: Option<u64>,
        program_id: u64,
        code: String,
        points: f64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateLoyaltyCardArgs {
                organization_id,
                partner_id,
                program_id,
                code,
                points,
            },
            callback,
        )
    }
}
