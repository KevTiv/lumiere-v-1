// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GetFormConfigurationArgs {
    pub organization_id: u64,
    pub module_id: String,
    pub form_id: String,
}

impl From<GetFormConfigurationArgs> for super::Reducer {
    fn from(args: GetFormConfigurationArgs) -> Self {
        Self::GetFormConfiguration {
            organization_id: args.organization_id,
            module_id: args.module_id,
            form_id: args.form_id,
        }
    }
}

impl __sdk::InModule for GetFormConfigurationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `get_form_configuration`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait get_form_configuration {
    /// Request that the remote module invoke the reducer `get_form_configuration` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`get_form_configuration:get_form_configuration_then`] to run a callback after the reducer completes.
    fn get_form_configuration(
        &self,
        organization_id: u64,
        module_id: String,
        form_id: String,
    ) -> __sdk::Result<()> {
        self.get_form_configuration_then(organization_id, module_id, form_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `get_form_configuration` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn get_form_configuration_then(
        &self,
        organization_id: u64,
        module_id: String,
        form_id: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl get_form_configuration for super::RemoteReducers {
    fn get_form_configuration_then(
        &self,
        organization_id: u64,
        module_id: String,
        form_id: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GetFormConfigurationArgs {
                organization_id,
                module_id,
                form_id,
            },
            callback,
        )
    }
}
