// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::apply_omnichannel_allocation_params_type::ApplyOmnichannelAllocationParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ApplyOmnichannelAllocationArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub order_id: u64,
    pub params: ApplyOmnichannelAllocationParams,
}

impl From<ApplyOmnichannelAllocationArgs> for super::Reducer {
    fn from(args: ApplyOmnichannelAllocationArgs) -> Self {
        Self::ApplyOmnichannelAllocation {
            organization_id: args.organization_id,
            company_id: args.company_id,
            order_id: args.order_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for ApplyOmnichannelAllocationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `apply_omnichannel_allocation`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait apply_omnichannel_allocation {
    /// Request that the remote module invoke the reducer `apply_omnichannel_allocation` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`apply_omnichannel_allocation:apply_omnichannel_allocation_then`] to run a callback after the reducer completes.
    fn apply_omnichannel_allocation(
        &self,
        organization_id: u64,
        company_id: u64,
        order_id: u64,
        params: ApplyOmnichannelAllocationParams,
    ) -> __sdk::Result<()> {
        self.apply_omnichannel_allocation_then(
            organization_id,
            company_id,
            order_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `apply_omnichannel_allocation` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn apply_omnichannel_allocation_then(
        &self,
        organization_id: u64,
        company_id: u64,
        order_id: u64,
        params: ApplyOmnichannelAllocationParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl apply_omnichannel_allocation for super::RemoteReducers {
    fn apply_omnichannel_allocation_then(
        &self,
        organization_id: u64,
        company_id: u64,
        order_id: u64,
        params: ApplyOmnichannelAllocationParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ApplyOmnichannelAllocationArgs {
                organization_id,
                company_id,
                order_id,
                params,
            },
            callback,
        )
    }
}
