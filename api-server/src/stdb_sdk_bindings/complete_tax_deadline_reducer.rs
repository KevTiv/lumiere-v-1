// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CompleteTaxDeadlineArgs {
    pub organization_id: u64,
    pub deadline_id: u64,
}

impl From<CompleteTaxDeadlineArgs> for super::Reducer {
    fn from(args: CompleteTaxDeadlineArgs) -> Self {
        Self::CompleteTaxDeadline {
            organization_id: args.organization_id,
            deadline_id: args.deadline_id,
        }
    }
}

impl __sdk::InModule for CompleteTaxDeadlineArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `complete_tax_deadline`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait complete_tax_deadline {
    /// Request that the remote module invoke the reducer `complete_tax_deadline` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`complete_tax_deadline:complete_tax_deadline_then`] to run a callback after the reducer completes.
    fn complete_tax_deadline(&self, organization_id: u64, deadline_id: u64) -> __sdk::Result<()> {
        self.complete_tax_deadline_then(organization_id, deadline_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `complete_tax_deadline` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn complete_tax_deadline_then(
        &self,
        organization_id: u64,
        deadline_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl complete_tax_deadline for super::RemoteReducers {
    fn complete_tax_deadline_then(
        &self,
        organization_id: u64,
        deadline_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CompleteTaxDeadlineArgs {
                organization_id,
                deadline_id,
            },
            callback,
        )
    }
}
