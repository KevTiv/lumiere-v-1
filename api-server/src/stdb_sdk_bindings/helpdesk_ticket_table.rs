// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::helpdesk_ticket_state_type::HelpdeskTicketState;
use super::helpdesk_ticket_type::HelpdeskTicket;
use super::ticket_priority_type::TicketPriority;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `helpdesk_ticket`.
///
/// Obtain a handle from the [`HelpdeskTicketTableAccess::helpdesk_ticket`] method on [`super::RemoteTables`],
/// like `ctx.db.helpdesk_ticket()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.helpdesk_ticket().on_insert(...)`.
pub struct HelpdeskTicketTableHandle<'ctx> {
    imp: __sdk::TableHandle<HelpdeskTicket>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `helpdesk_ticket`.
///
/// Implemented for [`super::RemoteTables`].
pub trait HelpdeskTicketTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`HelpdeskTicketTableHandle`], which mediates access to the table `helpdesk_ticket`.
    fn helpdesk_ticket(&self) -> HelpdeskTicketTableHandle<'_>;
}

impl HelpdeskTicketTableAccess for super::RemoteTables {
    fn helpdesk_ticket(&self) -> HelpdeskTicketTableHandle<'_> {
        HelpdeskTicketTableHandle {
            imp: self.imp.get_table::<HelpdeskTicket>("helpdesk_ticket"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct HelpdeskTicketInsertCallbackId(__sdk::CallbackId);
pub struct HelpdeskTicketDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for HelpdeskTicketTableHandle<'ctx> {
    type Row = HelpdeskTicket;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = HelpdeskTicket> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = HelpdeskTicketInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> HelpdeskTicketInsertCallbackId {
        HelpdeskTicketInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: HelpdeskTicketInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = HelpdeskTicketDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> HelpdeskTicketDeleteCallbackId {
        HelpdeskTicketDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: HelpdeskTicketDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct HelpdeskTicketUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for HelpdeskTicketTableHandle<'ctx> {
    type UpdateCallbackId = HelpdeskTicketUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> HelpdeskTicketUpdateCallbackId {
        HelpdeskTicketUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: HelpdeskTicketUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `helpdesk_ticket`,
/// which allows point queries on the field of the same name
/// via the [`HelpdeskTicketIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.helpdesk_ticket().id().find(...)`.
pub struct HelpdeskTicketIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<HelpdeskTicket, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> HelpdeskTicketTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `helpdesk_ticket`.
    pub fn id(&self) -> HelpdeskTicketIdUnique<'ctx> {
        HelpdeskTicketIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> HelpdeskTicketIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<HelpdeskTicket> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<HelpdeskTicket>("helpdesk_ticket");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<HelpdeskTicket>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<HelpdeskTicket>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `HelpdeskTicket`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait helpdesk_ticketQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `HelpdeskTicket`.
    fn helpdesk_ticket(&self) -> __sdk::__query_builder::Table<HelpdeskTicket>;
}

impl helpdesk_ticketQueryTableAccess for __sdk::QueryTableAccessor {
    fn helpdesk_ticket(&self) -> __sdk::__query_builder::Table<HelpdeskTicket> {
        __sdk::__query_builder::Table::new("helpdesk_ticket")
    }
}
