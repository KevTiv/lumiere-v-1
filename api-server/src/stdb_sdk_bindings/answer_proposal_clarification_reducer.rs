// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::answer_proposal_clarification_params_type::AnswerProposalClarificationParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AnswerProposalClarificationArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub clarification_id: u64,
    pub params: AnswerProposalClarificationParams,
}

impl From<AnswerProposalClarificationArgs> for super::Reducer {
    fn from(args: AnswerProposalClarificationArgs) -> Self {
        Self::AnswerProposalClarification {
            organization_id: args.organization_id,
            company_id: args.company_id,
            clarification_id: args.clarification_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for AnswerProposalClarificationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `answer_proposal_clarification`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait answer_proposal_clarification {
    /// Request that the remote module invoke the reducer `answer_proposal_clarification` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`answer_proposal_clarification:answer_proposal_clarification_then`] to run a callback after the reducer completes.
    fn answer_proposal_clarification(
        &self,
        organization_id: u64,
        company_id: u64,
        clarification_id: u64,
        params: AnswerProposalClarificationParams,
    ) -> __sdk::Result<()> {
        self.answer_proposal_clarification_then(
            organization_id,
            company_id,
            clarification_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `answer_proposal_clarification` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn answer_proposal_clarification_then(
        &self,
        organization_id: u64,
        company_id: u64,
        clarification_id: u64,
        params: AnswerProposalClarificationParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl answer_proposal_clarification for super::RemoteReducers {
    fn answer_proposal_clarification_then(
        &self,
        organization_id: u64,
        company_id: u64,
        clarification_id: u64,
        params: AnswerProposalClarificationParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AnswerProposalClarificationArgs {
                organization_id,
                company_id,
                clarification_id,
                params,
            },
            callback,
        )
    }
}
