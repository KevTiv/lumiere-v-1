// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::apply_subscription_bundle_params_type::ApplySubscriptionBundleParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ApplySubscriptionBundleArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub subscription_id: u64,
    pub params: ApplySubscriptionBundleParams,
}

impl From<ApplySubscriptionBundleArgs> for super::Reducer {
    fn from(args: ApplySubscriptionBundleArgs) -> Self {
        Self::ApplySubscriptionBundle {
            organization_id: args.organization_id,
            company_id: args.company_id,
            subscription_id: args.subscription_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for ApplySubscriptionBundleArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `apply_subscription_bundle`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait apply_subscription_bundle {
    /// Request that the remote module invoke the reducer `apply_subscription_bundle` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`apply_subscription_bundle:apply_subscription_bundle_then`] to run a callback after the reducer completes.
    fn apply_subscription_bundle(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: ApplySubscriptionBundleParams,
    ) -> __sdk::Result<()> {
        self.apply_subscription_bundle_then(
            organization_id,
            company_id,
            subscription_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `apply_subscription_bundle` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn apply_subscription_bundle_then(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: ApplySubscriptionBundleParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl apply_subscription_bundle for super::RemoteReducers {
    fn apply_subscription_bundle_then(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: ApplySubscriptionBundleParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ApplySubscriptionBundleArgs {
                organization_id,
                company_id,
                subscription_id,
                params,
            },
            callback,
        )
    }
}
