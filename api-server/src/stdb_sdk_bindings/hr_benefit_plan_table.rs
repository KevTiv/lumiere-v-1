// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::hr_benefit_plan_type::HrBenefitPlan;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `hr_benefit_plan`.
///
/// Obtain a handle from the [`HrBenefitPlanTableAccess::hr_benefit_plan`] method on [`super::RemoteTables`],
/// like `ctx.db.hr_benefit_plan()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.hr_benefit_plan().on_insert(...)`.
pub struct HrBenefitPlanTableHandle<'ctx> {
    imp: __sdk::TableHandle<HrBenefitPlan>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `hr_benefit_plan`.
///
/// Implemented for [`super::RemoteTables`].
pub trait HrBenefitPlanTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`HrBenefitPlanTableHandle`], which mediates access to the table `hr_benefit_plan`.
    fn hr_benefit_plan(&self) -> HrBenefitPlanTableHandle<'_>;
}

impl HrBenefitPlanTableAccess for super::RemoteTables {
    fn hr_benefit_plan(&self) -> HrBenefitPlanTableHandle<'_> {
        HrBenefitPlanTableHandle {
            imp: self.imp.get_table::<HrBenefitPlan>("hr_benefit_plan"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct HrBenefitPlanInsertCallbackId(__sdk::CallbackId);
pub struct HrBenefitPlanDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for HrBenefitPlanTableHandle<'ctx> {
    type Row = HrBenefitPlan;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = HrBenefitPlan> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = HrBenefitPlanInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> HrBenefitPlanInsertCallbackId {
        HrBenefitPlanInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: HrBenefitPlanInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = HrBenefitPlanDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> HrBenefitPlanDeleteCallbackId {
        HrBenefitPlanDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: HrBenefitPlanDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct HrBenefitPlanUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for HrBenefitPlanTableHandle<'ctx> {
    type UpdateCallbackId = HrBenefitPlanUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> HrBenefitPlanUpdateCallbackId {
        HrBenefitPlanUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: HrBenefitPlanUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `hr_benefit_plan`,
/// which allows point queries on the field of the same name
/// via the [`HrBenefitPlanIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.hr_benefit_plan().id().find(...)`.
pub struct HrBenefitPlanIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<HrBenefitPlan, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> HrBenefitPlanTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `hr_benefit_plan`.
    pub fn id(&self) -> HrBenefitPlanIdUnique<'ctx> {
        HrBenefitPlanIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> HrBenefitPlanIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<HrBenefitPlan> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<HrBenefitPlan>("hr_benefit_plan");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<HrBenefitPlan>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<HrBenefitPlan>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `HrBenefitPlan`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait hr_benefit_planQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `HrBenefitPlan`.
    fn hr_benefit_plan(&self) -> __sdk::__query_builder::Table<HrBenefitPlan>;
}

impl hr_benefit_planQueryTableAccess for __sdk::QueryTableAccessor {
    fn hr_benefit_plan(&self) -> __sdk::__query_builder::Table<HrBenefitPlan> {
        __sdk::__query_builder::Table::new("hr_benefit_plan")
    }
}
