// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UnlockDocumentArgs {
    pub organization_id: u64,
    pub document_id: u64,
}

impl From<UnlockDocumentArgs> for super::Reducer {
    fn from(args: UnlockDocumentArgs) -> Self {
        Self::UnlockDocument {
            organization_id: args.organization_id,
            document_id: args.document_id,
        }
    }
}

impl __sdk::InModule for UnlockDocumentArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `unlock_document`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait unlock_document {
    /// Request that the remote module invoke the reducer `unlock_document` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`unlock_document:unlock_document_then`] to run a callback after the reducer completes.
    fn unlock_document(&self, organization_id: u64, document_id: u64) -> __sdk::Result<()> {
        self.unlock_document_then(organization_id, document_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `unlock_document` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn unlock_document_then(
        &self,
        organization_id: u64,
        document_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl unlock_document for super::RemoteReducers {
    fn unlock_document_then(
        &self,
        organization_id: u64,
        document_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UnlockDocumentArgs {
                organization_id,
                document_id,
            },
            callback,
        )
    }
}
