// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ClosePosSessionArgs {
    pub organization_id: u64,
    pub session_id: u64,
    pub cash_register_balance_end_real: f64,
}

impl From<ClosePosSessionArgs> for super::Reducer {
    fn from(args: ClosePosSessionArgs) -> Self {
        Self::ClosePosSession {
            organization_id: args.organization_id,
            session_id: args.session_id,
            cash_register_balance_end_real: args.cash_register_balance_end_real,
        }
    }
}

impl __sdk::InModule for ClosePosSessionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `close_pos_session`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait close_pos_session {
    /// Request that the remote module invoke the reducer `close_pos_session` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`close_pos_session:close_pos_session_then`] to run a callback after the reducer completes.
    fn close_pos_session(
        &self,
        organization_id: u64,
        session_id: u64,
        cash_register_balance_end_real: f64,
    ) -> __sdk::Result<()> {
        self.close_pos_session_then(
            organization_id,
            session_id,
            cash_register_balance_end_real,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `close_pos_session` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn close_pos_session_then(
        &self,
        organization_id: u64,
        session_id: u64,
        cash_register_balance_end_real: f64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl close_pos_session for super::RemoteReducers {
    fn close_pos_session_then(
        &self,
        organization_id: u64,
        session_id: u64,
        cash_register_balance_end_real: f64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ClosePosSessionArgs {
                organization_id,
                session_id,
                cash_register_balance_end_real,
            },
            callback,
        )
    }
}
