// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::upsert_proposal_section_params_type::UpsertProposalSectionParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ResolveProposalSectionConflictArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub proposal_id: u64,
    pub section_id: u64,
    pub params: UpsertProposalSectionParams,
}

impl From<ResolveProposalSectionConflictArgs> for super::Reducer {
    fn from(args: ResolveProposalSectionConflictArgs) -> Self {
        Self::ResolveProposalSectionConflict {
            organization_id: args.organization_id,
            company_id: args.company_id,
            proposal_id: args.proposal_id,
            section_id: args.section_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for ResolveProposalSectionConflictArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `resolve_proposal_section_conflict`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait resolve_proposal_section_conflict {
    /// Request that the remote module invoke the reducer `resolve_proposal_section_conflict` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`resolve_proposal_section_conflict:resolve_proposal_section_conflict_then`] to run a callback after the reducer completes.
    fn resolve_proposal_section_conflict(
        &self,
        organization_id: u64,
        company_id: u64,
        proposal_id: u64,
        section_id: u64,
        params: UpsertProposalSectionParams,
    ) -> __sdk::Result<()> {
        self.resolve_proposal_section_conflict_then(
            organization_id,
            company_id,
            proposal_id,
            section_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `resolve_proposal_section_conflict` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn resolve_proposal_section_conflict_then(
        &self,
        organization_id: u64,
        company_id: u64,
        proposal_id: u64,
        section_id: u64,
        params: UpsertProposalSectionParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl resolve_proposal_section_conflict for super::RemoteReducers {
    fn resolve_proposal_section_conflict_then(
        &self,
        organization_id: u64,
        company_id: u64,
        proposal_id: u64,
        section_id: u64,
        params: UpsertProposalSectionParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ResolveProposalSectionConflictArgs {
                organization_id,
                company_id,
                proposal_id,
                section_id,
                params,
            },
            callback,
        )
    }
}
