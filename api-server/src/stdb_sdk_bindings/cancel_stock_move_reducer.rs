// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::company_scope_params_type::CompanyScopeParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CancelStockMoveArgs {
    pub organization_id: u64,
    pub move_id: u64,
    pub params: CompanyScopeParams,
}

impl From<CancelStockMoveArgs> for super::Reducer {
    fn from(args: CancelStockMoveArgs) -> Self {
        Self::CancelStockMove {
            organization_id: args.organization_id,
            move_id: args.move_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for CancelStockMoveArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `cancel_stock_move`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait cancel_stock_move {
    /// Request that the remote module invoke the reducer `cancel_stock_move` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`cancel_stock_move:cancel_stock_move_then`] to run a callback after the reducer completes.
    fn cancel_stock_move(
        &self,
        organization_id: u64,
        move_id: u64,
        params: CompanyScopeParams,
    ) -> __sdk::Result<()> {
        self.cancel_stock_move_then(organization_id, move_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `cancel_stock_move` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn cancel_stock_move_then(
        &self,
        organization_id: u64,
        move_id: u64,
        params: CompanyScopeParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl cancel_stock_move for super::RemoteReducers {
    fn cancel_stock_move_then(
        &self,
        organization_id: u64,
        move_id: u64,
        params: CompanyScopeParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CancelStockMoveArgs {
                organization_id,
                move_id,
                params,
            },
            callback,
        )
    }
}
