// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RefreshSaleOrderPromiseDatesArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub order_id: u64,
}

impl From<RefreshSaleOrderPromiseDatesArgs> for super::Reducer {
    fn from(args: RefreshSaleOrderPromiseDatesArgs) -> Self {
        Self::RefreshSaleOrderPromiseDates {
            organization_id: args.organization_id,
            company_id: args.company_id,
            order_id: args.order_id,
        }
    }
}

impl __sdk::InModule for RefreshSaleOrderPromiseDatesArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `refresh_sale_order_promise_dates`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait refresh_sale_order_promise_dates {
    /// Request that the remote module invoke the reducer `refresh_sale_order_promise_dates` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`refresh_sale_order_promise_dates:refresh_sale_order_promise_dates_then`] to run a callback after the reducer completes.
    fn refresh_sale_order_promise_dates(
        &self,
        organization_id: u64,
        company_id: u64,
        order_id: u64,
    ) -> __sdk::Result<()> {
        self.refresh_sale_order_promise_dates_then(organization_id, company_id, order_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `refresh_sale_order_promise_dates` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn refresh_sale_order_promise_dates_then(
        &self,
        organization_id: u64,
        company_id: u64,
        order_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl refresh_sale_order_promise_dates for super::RemoteReducers {
    fn refresh_sale_order_promise_dates_then(
        &self,
        organization_id: u64,
        company_id: u64,
        order_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RefreshSaleOrderPromiseDatesArgs {
                organization_id,
                company_id,
                order_id,
            },
            callback,
        )
    }
}
