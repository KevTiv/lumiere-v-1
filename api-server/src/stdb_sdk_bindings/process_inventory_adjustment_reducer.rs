// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ProcessInventoryAdjustmentArgs {
    pub organization_id: u64,
    pub adjustment_id: u64,
}

impl From<ProcessInventoryAdjustmentArgs> for super::Reducer {
    fn from(args: ProcessInventoryAdjustmentArgs) -> Self {
        Self::ProcessInventoryAdjustment {
            organization_id: args.organization_id,
            adjustment_id: args.adjustment_id,
        }
    }
}

impl __sdk::InModule for ProcessInventoryAdjustmentArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `process_inventory_adjustment`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait process_inventory_adjustment {
    /// Request that the remote module invoke the reducer `process_inventory_adjustment` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`process_inventory_adjustment:process_inventory_adjustment_then`] to run a callback after the reducer completes.
    fn process_inventory_adjustment(
        &self,
        organization_id: u64,
        adjustment_id: u64,
    ) -> __sdk::Result<()> {
        self.process_inventory_adjustment_then(organization_id, adjustment_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `process_inventory_adjustment` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn process_inventory_adjustment_then(
        &self,
        organization_id: u64,
        adjustment_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl process_inventory_adjustment for super::RemoteReducers {
    fn process_inventory_adjustment_then(
        &self,
        organization_id: u64,
        adjustment_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ProcessInventoryAdjustmentArgs {
                organization_id,
                adjustment_id,
            },
            callback,
        )
    }
}
