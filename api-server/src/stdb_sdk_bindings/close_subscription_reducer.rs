// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::close_subscription_params_type::CloseSubscriptionParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CloseSubscriptionArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub subscription_id: u64,
    pub params: CloseSubscriptionParams,
}

impl From<CloseSubscriptionArgs> for super::Reducer {
    fn from(args: CloseSubscriptionArgs) -> Self {
        Self::CloseSubscription {
            organization_id: args.organization_id,
            company_id: args.company_id,
            subscription_id: args.subscription_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for CloseSubscriptionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `close_subscription`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait close_subscription {
    /// Request that the remote module invoke the reducer `close_subscription` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`close_subscription:close_subscription_then`] to run a callback after the reducer completes.
    fn close_subscription(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: CloseSubscriptionParams,
    ) -> __sdk::Result<()> {
        self.close_subscription_then(
            organization_id,
            company_id,
            subscription_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `close_subscription` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn close_subscription_then(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: CloseSubscriptionParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl close_subscription for super::RemoteReducers {
    fn close_subscription_then(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: CloseSubscriptionParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CloseSubscriptionArgs {
                organization_id,
                company_id,
                subscription_id,
                params,
            },
            callback,
        )
    }
}
