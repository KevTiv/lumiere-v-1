// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CloseFiscalYearArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub fiscal_year_id: u64,
}

impl From<CloseFiscalYearArgs> for super::Reducer {
    fn from(args: CloseFiscalYearArgs) -> Self {
        Self::CloseFiscalYear {
            organization_id: args.organization_id,
            company_id: args.company_id,
            fiscal_year_id: args.fiscal_year_id,
        }
    }
}

impl __sdk::InModule for CloseFiscalYearArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `close_fiscal_year`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait close_fiscal_year {
    /// Request that the remote module invoke the reducer `close_fiscal_year` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`close_fiscal_year:close_fiscal_year_then`] to run a callback after the reducer completes.
    fn close_fiscal_year(
        &self,
        organization_id: u64,
        company_id: u64,
        fiscal_year_id: u64,
    ) -> __sdk::Result<()> {
        self.close_fiscal_year_then(organization_id, company_id, fiscal_year_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `close_fiscal_year` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn close_fiscal_year_then(
        &self,
        organization_id: u64,
        company_id: u64,
        fiscal_year_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl close_fiscal_year for super::RemoteReducers {
    fn close_fiscal_year_then(
        &self,
        organization_id: u64,
        company_id: u64,
        fiscal_year_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CloseFiscalYearArgs {
                organization_id,
                company_id,
                fiscal_year_id,
            },
            callback,
        )
    }
}
