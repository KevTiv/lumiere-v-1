// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ClaimHubWithTokenArgs {
    pub token: String,
    pub serial: String,
    pub name: String,
    pub ip_address: Option<String>,
    pub firmware_version: Option<String>,
}

impl From<ClaimHubWithTokenArgs> for super::Reducer {
    fn from(args: ClaimHubWithTokenArgs) -> Self {
        Self::ClaimHubWithToken {
            token: args.token,
            serial: args.serial,
            name: args.name,
            ip_address: args.ip_address,
            firmware_version: args.firmware_version,
        }
    }
}

impl __sdk::InModule for ClaimHubWithTokenArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `claim_hub_with_token`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait claim_hub_with_token {
    /// Request that the remote module invoke the reducer `claim_hub_with_token` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`claim_hub_with_token:claim_hub_with_token_then`] to run a callback after the reducer completes.
    fn claim_hub_with_token(
        &self,
        token: String,
        serial: String,
        name: String,
        ip_address: Option<String>,
        firmware_version: Option<String>,
    ) -> __sdk::Result<()> {
        self.claim_hub_with_token_then(token, serial, name, ip_address, firmware_version, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `claim_hub_with_token` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn claim_hub_with_token_then(
        &self,
        token: String,
        serial: String,
        name: String,
        ip_address: Option<String>,
        firmware_version: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl claim_hub_with_token for super::RemoteReducers {
    fn claim_hub_with_token_then(
        &self,
        token: String,
        serial: String,
        name: String,
        ip_address: Option<String>,
        firmware_version: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ClaimHubWithTokenArgs {
                token,
                serial,
                name,
                ip_address,
                firmware_version,
            },
            callback,
        )
    }
}
