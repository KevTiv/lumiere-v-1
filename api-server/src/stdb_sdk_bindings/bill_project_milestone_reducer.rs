// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::bill_project_milestone_params_type::BillProjectMilestoneParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BillProjectMilestoneArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub milestone_id: u64,
    pub params: BillProjectMilestoneParams,
}

impl From<BillProjectMilestoneArgs> for super::Reducer {
    fn from(args: BillProjectMilestoneArgs) -> Self {
        Self::BillProjectMilestone {
            organization_id: args.organization_id,
            company_id: args.company_id,
            milestone_id: args.milestone_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for BillProjectMilestoneArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `bill_project_milestone`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait bill_project_milestone {
    /// Request that the remote module invoke the reducer `bill_project_milestone` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`bill_project_milestone:bill_project_milestone_then`] to run a callback after the reducer completes.
    fn bill_project_milestone(
        &self,
        organization_id: u64,
        company_id: u64,
        milestone_id: u64,
        params: BillProjectMilestoneParams,
    ) -> __sdk::Result<()> {
        self.bill_project_milestone_then(
            organization_id,
            company_id,
            milestone_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `bill_project_milestone` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn bill_project_milestone_then(
        &self,
        organization_id: u64,
        company_id: u64,
        milestone_id: u64,
        params: BillProjectMilestoneParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl bill_project_milestone for super::RemoteReducers {
    fn bill_project_milestone_then(
        &self,
        organization_id: u64,
        company_id: u64,
        milestone_id: u64,
        params: BillProjectMilestoneParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            BillProjectMilestoneArgs {
                organization_id,
                company_id,
                milestone_id,
                params,
            },
            callback,
        )
    }
}
