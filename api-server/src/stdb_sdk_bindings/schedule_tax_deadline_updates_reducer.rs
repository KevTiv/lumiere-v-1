// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ScheduleTaxDeadlineUpdatesArgs {
    pub organization_id: Option<u64>,
}

impl From<ScheduleTaxDeadlineUpdatesArgs> for super::Reducer {
    fn from(args: ScheduleTaxDeadlineUpdatesArgs) -> Self {
        Self::ScheduleTaxDeadlineUpdates {
            organization_id: args.organization_id,
        }
    }
}

impl __sdk::InModule for ScheduleTaxDeadlineUpdatesArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `schedule_tax_deadline_updates`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait schedule_tax_deadline_updates {
    /// Request that the remote module invoke the reducer `schedule_tax_deadline_updates` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`schedule_tax_deadline_updates:schedule_tax_deadline_updates_then`] to run a callback after the reducer completes.
    fn schedule_tax_deadline_updates(&self, organization_id: Option<u64>) -> __sdk::Result<()> {
        self.schedule_tax_deadline_updates_then(organization_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `schedule_tax_deadline_updates` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn schedule_tax_deadline_updates_then(
        &self,
        organization_id: Option<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl schedule_tax_deadline_updates for super::RemoteReducers {
    fn schedule_tax_deadline_updates_then(
        &self,
        organization_id: Option<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ScheduleTaxDeadlineUpdatesArgs { organization_id },
            callback,
        )
    }
}
