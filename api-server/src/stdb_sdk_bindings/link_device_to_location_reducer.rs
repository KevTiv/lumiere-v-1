// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct LinkDeviceToLocationArgs {
    pub organization_id: u64,
    pub device_id: u64,
    pub location_id: u64,
}

impl From<LinkDeviceToLocationArgs> for super::Reducer {
    fn from(args: LinkDeviceToLocationArgs) -> Self {
        Self::LinkDeviceToLocation {
            organization_id: args.organization_id,
            device_id: args.device_id,
            location_id: args.location_id,
        }
    }
}

impl __sdk::InModule for LinkDeviceToLocationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `link_device_to_location`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait link_device_to_location {
    /// Request that the remote module invoke the reducer `link_device_to_location` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`link_device_to_location:link_device_to_location_then`] to run a callback after the reducer completes.
    fn link_device_to_location(
        &self,
        organization_id: u64,
        device_id: u64,
        location_id: u64,
    ) -> __sdk::Result<()> {
        self.link_device_to_location_then(organization_id, device_id, location_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `link_device_to_location` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn link_device_to_location_then(
        &self,
        organization_id: u64,
        device_id: u64,
        location_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl link_device_to_location for super::RemoteReducers {
    fn link_device_to_location_then(
        &self,
        organization_id: u64,
        device_id: u64,
        location_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            LinkDeviceToLocationArgs {
                organization_id,
                device_id,
                location_id,
            },
            callback,
        )
    }
}
