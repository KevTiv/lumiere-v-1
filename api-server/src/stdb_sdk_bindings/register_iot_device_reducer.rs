// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::register_device_params_type::RegisterDeviceParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RegisterIotDeviceArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub hub_id: u64,
    pub params: RegisterDeviceParams,
}

impl From<RegisterIotDeviceArgs> for super::Reducer {
    fn from(args: RegisterIotDeviceArgs) -> Self {
        Self::RegisterIotDevice {
            organization_id: args.organization_id,
            company_id: args.company_id,
            hub_id: args.hub_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for RegisterIotDeviceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `register_iot_device`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait register_iot_device {
    /// Request that the remote module invoke the reducer `register_iot_device` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`register_iot_device:register_iot_device_then`] to run a callback after the reducer completes.
    fn register_iot_device(
        &self,
        organization_id: u64,
        company_id: u64,
        hub_id: u64,
        params: RegisterDeviceParams,
    ) -> __sdk::Result<()> {
        self.register_iot_device_then(organization_id, company_id, hub_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `register_iot_device` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn register_iot_device_then(
        &self,
        organization_id: u64,
        company_id: u64,
        hub_id: u64,
        params: RegisterDeviceParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl register_iot_device for super::RemoteReducers {
    fn register_iot_device_then(
        &self,
        organization_id: u64,
        company_id: u64,
        hub_id: u64,
        params: RegisterDeviceParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RegisterIotDeviceArgs {
                organization_id,
                company_id,
                hub_id,
                params,
            },
            callback,
        )
    }
}
