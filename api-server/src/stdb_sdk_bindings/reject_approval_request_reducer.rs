// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::reject_approval_request_params_type::RejectApprovalRequestParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RejectApprovalRequestArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub request_id: u64,
    pub params: RejectApprovalRequestParams,
}

impl From<RejectApprovalRequestArgs> for super::Reducer {
    fn from(args: RejectApprovalRequestArgs) -> Self {
        Self::RejectApprovalRequest {
            organization_id: args.organization_id,
            company_id: args.company_id,
            request_id: args.request_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for RejectApprovalRequestArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `reject_approval_request`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait reject_approval_request {
    /// Request that the remote module invoke the reducer `reject_approval_request` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`reject_approval_request:reject_approval_request_then`] to run a callback after the reducer completes.
    fn reject_approval_request(
        &self,
        organization_id: u64,
        company_id: u64,
        request_id: u64,
        params: RejectApprovalRequestParams,
    ) -> __sdk::Result<()> {
        self.reject_approval_request_then(
            organization_id,
            company_id,
            request_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `reject_approval_request` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn reject_approval_request_then(
        &self,
        organization_id: u64,
        company_id: u64,
        request_id: u64,
        params: RejectApprovalRequestParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl reject_approval_request for super::RemoteReducers {
    fn reject_approval_request_then(
        &self,
        organization_id: u64,
        company_id: u64,
        request_id: u64,
        params: RejectApprovalRequestParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RejectApprovalRequestArgs {
                organization_id,
                company_id,
                request_id,
                params,
            },
            callback,
        )
    }
}
