// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateKnowledgeArticlePresenceArgs {
    pub organization_id: u64,
    pub article_id: u64,
    pub user_name: String,
}

impl From<UpdateKnowledgeArticlePresenceArgs> for super::Reducer {
    fn from(args: UpdateKnowledgeArticlePresenceArgs) -> Self {
        Self::UpdateKnowledgeArticlePresence {
            organization_id: args.organization_id,
            article_id: args.article_id,
            user_name: args.user_name,
        }
    }
}

impl __sdk::InModule for UpdateKnowledgeArticlePresenceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_knowledge_article_presence`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_knowledge_article_presence {
    /// Request that the remote module invoke the reducer `update_knowledge_article_presence` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_knowledge_article_presence:update_knowledge_article_presence_then`] to run a callback after the reducer completes.
    fn update_knowledge_article_presence(
        &self,
        organization_id: u64,
        article_id: u64,
        user_name: String,
    ) -> __sdk::Result<()> {
        self.update_knowledge_article_presence_then(
            organization_id,
            article_id,
            user_name,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_knowledge_article_presence` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_knowledge_article_presence_then(
        &self,
        organization_id: u64,
        article_id: u64,
        user_name: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_knowledge_article_presence for super::RemoteReducers {
    fn update_knowledge_article_presence_then(
        &self,
        organization_id: u64,
        article_id: u64,
        user_name: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateKnowledgeArticlePresenceArgs {
                organization_id,
                article_id,
                user_name,
            },
            callback,
        )
    }
}
