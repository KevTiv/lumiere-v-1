// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CleanupStaleProposalPresenceArgs {
    pub organization_id: u64,
    pub max_age_micros: u64,
}

impl From<CleanupStaleProposalPresenceArgs> for super::Reducer {
    fn from(args: CleanupStaleProposalPresenceArgs) -> Self {
        Self::CleanupStaleProposalPresence {
            organization_id: args.organization_id,
            max_age_micros: args.max_age_micros,
        }
    }
}

impl __sdk::InModule for CleanupStaleProposalPresenceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `cleanup_stale_proposal_presence`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait cleanup_stale_proposal_presence {
    /// Request that the remote module invoke the reducer `cleanup_stale_proposal_presence` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`cleanup_stale_proposal_presence:cleanup_stale_proposal_presence_then`] to run a callback after the reducer completes.
    fn cleanup_stale_proposal_presence(
        &self,
        organization_id: u64,
        max_age_micros: u64,
    ) -> __sdk::Result<()> {
        self.cleanup_stale_proposal_presence_then(organization_id, max_age_micros, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `cleanup_stale_proposal_presence` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn cleanup_stale_proposal_presence_then(
        &self,
        organization_id: u64,
        max_age_micros: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl cleanup_stale_proposal_presence for super::RemoteReducers {
    fn cleanup_stale_proposal_presence_then(
        &self,
        organization_id: u64,
        max_age_micros: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CleanupStaleProposalPresenceArgs {
                organization_id,
                max_age_micros,
            },
            callback,
        )
    }
}
