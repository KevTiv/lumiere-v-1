// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UnenrollBenefitEnrollmentArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub enrollment_id: u64,
}

impl From<UnenrollBenefitEnrollmentArgs> for super::Reducer {
    fn from(args: UnenrollBenefitEnrollmentArgs) -> Self {
        Self::UnenrollBenefitEnrollment {
            organization_id: args.organization_id,
            company_id: args.company_id,
            enrollment_id: args.enrollment_id,
        }
    }
}

impl __sdk::InModule for UnenrollBenefitEnrollmentArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `unenroll_benefit_enrollment`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait unenroll_benefit_enrollment {
    /// Request that the remote module invoke the reducer `unenroll_benefit_enrollment` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`unenroll_benefit_enrollment:unenroll_benefit_enrollment_then`] to run a callback after the reducer completes.
    fn unenroll_benefit_enrollment(
        &self,
        organization_id: u64,
        company_id: u64,
        enrollment_id: u64,
    ) -> __sdk::Result<()> {
        self.unenroll_benefit_enrollment_then(organization_id, company_id, enrollment_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `unenroll_benefit_enrollment` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn unenroll_benefit_enrollment_then(
        &self,
        organization_id: u64,
        company_id: u64,
        enrollment_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl unenroll_benefit_enrollment for super::RemoteReducers {
    fn unenroll_benefit_enrollment_then(
        &self,
        organization_id: u64,
        company_id: u64,
        enrollment_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UnenrollBenefitEnrollmentArgs {
                organization_id,
                company_id,
                enrollment_id,
            },
            callback,
        )
    }
}
