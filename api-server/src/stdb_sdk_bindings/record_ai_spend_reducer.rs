// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecordAiSpendArgs {
    pub organization_id: u64,
    pub agent_id: u64,
    pub tokens_used: u32,
}

impl From<RecordAiSpendArgs> for super::Reducer {
    fn from(args: RecordAiSpendArgs) -> Self {
        Self::RecordAiSpend {
            organization_id: args.organization_id,
            agent_id: args.agent_id,
            tokens_used: args.tokens_used,
        }
    }
}

impl __sdk::InModule for RecordAiSpendArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `record_ai_spend`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait record_ai_spend {
    /// Request that the remote module invoke the reducer `record_ai_spend` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`record_ai_spend:record_ai_spend_then`] to run a callback after the reducer completes.
    fn record_ai_spend(
        &self,
        organization_id: u64,
        agent_id: u64,
        tokens_used: u32,
    ) -> __sdk::Result<()> {
        self.record_ai_spend_then(organization_id, agent_id, tokens_used, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `record_ai_spend` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn record_ai_spend_then(
        &self,
        organization_id: u64,
        agent_id: u64,
        tokens_used: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl record_ai_spend for super::RemoteReducers {
    fn record_ai_spend_then(
        &self,
        organization_id: u64,
        agent_id: u64,
        tokens_used: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecordAiSpendArgs {
                organization_id,
                agent_id,
                tokens_used,
            },
            callback,
        )
    }
}
