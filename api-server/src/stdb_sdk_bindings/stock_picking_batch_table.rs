// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::batch_state_type::BatchState;
use super::stock_picking_batch_type::StockPickingBatch;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `stock_picking_batch`.
///
/// Obtain a handle from the [`StockPickingBatchTableAccess::stock_picking_batch`] method on [`super::RemoteTables`],
/// like `ctx.db.stock_picking_batch()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.stock_picking_batch().on_insert(...)`.
pub struct StockPickingBatchTableHandle<'ctx> {
    imp: __sdk::TableHandle<StockPickingBatch>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `stock_picking_batch`.
///
/// Implemented for [`super::RemoteTables`].
pub trait StockPickingBatchTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`StockPickingBatchTableHandle`], which mediates access to the table `stock_picking_batch`.
    fn stock_picking_batch(&self) -> StockPickingBatchTableHandle<'_>;
}

impl StockPickingBatchTableAccess for super::RemoteTables {
    fn stock_picking_batch(&self) -> StockPickingBatchTableHandle<'_> {
        StockPickingBatchTableHandle {
            imp: self
                .imp
                .get_table::<StockPickingBatch>("stock_picking_batch"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct StockPickingBatchInsertCallbackId(__sdk::CallbackId);
pub struct StockPickingBatchDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for StockPickingBatchTableHandle<'ctx> {
    type Row = StockPickingBatch;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = StockPickingBatch> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = StockPickingBatchInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> StockPickingBatchInsertCallbackId {
        StockPickingBatchInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: StockPickingBatchInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = StockPickingBatchDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> StockPickingBatchDeleteCallbackId {
        StockPickingBatchDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: StockPickingBatchDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct StockPickingBatchUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for StockPickingBatchTableHandle<'ctx> {
    type UpdateCallbackId = StockPickingBatchUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> StockPickingBatchUpdateCallbackId {
        StockPickingBatchUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: StockPickingBatchUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `stock_picking_batch`,
/// which allows point queries on the field of the same name
/// via the [`StockPickingBatchIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.stock_picking_batch().id().find(...)`.
pub struct StockPickingBatchIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<StockPickingBatch, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> StockPickingBatchTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `stock_picking_batch`.
    pub fn id(&self) -> StockPickingBatchIdUnique<'ctx> {
        StockPickingBatchIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> StockPickingBatchIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<StockPickingBatch> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<StockPickingBatch>("stock_picking_batch");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<StockPickingBatch>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<StockPickingBatch>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `StockPickingBatch`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait stock_picking_batchQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `StockPickingBatch`.
    fn stock_picking_batch(&self) -> __sdk::__query_builder::Table<StockPickingBatch>;
}

impl stock_picking_batchQueryTableAccess for __sdk::QueryTableAccessor {
    fn stock_picking_batch(&self) -> __sdk::__query_builder::Table<StockPickingBatch> {
        __sdk::__query_builder::Table::new("stock_picking_batch")
    }
}
