// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::create_form_field_params_type::CreateFormFieldParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddFormFieldArgs {
    pub organization_id: u64,
    pub configuration_id: u64,
    pub params: CreateFormFieldParams,
}

impl From<AddFormFieldArgs> for super::Reducer {
    fn from(args: AddFormFieldArgs) -> Self {
        Self::AddFormField {
            organization_id: args.organization_id,
            configuration_id: args.configuration_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for AddFormFieldArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_form_field`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_form_field {
    /// Request that the remote module invoke the reducer `add_form_field` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_form_field:add_form_field_then`] to run a callback after the reducer completes.
    fn add_form_field(
        &self,
        organization_id: u64,
        configuration_id: u64,
        params: CreateFormFieldParams,
    ) -> __sdk::Result<()> {
        self.add_form_field_then(organization_id, configuration_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `add_form_field` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_form_field_then(
        &self,
        organization_id: u64,
        configuration_id: u64,
        params: CreateFormFieldParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl add_form_field for super::RemoteReducers {
    fn add_form_field_then(
        &self,
        organization_id: u64,
        configuration_id: u64,
        params: CreateFormFieldParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddFormFieldArgs {
                organization_id,
                configuration_id,
                params,
            },
            callback,
        )
    }
}
