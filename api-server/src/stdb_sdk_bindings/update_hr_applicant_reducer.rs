// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_hr_applicant_params_type::UpdateHrApplicantParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateHrApplicantArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub applicant_id: u64,
    pub params: UpdateHrApplicantParams,
}

impl From<UpdateHrApplicantArgs> for super::Reducer {
    fn from(args: UpdateHrApplicantArgs) -> Self {
        Self::UpdateHrApplicant {
            organization_id: args.organization_id,
            company_id: args.company_id,
            applicant_id: args.applicant_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateHrApplicantArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_hr_applicant`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_hr_applicant {
    /// Request that the remote module invoke the reducer `update_hr_applicant` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_hr_applicant:update_hr_applicant_then`] to run a callback after the reducer completes.
    fn update_hr_applicant(
        &self,
        organization_id: u64,
        company_id: u64,
        applicant_id: u64,
        params: UpdateHrApplicantParams,
    ) -> __sdk::Result<()> {
        self.update_hr_applicant_then(organization_id, company_id, applicant_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_hr_applicant` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_hr_applicant_then(
        &self,
        organization_id: u64,
        company_id: u64,
        applicant_id: u64,
        params: UpdateHrApplicantParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_hr_applicant for super::RemoteReducers {
    fn update_hr_applicant_then(
        &self,
        organization_id: u64,
        company_id: u64,
        applicant_id: u64,
        params: UpdateHrApplicantParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateHrApplicantArgs {
                organization_id,
                company_id,
                applicant_id,
                params,
            },
            callback,
        )
    }
}
