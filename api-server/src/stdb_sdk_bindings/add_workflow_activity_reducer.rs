// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::add_workflow_activity_params_type::AddWorkflowActivityParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddWorkflowActivityArgs {
    pub organization_id: u64,
    pub workflow_id: u64,
    pub params: AddWorkflowActivityParams,
}

impl From<AddWorkflowActivityArgs> for super::Reducer {
    fn from(args: AddWorkflowActivityArgs) -> Self {
        Self::AddWorkflowActivity {
            organization_id: args.organization_id,
            workflow_id: args.workflow_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for AddWorkflowActivityArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_workflow_activity`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_workflow_activity {
    /// Request that the remote module invoke the reducer `add_workflow_activity` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_workflow_activity:add_workflow_activity_then`] to run a callback after the reducer completes.
    fn add_workflow_activity(
        &self,
        organization_id: u64,
        workflow_id: u64,
        params: AddWorkflowActivityParams,
    ) -> __sdk::Result<()> {
        self.add_workflow_activity_then(organization_id, workflow_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `add_workflow_activity` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_workflow_activity_then(
        &self,
        organization_id: u64,
        workflow_id: u64,
        params: AddWorkflowActivityParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl add_workflow_activity for super::RemoteReducers {
    fn add_workflow_activity_then(
        &self,
        organization_id: u64,
        workflow_id: u64,
        params: AddWorkflowActivityParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddWorkflowActivityArgs {
                organization_id,
                workflow_id,
                params,
            },
            callback,
        )
    }
}
