// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UnsubscribeFromRecordArgs {
    pub organization_id: u64,
    pub res_model: String,
    pub res_id: u64,
}

impl From<UnsubscribeFromRecordArgs> for super::Reducer {
    fn from(args: UnsubscribeFromRecordArgs) -> Self {
        Self::UnsubscribeFromRecord {
            organization_id: args.organization_id,
            res_model: args.res_model,
            res_id: args.res_id,
        }
    }
}

impl __sdk::InModule for UnsubscribeFromRecordArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `unsubscribe_from_record`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait unsubscribe_from_record {
    /// Request that the remote module invoke the reducer `unsubscribe_from_record` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`unsubscribe_from_record:unsubscribe_from_record_then`] to run a callback after the reducer completes.
    fn unsubscribe_from_record(
        &self,
        organization_id: u64,
        res_model: String,
        res_id: u64,
    ) -> __sdk::Result<()> {
        self.unsubscribe_from_record_then(organization_id, res_model, res_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `unsubscribe_from_record` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn unsubscribe_from_record_then(
        &self,
        organization_id: u64,
        res_model: String,
        res_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl unsubscribe_from_record for super::RemoteReducers {
    fn unsubscribe_from_record_then(
        &self,
        organization_id: u64,
        res_model: String,
        res_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UnsubscribeFromRecordArgs {
                organization_id,
                res_model,
                res_id,
            },
            callback,
        )
    }
}
