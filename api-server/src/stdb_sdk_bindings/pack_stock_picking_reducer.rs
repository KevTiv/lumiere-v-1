// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::pack_stock_picking_params_type::PackStockPickingParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PackStockPickingArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub params: PackStockPickingParams,
}

impl From<PackStockPickingArgs> for super::Reducer {
    fn from(args: PackStockPickingArgs) -> Self {
        Self::PackStockPicking {
            organization_id: args.organization_id,
            company_id: args.company_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for PackStockPickingArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `pack_stock_picking`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait pack_stock_picking {
    /// Request that the remote module invoke the reducer `pack_stock_picking` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`pack_stock_picking:pack_stock_picking_then`] to run a callback after the reducer completes.
    fn pack_stock_picking(
        &self,
        organization_id: u64,
        company_id: u64,
        params: PackStockPickingParams,
    ) -> __sdk::Result<()> {
        self.pack_stock_picking_then(organization_id, company_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `pack_stock_picking` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn pack_stock_picking_then(
        &self,
        organization_id: u64,
        company_id: u64,
        params: PackStockPickingParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl pack_stock_picking for super::RemoteReducers {
    fn pack_stock_picking_then(
        &self,
        organization_id: u64,
        company_id: u64,
        params: PackStockPickingParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PackStockPickingArgs {
                organization_id,
                company_id,
                params,
            },
            callback,
        )
    }
}
