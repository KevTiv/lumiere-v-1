// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PostLandedCostsArgs {
    pub organization_id: u64,
    pub landed_cost_id: u64,
}

impl From<PostLandedCostsArgs> for super::Reducer {
    fn from(args: PostLandedCostsArgs) -> Self {
        Self::PostLandedCosts {
            organization_id: args.organization_id,
            landed_cost_id: args.landed_cost_id,
        }
    }
}

impl __sdk::InModule for PostLandedCostsArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `post_landed_costs`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait post_landed_costs {
    /// Request that the remote module invoke the reducer `post_landed_costs` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`post_landed_costs:post_landed_costs_then`] to run a callback after the reducer completes.
    fn post_landed_costs(&self, organization_id: u64, landed_cost_id: u64) -> __sdk::Result<()> {
        self.post_landed_costs_then(organization_id, landed_cost_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `post_landed_costs` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn post_landed_costs_then(
        &self,
        organization_id: u64,
        landed_cost_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl post_landed_costs for super::RemoteReducers {
    fn post_landed_costs_then(
        &self,
        organization_id: u64,
        landed_cost_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PostLandedCostsArgs {
                organization_id,
                landed_cost_id,
            },
            callback,
        )
    }
}
