// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AssignTaskUsersArgs {
    pub organization_id: u64,
    pub task_id: u64,
    pub user_ids: Vec<__sdk::Identity>,
}

impl From<AssignTaskUsersArgs> for super::Reducer {
    fn from(args: AssignTaskUsersArgs) -> Self {
        Self::AssignTaskUsers {
            organization_id: args.organization_id,
            task_id: args.task_id,
            user_ids: args.user_ids,
        }
    }
}

impl __sdk::InModule for AssignTaskUsersArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `assign_task_users`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait assign_task_users {
    /// Request that the remote module invoke the reducer `assign_task_users` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`assign_task_users:assign_task_users_then`] to run a callback after the reducer completes.
    fn assign_task_users(
        &self,
        organization_id: u64,
        task_id: u64,
        user_ids: Vec<__sdk::Identity>,
    ) -> __sdk::Result<()> {
        self.assign_task_users_then(organization_id, task_id, user_ids, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `assign_task_users` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn assign_task_users_then(
        &self,
        organization_id: u64,
        task_id: u64,
        user_ids: Vec<__sdk::Identity>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl assign_task_users for super::RemoteReducers {
    fn assign_task_users_then(
        &self,
        organization_id: u64,
        task_id: u64,
        user_ids: Vec<__sdk::Identity>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AssignTaskUsersArgs {
                organization_id,
                task_id,
                user_ids,
            },
            callback,
        )
    }
}
