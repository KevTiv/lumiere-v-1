// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::renew_subscription_params_type::RenewSubscriptionParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RenewSubscriptionArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub subscription_id: u64,
    pub params: RenewSubscriptionParams,
}

impl From<RenewSubscriptionArgs> for super::Reducer {
    fn from(args: RenewSubscriptionArgs) -> Self {
        Self::RenewSubscription {
            organization_id: args.organization_id,
            company_id: args.company_id,
            subscription_id: args.subscription_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for RenewSubscriptionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `renew_subscription`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait renew_subscription {
    /// Request that the remote module invoke the reducer `renew_subscription` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`renew_subscription:renew_subscription_then`] to run a callback after the reducer completes.
    fn renew_subscription(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: RenewSubscriptionParams,
    ) -> __sdk::Result<()> {
        self.renew_subscription_then(
            organization_id,
            company_id,
            subscription_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `renew_subscription` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn renew_subscription_then(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: RenewSubscriptionParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl renew_subscription for super::RemoteReducers {
    fn renew_subscription_then(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: RenewSubscriptionParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RenewSubscriptionArgs {
                organization_id,
                company_id,
                subscription_id,
                params,
            },
            callback,
        )
    }
}
