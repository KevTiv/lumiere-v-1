// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::record_payroll_export_result_params_type::RecordPayrollExportResultParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecordPayrollExportResultArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub intent_id: u64,
    pub params: RecordPayrollExportResultParams,
}

impl From<RecordPayrollExportResultArgs> for super::Reducer {
    fn from(args: RecordPayrollExportResultArgs) -> Self {
        Self::RecordPayrollExportResult {
            organization_id: args.organization_id,
            company_id: args.company_id,
            intent_id: args.intent_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for RecordPayrollExportResultArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `record_payroll_export_result`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait record_payroll_export_result {
    /// Request that the remote module invoke the reducer `record_payroll_export_result` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`record_payroll_export_result:record_payroll_export_result_then`] to run a callback after the reducer completes.
    fn record_payroll_export_result(
        &self,
        organization_id: u64,
        company_id: u64,
        intent_id: u64,
        params: RecordPayrollExportResultParams,
    ) -> __sdk::Result<()> {
        self.record_payroll_export_result_then(
            organization_id,
            company_id,
            intent_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `record_payroll_export_result` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn record_payroll_export_result_then(
        &self,
        organization_id: u64,
        company_id: u64,
        intent_id: u64,
        params: RecordPayrollExportResultParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl record_payroll_export_result for super::RemoteReducers {
    fn record_payroll_export_result_then(
        &self,
        organization_id: u64,
        company_id: u64,
        intent_id: u64,
        params: RecordPayrollExportResultParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecordPayrollExportResultArgs {
                organization_id,
                company_id,
                intent_id,
                params,
            },
            callback,
        )
    }
}
