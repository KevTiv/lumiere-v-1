// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::quality_team_type::QualityTeam;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `quality_team`.
///
/// Obtain a handle from the [`QualityTeamTableAccess::quality_team`] method on [`super::RemoteTables`],
/// like `ctx.db.quality_team()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.quality_team().on_insert(...)`.
pub struct QualityTeamTableHandle<'ctx> {
    imp: __sdk::TableHandle<QualityTeam>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `quality_team`.
///
/// Implemented for [`super::RemoteTables`].
pub trait QualityTeamTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`QualityTeamTableHandle`], which mediates access to the table `quality_team`.
    fn quality_team(&self) -> QualityTeamTableHandle<'_>;
}

impl QualityTeamTableAccess for super::RemoteTables {
    fn quality_team(&self) -> QualityTeamTableHandle<'_> {
        QualityTeamTableHandle {
            imp: self.imp.get_table::<QualityTeam>("quality_team"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct QualityTeamInsertCallbackId(__sdk::CallbackId);
pub struct QualityTeamDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for QualityTeamTableHandle<'ctx> {
    type Row = QualityTeam;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = QualityTeam> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = QualityTeamInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> QualityTeamInsertCallbackId {
        QualityTeamInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: QualityTeamInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = QualityTeamDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> QualityTeamDeleteCallbackId {
        QualityTeamDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: QualityTeamDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct QualityTeamUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for QualityTeamTableHandle<'ctx> {
    type UpdateCallbackId = QualityTeamUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> QualityTeamUpdateCallbackId {
        QualityTeamUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: QualityTeamUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `quality_team`,
/// which allows point queries on the field of the same name
/// via the [`QualityTeamIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.quality_team().id().find(...)`.
pub struct QualityTeamIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<QualityTeam, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> QualityTeamTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `quality_team`.
    pub fn id(&self) -> QualityTeamIdUnique<'ctx> {
        QualityTeamIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> QualityTeamIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<QualityTeam> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<QualityTeam>("quality_team");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<QualityTeam>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<QualityTeam>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `QualityTeam`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait quality_teamQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `QualityTeam`.
    fn quality_team(&self) -> __sdk::__query_builder::Table<QualityTeam>;
}

impl quality_teamQueryTableAccess for __sdk::QueryTableAccessor {
    fn quality_team(&self) -> __sdk::__query_builder::Table<QualityTeam> {
        __sdk::__query_builder::Table::new("quality_team")
    }
}
