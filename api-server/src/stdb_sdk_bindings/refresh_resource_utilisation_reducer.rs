// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::refresh_resource_utilisation_params_type::RefreshResourceUtilisationParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RefreshResourceUtilisationArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub params: RefreshResourceUtilisationParams,
}

impl From<RefreshResourceUtilisationArgs> for super::Reducer {
    fn from(args: RefreshResourceUtilisationArgs) -> Self {
        Self::RefreshResourceUtilisation {
            organization_id: args.organization_id,
            company_id: args.company_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for RefreshResourceUtilisationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `refresh_resource_utilisation`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait refresh_resource_utilisation {
    /// Request that the remote module invoke the reducer `refresh_resource_utilisation` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`refresh_resource_utilisation:refresh_resource_utilisation_then`] to run a callback after the reducer completes.
    fn refresh_resource_utilisation(
        &self,
        organization_id: u64,
        company_id: u64,
        params: RefreshResourceUtilisationParams,
    ) -> __sdk::Result<()> {
        self.refresh_resource_utilisation_then(organization_id, company_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `refresh_resource_utilisation` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn refresh_resource_utilisation_then(
        &self,
        organization_id: u64,
        company_id: u64,
        params: RefreshResourceUtilisationParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl refresh_resource_utilisation for super::RemoteReducers {
    fn refresh_resource_utilisation_then(
        &self,
        organization_id: u64,
        company_id: u64,
        params: RefreshResourceUtilisationParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RefreshResourceUtilisationArgs {
                organization_id,
                company_id,
                params,
            },
            callback,
        )
    }
}
