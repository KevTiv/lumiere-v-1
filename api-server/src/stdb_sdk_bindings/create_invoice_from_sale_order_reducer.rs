// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateInvoiceFromSaleOrderArgs {
    pub organization_id: u64,
    pub sale_order_id: u64,
    pub journal_id: u64,
    pub default_income_account_id: u64,
}

impl From<CreateInvoiceFromSaleOrderArgs> for super::Reducer {
    fn from(args: CreateInvoiceFromSaleOrderArgs) -> Self {
        Self::CreateInvoiceFromSaleOrder {
            organization_id: args.organization_id,
            sale_order_id: args.sale_order_id,
            journal_id: args.journal_id,
            default_income_account_id: args.default_income_account_id,
        }
    }
}

impl __sdk::InModule for CreateInvoiceFromSaleOrderArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_invoice_from_sale_order`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_invoice_from_sale_order {
    /// Request that the remote module invoke the reducer `create_invoice_from_sale_order` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_invoice_from_sale_order:create_invoice_from_sale_order_then`] to run a callback after the reducer completes.
    fn create_invoice_from_sale_order(
        &self,
        organization_id: u64,
        sale_order_id: u64,
        journal_id: u64,
        default_income_account_id: u64,
    ) -> __sdk::Result<()> {
        self.create_invoice_from_sale_order_then(
            organization_id,
            sale_order_id,
            journal_id,
            default_income_account_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_invoice_from_sale_order` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_invoice_from_sale_order_then(
        &self,
        organization_id: u64,
        sale_order_id: u64,
        journal_id: u64,
        default_income_account_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_invoice_from_sale_order for super::RemoteReducers {
    fn create_invoice_from_sale_order_then(
        &self,
        organization_id: u64,
        sale_order_id: u64,
        journal_id: u64,
        default_income_account_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateInvoiceFromSaleOrderArgs {
                organization_id,
                sale_order_id,
                journal_id,
                default_income_account_id,
            },
            callback,
        )
    }
}
