// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_workflow_draft_params_type::UpdateWorkflowDraftParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateWorkflowDraftArgs {
    pub organization_id: u64,
    pub workflow_version_id: u64,
    pub expected_revision: u64,
    pub params: UpdateWorkflowDraftParams,
}

impl From<UpdateWorkflowDraftArgs> for super::Reducer {
    fn from(args: UpdateWorkflowDraftArgs) -> Self {
        Self::UpdateWorkflowDraft {
            organization_id: args.organization_id,
            workflow_version_id: args.workflow_version_id,
            expected_revision: args.expected_revision,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateWorkflowDraftArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_workflow_draft`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_workflow_draft {
    /// Request that the remote module invoke the reducer `update_workflow_draft` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_workflow_draft:update_workflow_draft_then`] to run a callback after the reducer completes.
    fn update_workflow_draft(
        &self,
        organization_id: u64,
        workflow_version_id: u64,
        expected_revision: u64,
        params: UpdateWorkflowDraftParams,
    ) -> __sdk::Result<()> {
        self.update_workflow_draft_then(
            organization_id,
            workflow_version_id,
            expected_revision,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_workflow_draft` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_workflow_draft_then(
        &self,
        organization_id: u64,
        workflow_version_id: u64,
        expected_revision: u64,
        params: UpdateWorkflowDraftParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_workflow_draft for super::RemoteReducers {
    fn update_workflow_draft_then(
        &self,
        organization_id: u64,
        workflow_version_id: u64,
        expected_revision: u64,
        params: UpdateWorkflowDraftParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateWorkflowDraftArgs {
                organization_id,
                workflow_version_id,
                expected_revision,
                params,
            },
            callback,
        )
    }
}
