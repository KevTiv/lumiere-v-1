// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecomputeLeadScoreArgs {
    pub organization_id: u64,
    pub lead_id: u64,
}

impl From<RecomputeLeadScoreArgs> for super::Reducer {
    fn from(args: RecomputeLeadScoreArgs) -> Self {
        Self::RecomputeLeadScore {
            organization_id: args.organization_id,
            lead_id: args.lead_id,
        }
    }
}

impl __sdk::InModule for RecomputeLeadScoreArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `recompute_lead_score`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait recompute_lead_score {
    /// Request that the remote module invoke the reducer `recompute_lead_score` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`recompute_lead_score:recompute_lead_score_then`] to run a callback after the reducer completes.
    fn recompute_lead_score(&self, organization_id: u64, lead_id: u64) -> __sdk::Result<()> {
        self.recompute_lead_score_then(organization_id, lead_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `recompute_lead_score` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn recompute_lead_score_then(
        &self,
        organization_id: u64,
        lead_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl recompute_lead_score for super::RemoteReducers {
    fn recompute_lead_score_then(
        &self,
        organization_id: u64,
        lead_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecomputeLeadScoreArgs {
                organization_id,
                lead_id,
            },
            callback,
        )
    }
}
