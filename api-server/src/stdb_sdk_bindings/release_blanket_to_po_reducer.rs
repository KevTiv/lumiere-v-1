// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::release_blanket_to_po_params_type::ReleaseBlanketToPoParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ReleaseBlanketToPoArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub blanket_order_id: u64,
    pub params: ReleaseBlanketToPoParams,
}

impl From<ReleaseBlanketToPoArgs> for super::Reducer {
    fn from(args: ReleaseBlanketToPoArgs) -> Self {
        Self::ReleaseBlanketToPo {
            organization_id: args.organization_id,
            company_id: args.company_id,
            blanket_order_id: args.blanket_order_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for ReleaseBlanketToPoArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `release_blanket_to_po`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait release_blanket_to_po {
    /// Request that the remote module invoke the reducer `release_blanket_to_po` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`release_blanket_to_po:release_blanket_to_po_then`] to run a callback after the reducer completes.
    fn release_blanket_to_po(
        &self,
        organization_id: u64,
        company_id: u64,
        blanket_order_id: u64,
        params: ReleaseBlanketToPoParams,
    ) -> __sdk::Result<()> {
        self.release_blanket_to_po_then(
            organization_id,
            company_id,
            blanket_order_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `release_blanket_to_po` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn release_blanket_to_po_then(
        &self,
        organization_id: u64,
        company_id: u64,
        blanket_order_id: u64,
        params: ReleaseBlanketToPoParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl release_blanket_to_po for super::RemoteReducers {
    fn release_blanket_to_po_then(
        &self,
        organization_id: u64,
        company_id: u64,
        blanket_order_id: u64,
        params: ReleaseBlanketToPoParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ReleaseBlanketToPoArgs {
                organization_id,
                company_id,
                blanket_order_id,
                params,
            },
            callback,
        )
    }
}
