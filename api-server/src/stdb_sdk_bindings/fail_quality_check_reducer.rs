// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct FailQualityCheckArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub check_id: u64,
    pub qty_failed: f64,
    pub note: Option<String>,
    pub picture_fail: Option<String>,
    pub failure_location_id: Option<u64>,
}

impl From<FailQualityCheckArgs> for super::Reducer {
    fn from(args: FailQualityCheckArgs) -> Self {
        Self::FailQualityCheck {
            organization_id: args.organization_id,
            company_id: args.company_id,
            check_id: args.check_id,
            qty_failed: args.qty_failed,
            note: args.note,
            picture_fail: args.picture_fail,
            failure_location_id: args.failure_location_id,
        }
    }
}

impl __sdk::InModule for FailQualityCheckArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `fail_quality_check`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait fail_quality_check {
    /// Request that the remote module invoke the reducer `fail_quality_check` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`fail_quality_check:fail_quality_check_then`] to run a callback after the reducer completes.
    fn fail_quality_check(
        &self,
        organization_id: u64,
        company_id: u64,
        check_id: u64,
        qty_failed: f64,
        note: Option<String>,
        picture_fail: Option<String>,
        failure_location_id: Option<u64>,
    ) -> __sdk::Result<()> {
        self.fail_quality_check_then(
            organization_id,
            company_id,
            check_id,
            qty_failed,
            note,
            picture_fail,
            failure_location_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `fail_quality_check` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn fail_quality_check_then(
        &self,
        organization_id: u64,
        company_id: u64,
        check_id: u64,
        qty_failed: f64,
        note: Option<String>,
        picture_fail: Option<String>,
        failure_location_id: Option<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl fail_quality_check for super::RemoteReducers {
    fn fail_quality_check_then(
        &self,
        organization_id: u64,
        company_id: u64,
        check_id: u64,
        qty_failed: f64,
        note: Option<String>,
        picture_fail: Option<String>,
        failure_location_id: Option<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            FailQualityCheckArgs {
                organization_id,
                company_id,
                check_id,
                qty_failed,
                note,
                picture_fail,
                failure_location_id,
            },
            callback,
        )
    }
}
