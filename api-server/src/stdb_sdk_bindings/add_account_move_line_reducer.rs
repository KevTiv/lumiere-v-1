// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::add_account_move_line_params_type::AddAccountMoveLineParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddAccountMoveLineArgs {
    pub organization_id: u64,
    pub move_id: u64,
    pub params: AddAccountMoveLineParams,
}

impl From<AddAccountMoveLineArgs> for super::Reducer {
    fn from(args: AddAccountMoveLineArgs) -> Self {
        Self::AddAccountMoveLine {
            organization_id: args.organization_id,
            move_id: args.move_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for AddAccountMoveLineArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_account_move_line`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_account_move_line {
    /// Request that the remote module invoke the reducer `add_account_move_line` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_account_move_line:add_account_move_line_then`] to run a callback after the reducer completes.
    fn add_account_move_line(
        &self,
        organization_id: u64,
        move_id: u64,
        params: AddAccountMoveLineParams,
    ) -> __sdk::Result<()> {
        self.add_account_move_line_then(organization_id, move_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `add_account_move_line` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_account_move_line_then(
        &self,
        organization_id: u64,
        move_id: u64,
        params: AddAccountMoveLineParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl add_account_move_line for super::RemoteReducers {
    fn add_account_move_line_then(
        &self,
        organization_id: u64,
        move_id: u64,
        params: AddAccountMoveLineParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddAccountMoveLineArgs {
                organization_id,
                move_id,
                params,
            },
            callback,
        )
    }
}
