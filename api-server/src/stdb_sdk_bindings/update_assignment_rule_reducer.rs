// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_assignment_rule_params_type::UpdateAssignmentRuleParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateAssignmentRuleArgs {
    pub organization_id: u64,
    pub rule_id: u64,
    pub params: UpdateAssignmentRuleParams,
}

impl From<UpdateAssignmentRuleArgs> for super::Reducer {
    fn from(args: UpdateAssignmentRuleArgs) -> Self {
        Self::UpdateAssignmentRule {
            organization_id: args.organization_id,
            rule_id: args.rule_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateAssignmentRuleArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_assignment_rule`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_assignment_rule {
    /// Request that the remote module invoke the reducer `update_assignment_rule` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_assignment_rule:update_assignment_rule_then`] to run a callback after the reducer completes.
    fn update_assignment_rule(
        &self,
        organization_id: u64,
        rule_id: u64,
        params: UpdateAssignmentRuleParams,
    ) -> __sdk::Result<()> {
        self.update_assignment_rule_then(organization_id, rule_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_assignment_rule` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_assignment_rule_then(
        &self,
        organization_id: u64,
        rule_id: u64,
        params: UpdateAssignmentRuleParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_assignment_rule for super::RemoteReducers {
    fn update_assignment_rule_then(
        &self,
        organization_id: u64,
        rule_id: u64,
        params: UpdateAssignmentRuleParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateAssignmentRuleArgs {
                organization_id,
                rule_id,
                params,
            },
            callback,
        )
    }
}
