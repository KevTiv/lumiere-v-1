// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::consolidation_journal_type::ConsolidationJournal;
use super::consolidation_state_type::ConsolidationState;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `consolidation_journal`.
///
/// Obtain a handle from the [`ConsolidationJournalTableAccess::consolidation_journal`] method on [`super::RemoteTables`],
/// like `ctx.db.consolidation_journal()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.consolidation_journal().on_insert(...)`.
pub struct ConsolidationJournalTableHandle<'ctx> {
    imp: __sdk::TableHandle<ConsolidationJournal>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `consolidation_journal`.
///
/// Implemented for [`super::RemoteTables`].
pub trait ConsolidationJournalTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`ConsolidationJournalTableHandle`], which mediates access to the table `consolidation_journal`.
    fn consolidation_journal(&self) -> ConsolidationJournalTableHandle<'_>;
}

impl ConsolidationJournalTableAccess for super::RemoteTables {
    fn consolidation_journal(&self) -> ConsolidationJournalTableHandle<'_> {
        ConsolidationJournalTableHandle {
            imp: self
                .imp
                .get_table::<ConsolidationJournal>("consolidation_journal"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct ConsolidationJournalInsertCallbackId(__sdk::CallbackId);
pub struct ConsolidationJournalDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for ConsolidationJournalTableHandle<'ctx> {
    type Row = ConsolidationJournal;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = ConsolidationJournal> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = ConsolidationJournalInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ConsolidationJournalInsertCallbackId {
        ConsolidationJournalInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: ConsolidationJournalInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = ConsolidationJournalDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> ConsolidationJournalDeleteCallbackId {
        ConsolidationJournalDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: ConsolidationJournalDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct ConsolidationJournalUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for ConsolidationJournalTableHandle<'ctx> {
    type UpdateCallbackId = ConsolidationJournalUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> ConsolidationJournalUpdateCallbackId {
        ConsolidationJournalUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: ConsolidationJournalUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `consolidation_journal`,
/// which allows point queries on the field of the same name
/// via the [`ConsolidationJournalIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.consolidation_journal().id().find(...)`.
pub struct ConsolidationJournalIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<ConsolidationJournal, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> ConsolidationJournalTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `consolidation_journal`.
    pub fn id(&self) -> ConsolidationJournalIdUnique<'ctx> {
        ConsolidationJournalIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> ConsolidationJournalIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<ConsolidationJournal> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<ConsolidationJournal>("consolidation_journal");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<ConsolidationJournal>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<ConsolidationJournal>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `ConsolidationJournal`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait consolidation_journalQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `ConsolidationJournal`.
    fn consolidation_journal(&self) -> __sdk::__query_builder::Table<ConsolidationJournal>;
}

impl consolidation_journalQueryTableAccess for __sdk::QueryTableAccessor {
    fn consolidation_journal(&self) -> __sdk::__query_builder::Table<ConsolidationJournal> {
        __sdk::__query_builder::Table::new("consolidation_journal")
    }
}
