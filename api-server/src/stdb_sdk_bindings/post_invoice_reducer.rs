// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PostInvoiceArgs {
    pub organization_id: u64,
    pub move_id: u64,
    pub cogs_account_id: u64,
    pub inventory_account_id: u64,
}

impl From<PostInvoiceArgs> for super::Reducer {
    fn from(args: PostInvoiceArgs) -> Self {
        Self::PostInvoice {
            organization_id: args.organization_id,
            move_id: args.move_id,
            cogs_account_id: args.cogs_account_id,
            inventory_account_id: args.inventory_account_id,
        }
    }
}

impl __sdk::InModule for PostInvoiceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `post_invoice`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait post_invoice {
    /// Request that the remote module invoke the reducer `post_invoice` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`post_invoice:post_invoice_then`] to run a callback after the reducer completes.
    fn post_invoice(
        &self,
        organization_id: u64,
        move_id: u64,
        cogs_account_id: u64,
        inventory_account_id: u64,
    ) -> __sdk::Result<()> {
        self.post_invoice_then(
            organization_id,
            move_id,
            cogs_account_id,
            inventory_account_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `post_invoice` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn post_invoice_then(
        &self,
        organization_id: u64,
        move_id: u64,
        cogs_account_id: u64,
        inventory_account_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl post_invoice for super::RemoteReducers {
    fn post_invoice_then(
        &self,
        organization_id: u64,
        move_id: u64,
        cogs_account_id: u64,
        inventory_account_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PostInvoiceArgs {
                organization_id,
                move_id,
                cogs_account_id,
                inventory_account_id,
            },
            callback,
        )
    }
}
