// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::add_landed_cost_line_params_type::AddLandedCostLineParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddLandedCostLineArgs {
    pub organization_id: u64,
    pub landed_cost_id: u64,
    pub params: AddLandedCostLineParams,
}

impl From<AddLandedCostLineArgs> for super::Reducer {
    fn from(args: AddLandedCostLineArgs) -> Self {
        Self::AddLandedCostLine {
            organization_id: args.organization_id,
            landed_cost_id: args.landed_cost_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for AddLandedCostLineArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_landed_cost_line`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_landed_cost_line {
    /// Request that the remote module invoke the reducer `add_landed_cost_line` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_landed_cost_line:add_landed_cost_line_then`] to run a callback after the reducer completes.
    fn add_landed_cost_line(
        &self,
        organization_id: u64,
        landed_cost_id: u64,
        params: AddLandedCostLineParams,
    ) -> __sdk::Result<()> {
        self.add_landed_cost_line_then(organization_id, landed_cost_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `add_landed_cost_line` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_landed_cost_line_then(
        &self,
        organization_id: u64,
        landed_cost_id: u64,
        params: AddLandedCostLineParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl add_landed_cost_line for super::RemoteReducers {
    fn add_landed_cost_line_then(
        &self,
        organization_id: u64,
        landed_cost_id: u64,
        params: AddLandedCostLineParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddLandedCostLineArgs {
                organization_id,
                landed_cost_id,
                params,
            },
            callback,
        )
    }
}
