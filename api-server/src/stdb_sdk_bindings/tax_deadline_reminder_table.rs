// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::tax_deadline_reminder_type::TaxDeadlineReminder;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `tax_deadline_reminder`.
///
/// Obtain a handle from the [`TaxDeadlineReminderTableAccess::tax_deadline_reminder`] method on [`super::RemoteTables`],
/// like `ctx.db.tax_deadline_reminder()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.tax_deadline_reminder().on_insert(...)`.
pub struct TaxDeadlineReminderTableHandle<'ctx> {
    imp: __sdk::TableHandle<TaxDeadlineReminder>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `tax_deadline_reminder`.
///
/// Implemented for [`super::RemoteTables`].
pub trait TaxDeadlineReminderTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`TaxDeadlineReminderTableHandle`], which mediates access to the table `tax_deadline_reminder`.
    fn tax_deadline_reminder(&self) -> TaxDeadlineReminderTableHandle<'_>;
}

impl TaxDeadlineReminderTableAccess for super::RemoteTables {
    fn tax_deadline_reminder(&self) -> TaxDeadlineReminderTableHandle<'_> {
        TaxDeadlineReminderTableHandle {
            imp: self
                .imp
                .get_table::<TaxDeadlineReminder>("tax_deadline_reminder"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct TaxDeadlineReminderInsertCallbackId(__sdk::CallbackId);
pub struct TaxDeadlineReminderDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for TaxDeadlineReminderTableHandle<'ctx> {
    type Row = TaxDeadlineReminder;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TaxDeadlineReminder> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = TaxDeadlineReminderInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> TaxDeadlineReminderInsertCallbackId {
        TaxDeadlineReminderInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: TaxDeadlineReminderInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = TaxDeadlineReminderDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> TaxDeadlineReminderDeleteCallbackId {
        TaxDeadlineReminderDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: TaxDeadlineReminderDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct TaxDeadlineReminderUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for TaxDeadlineReminderTableHandle<'ctx> {
    type UpdateCallbackId = TaxDeadlineReminderUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> TaxDeadlineReminderUpdateCallbackId {
        TaxDeadlineReminderUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: TaxDeadlineReminderUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `tax_deadline_reminder`,
/// which allows point queries on the field of the same name
/// via the [`TaxDeadlineReminderIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.tax_deadline_reminder().id().find(...)`.
pub struct TaxDeadlineReminderIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<TaxDeadlineReminder, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> TaxDeadlineReminderTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `tax_deadline_reminder`.
    pub fn id(&self) -> TaxDeadlineReminderIdUnique<'ctx> {
        TaxDeadlineReminderIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> TaxDeadlineReminderIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TaxDeadlineReminder> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<TaxDeadlineReminder>("tax_deadline_reminder");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<TaxDeadlineReminder>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<TaxDeadlineReminder>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `TaxDeadlineReminder`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait tax_deadline_reminderQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `TaxDeadlineReminder`.
    fn tax_deadline_reminder(&self) -> __sdk::__query_builder::Table<TaxDeadlineReminder>;
}

impl tax_deadline_reminderQueryTableAccess for __sdk::QueryTableAccessor {
    fn tax_deadline_reminder(&self) -> __sdk::__query_builder::Table<TaxDeadlineReminder> {
        __sdk::__query_builder::Table::new("tax_deadline_reminder")
    }
}
