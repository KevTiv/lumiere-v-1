// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::apply_project_change_order_params_type::ApplyProjectChangeOrderParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ApplyProjectChangeOrderArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub change_order_id: u64,
    pub params: ApplyProjectChangeOrderParams,
}

impl From<ApplyProjectChangeOrderArgs> for super::Reducer {
    fn from(args: ApplyProjectChangeOrderArgs) -> Self {
        Self::ApplyProjectChangeOrder {
            organization_id: args.organization_id,
            company_id: args.company_id,
            change_order_id: args.change_order_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for ApplyProjectChangeOrderArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `apply_project_change_order`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait apply_project_change_order {
    /// Request that the remote module invoke the reducer `apply_project_change_order` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`apply_project_change_order:apply_project_change_order_then`] to run a callback after the reducer completes.
    fn apply_project_change_order(
        &self,
        organization_id: u64,
        company_id: u64,
        change_order_id: u64,
        params: ApplyProjectChangeOrderParams,
    ) -> __sdk::Result<()> {
        self.apply_project_change_order_then(
            organization_id,
            company_id,
            change_order_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `apply_project_change_order` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn apply_project_change_order_then(
        &self,
        organization_id: u64,
        company_id: u64,
        change_order_id: u64,
        params: ApplyProjectChangeOrderParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl apply_project_change_order for super::RemoteReducers {
    fn apply_project_change_order_then(
        &self,
        organization_id: u64,
        company_id: u64,
        change_order_id: u64,
        params: ApplyProjectChangeOrderParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ApplyProjectChangeOrderArgs {
                organization_id,
                company_id,
                change_order_id,
                params,
            },
            callback,
        )
    }
}
