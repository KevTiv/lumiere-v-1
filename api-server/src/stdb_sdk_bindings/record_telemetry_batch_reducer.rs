// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::record_telemetry_params_type::RecordTelemetryParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecordTelemetryBatchArgs {
    pub organization_id: u64,
    pub device_id: u64,
    pub readings: Vec<RecordTelemetryParams>,
}

impl From<RecordTelemetryBatchArgs> for super::Reducer {
    fn from(args: RecordTelemetryBatchArgs) -> Self {
        Self::RecordTelemetryBatch {
            organization_id: args.organization_id,
            device_id: args.device_id,
            readings: args.readings,
        }
    }
}

impl __sdk::InModule for RecordTelemetryBatchArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `record_telemetry_batch`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait record_telemetry_batch {
    /// Request that the remote module invoke the reducer `record_telemetry_batch` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`record_telemetry_batch:record_telemetry_batch_then`] to run a callback after the reducer completes.
    fn record_telemetry_batch(
        &self,
        organization_id: u64,
        device_id: u64,
        readings: Vec<RecordTelemetryParams>,
    ) -> __sdk::Result<()> {
        self.record_telemetry_batch_then(organization_id, device_id, readings, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `record_telemetry_batch` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn record_telemetry_batch_then(
        &self,
        organization_id: u64,
        device_id: u64,
        readings: Vec<RecordTelemetryParams>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl record_telemetry_batch for super::RemoteReducers {
    fn record_telemetry_batch_then(
        &self,
        organization_id: u64,
        device_id: u64,
        readings: Vec<RecordTelemetryParams>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecordTelemetryBatchArgs {
                organization_id,
                device_id,
                readings,
            },
            callback,
        )
    }
}
