// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::lead_score_type::LeadScore;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `lead_score`.
///
/// Obtain a handle from the [`LeadScoreTableAccess::lead_score`] method on [`super::RemoteTables`],
/// like `ctx.db.lead_score()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.lead_score().on_insert(...)`.
pub struct LeadScoreTableHandle<'ctx> {
    imp: __sdk::TableHandle<LeadScore>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `lead_score`.
///
/// Implemented for [`super::RemoteTables`].
pub trait LeadScoreTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`LeadScoreTableHandle`], which mediates access to the table `lead_score`.
    fn lead_score(&self) -> LeadScoreTableHandle<'_>;
}

impl LeadScoreTableAccess for super::RemoteTables {
    fn lead_score(&self) -> LeadScoreTableHandle<'_> {
        LeadScoreTableHandle {
            imp: self.imp.get_table::<LeadScore>("lead_score"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct LeadScoreInsertCallbackId(__sdk::CallbackId);
pub struct LeadScoreDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for LeadScoreTableHandle<'ctx> {
    type Row = LeadScore;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = LeadScore> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = LeadScoreInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> LeadScoreInsertCallbackId {
        LeadScoreInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: LeadScoreInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = LeadScoreDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> LeadScoreDeleteCallbackId {
        LeadScoreDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: LeadScoreDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct LeadScoreUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for LeadScoreTableHandle<'ctx> {
    type UpdateCallbackId = LeadScoreUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> LeadScoreUpdateCallbackId {
        LeadScoreUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: LeadScoreUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `lead_score`,
/// which allows point queries on the field of the same name
/// via the [`LeadScoreIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.lead_score().id().find(...)`.
pub struct LeadScoreIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<LeadScore, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> LeadScoreTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `lead_score`.
    pub fn id(&self) -> LeadScoreIdUnique<'ctx> {
        LeadScoreIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> LeadScoreIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<LeadScore> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<LeadScore>("lead_score");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<LeadScore>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<LeadScore>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `LeadScore`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait lead_scoreQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `LeadScore`.
    fn lead_score(&self) -> __sdk::__query_builder::Table<LeadScore>;
}

impl lead_scoreQueryTableAccess for __sdk::QueryTableAccessor {
    fn lead_score(&self) -> __sdk::__query_builder::Table<LeadScore> {
        __sdk::__query_builder::Table::new("lead_score")
    }
}
