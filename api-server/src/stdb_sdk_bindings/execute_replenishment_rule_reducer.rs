// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ExecuteReplenishmentRuleArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub rule_id: u64,
}

impl From<ExecuteReplenishmentRuleArgs> for super::Reducer {
    fn from(args: ExecuteReplenishmentRuleArgs) -> Self {
        Self::ExecuteReplenishmentRule {
            organization_id: args.organization_id,
            company_id: args.company_id,
            rule_id: args.rule_id,
        }
    }
}

impl __sdk::InModule for ExecuteReplenishmentRuleArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `execute_replenishment_rule`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait execute_replenishment_rule {
    /// Request that the remote module invoke the reducer `execute_replenishment_rule` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`execute_replenishment_rule:execute_replenishment_rule_then`] to run a callback after the reducer completes.
    fn execute_replenishment_rule(
        &self,
        organization_id: u64,
        company_id: u64,
        rule_id: u64,
    ) -> __sdk::Result<()> {
        self.execute_replenishment_rule_then(organization_id, company_id, rule_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `execute_replenishment_rule` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn execute_replenishment_rule_then(
        &self,
        organization_id: u64,
        company_id: u64,
        rule_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl execute_replenishment_rule for super::RemoteReducers {
    fn execute_replenishment_rule_then(
        &self,
        organization_id: u64,
        company_id: u64,
        rule_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ExecuteReplenishmentRuleArgs {
                organization_id,
                company_id,
                rule_id,
            },
            callback,
        )
    }
}
