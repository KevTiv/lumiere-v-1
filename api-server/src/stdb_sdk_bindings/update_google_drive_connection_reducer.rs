// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::sync_direction_type::SyncDirection;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateGoogleDriveConnectionArgs {
    pub connection_id: u64,
    pub organization_id: u64,
    pub name: Option<String>,
    pub root_folder_id: Option<String>,
    pub shared_drive_id: Option<String>,
    pub sync_enabled: Option<bool>,
    pub auto_sync_files: Option<bool>,
    pub allowed_file_types: Option<Vec<String>>,
    pub max_file_size_mb: Option<u32>,
    pub webhook_enabled: Option<bool>,
    pub webhook_url: Option<String>,
    pub sync_direction: Option<SyncDirection>,
    pub sync_frequency_minutes: Option<u32>,
}

impl From<UpdateGoogleDriveConnectionArgs> for super::Reducer {
    fn from(args: UpdateGoogleDriveConnectionArgs) -> Self {
        Self::UpdateGoogleDriveConnection {
            connection_id: args.connection_id,
            organization_id: args.organization_id,
            name: args.name,
            root_folder_id: args.root_folder_id,
            shared_drive_id: args.shared_drive_id,
            sync_enabled: args.sync_enabled,
            auto_sync_files: args.auto_sync_files,
            allowed_file_types: args.allowed_file_types,
            max_file_size_mb: args.max_file_size_mb,
            webhook_enabled: args.webhook_enabled,
            webhook_url: args.webhook_url,
            sync_direction: args.sync_direction,
            sync_frequency_minutes: args.sync_frequency_minutes,
        }
    }
}

impl __sdk::InModule for UpdateGoogleDriveConnectionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_google_drive_connection`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_google_drive_connection {
    /// Request that the remote module invoke the reducer `update_google_drive_connection` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_google_drive_connection:update_google_drive_connection_then`] to run a callback after the reducer completes.
    fn update_google_drive_connection(
        &self,
        connection_id: u64,
        organization_id: u64,
        name: Option<String>,
        root_folder_id: Option<String>,
        shared_drive_id: Option<String>,
        sync_enabled: Option<bool>,
        auto_sync_files: Option<bool>,
        allowed_file_types: Option<Vec<String>>,
        max_file_size_mb: Option<u32>,
        webhook_enabled: Option<bool>,
        webhook_url: Option<String>,
        sync_direction: Option<SyncDirection>,
        sync_frequency_minutes: Option<u32>,
    ) -> __sdk::Result<()> {
        self.update_google_drive_connection_then(
            connection_id,
            organization_id,
            name,
            root_folder_id,
            shared_drive_id,
            sync_enabled,
            auto_sync_files,
            allowed_file_types,
            max_file_size_mb,
            webhook_enabled,
            webhook_url,
            sync_direction,
            sync_frequency_minutes,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_google_drive_connection` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_google_drive_connection_then(
        &self,
        connection_id: u64,
        organization_id: u64,
        name: Option<String>,
        root_folder_id: Option<String>,
        shared_drive_id: Option<String>,
        sync_enabled: Option<bool>,
        auto_sync_files: Option<bool>,
        allowed_file_types: Option<Vec<String>>,
        max_file_size_mb: Option<u32>,
        webhook_enabled: Option<bool>,
        webhook_url: Option<String>,
        sync_direction: Option<SyncDirection>,
        sync_frequency_minutes: Option<u32>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_google_drive_connection for super::RemoteReducers {
    fn update_google_drive_connection_then(
        &self,
        connection_id: u64,
        organization_id: u64,
        name: Option<String>,
        root_folder_id: Option<String>,
        shared_drive_id: Option<String>,
        sync_enabled: Option<bool>,
        auto_sync_files: Option<bool>,
        allowed_file_types: Option<Vec<String>>,
        max_file_size_mb: Option<u32>,
        webhook_enabled: Option<bool>,
        webhook_url: Option<String>,
        sync_direction: Option<SyncDirection>,
        sync_frequency_minutes: Option<u32>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateGoogleDriveConnectionArgs {
                connection_id,
                organization_id,
                name,
                root_folder_id,
                shared_drive_id,
                sync_enabled,
                auto_sync_files,
                allowed_file_types,
                max_file_size_mb,
                webhook_enabled,
                webhook_url,
                sync_direction,
                sync_frequency_minutes,
            },
            callback,
        )
    }
}
