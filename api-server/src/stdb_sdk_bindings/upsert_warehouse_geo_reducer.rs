// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertWarehouseGeoArgs {
    pub organization_id: u64,
    pub warehouse_id: u64,
    pub latitude: f64,
    pub longitude: f64,
    pub address: Option<String>,
    pub city: Option<String>,
    pub country_code: Option<String>,
    pub manager_name: Option<String>,
}

impl From<UpsertWarehouseGeoArgs> for super::Reducer {
    fn from(args: UpsertWarehouseGeoArgs) -> Self {
        Self::UpsertWarehouseGeo {
            organization_id: args.organization_id,
            warehouse_id: args.warehouse_id,
            latitude: args.latitude,
            longitude: args.longitude,
            address: args.address,
            city: args.city,
            country_code: args.country_code,
            manager_name: args.manager_name,
        }
    }
}

impl __sdk::InModule for UpsertWarehouseGeoArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_warehouse_geo`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_warehouse_geo {
    /// Request that the remote module invoke the reducer `upsert_warehouse_geo` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_warehouse_geo:upsert_warehouse_geo_then`] to run a callback after the reducer completes.
    fn upsert_warehouse_geo(
        &self,
        organization_id: u64,
        warehouse_id: u64,
        latitude: f64,
        longitude: f64,
        address: Option<String>,
        city: Option<String>,
        country_code: Option<String>,
        manager_name: Option<String>,
    ) -> __sdk::Result<()> {
        self.upsert_warehouse_geo_then(
            organization_id,
            warehouse_id,
            latitude,
            longitude,
            address,
            city,
            country_code,
            manager_name,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_warehouse_geo` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_warehouse_geo_then(
        &self,
        organization_id: u64,
        warehouse_id: u64,
        latitude: f64,
        longitude: f64,
        address: Option<String>,
        city: Option<String>,
        country_code: Option<String>,
        manager_name: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_warehouse_geo for super::RemoteReducers {
    fn upsert_warehouse_geo_then(
        &self,
        organization_id: u64,
        warehouse_id: u64,
        latitude: f64,
        longitude: f64,
        address: Option<String>,
        city: Option<String>,
        country_code: Option<String>,
        manager_name: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertWarehouseGeoArgs {
                organization_id,
                warehouse_id,
                latitude,
                longitude,
                address,
                city,
                country_code,
                manager_name,
            },
            callback,
        )
    }
}
