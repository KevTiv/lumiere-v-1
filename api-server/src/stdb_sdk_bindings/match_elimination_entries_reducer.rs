// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct MatchEliminationEntriesArgs {
    pub organization_id: u64,
    pub entry_id: u64,
    pub matched_entry_id: u64,
}

impl From<MatchEliminationEntriesArgs> for super::Reducer {
    fn from(args: MatchEliminationEntriesArgs) -> Self {
        Self::MatchEliminationEntries {
            organization_id: args.organization_id,
            entry_id: args.entry_id,
            matched_entry_id: args.matched_entry_id,
        }
    }
}

impl __sdk::InModule for MatchEliminationEntriesArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `match_elimination_entries`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait match_elimination_entries {
    /// Request that the remote module invoke the reducer `match_elimination_entries` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`match_elimination_entries:match_elimination_entries_then`] to run a callback after the reducer completes.
    fn match_elimination_entries(
        &self,
        organization_id: u64,
        entry_id: u64,
        matched_entry_id: u64,
    ) -> __sdk::Result<()> {
        self.match_elimination_entries_then(organization_id, entry_id, matched_entry_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `match_elimination_entries` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn match_elimination_entries_then(
        &self,
        organization_id: u64,
        entry_id: u64,
        matched_entry_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl match_elimination_entries for super::RemoteReducers {
    fn match_elimination_entries_then(
        &self,
        organization_id: u64,
        entry_id: u64,
        matched_entry_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            MatchEliminationEntriesArgs {
                organization_id,
                entry_id,
                matched_entry_id,
            },
            callback,
        )
    }
}
