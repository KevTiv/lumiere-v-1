// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PromoteAiSkillVersionArgs {
    pub organization_id: u64,
    pub skill_version_id: u64,
    pub reason: Option<String>,
}

impl From<PromoteAiSkillVersionArgs> for super::Reducer {
    fn from(args: PromoteAiSkillVersionArgs) -> Self {
        Self::PromoteAiSkillVersion {
            organization_id: args.organization_id,
            skill_version_id: args.skill_version_id,
            reason: args.reason,
        }
    }
}

impl __sdk::InModule for PromoteAiSkillVersionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `promote_ai_skill_version`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait promote_ai_skill_version {
    /// Request that the remote module invoke the reducer `promote_ai_skill_version` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`promote_ai_skill_version:promote_ai_skill_version_then`] to run a callback after the reducer completes.
    fn promote_ai_skill_version(
        &self,
        organization_id: u64,
        skill_version_id: u64,
        reason: Option<String>,
    ) -> __sdk::Result<()> {
        self.promote_ai_skill_version_then(organization_id, skill_version_id, reason, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `promote_ai_skill_version` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn promote_ai_skill_version_then(
        &self,
        organization_id: u64,
        skill_version_id: u64,
        reason: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl promote_ai_skill_version for super::RemoteReducers {
    fn promote_ai_skill_version_then(
        &self,
        organization_id: u64,
        skill_version_id: u64,
        reason: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PromoteAiSkillVersionArgs {
                organization_id,
                skill_version_id,
                reason,
            },
            callback,
        )
    }
}
