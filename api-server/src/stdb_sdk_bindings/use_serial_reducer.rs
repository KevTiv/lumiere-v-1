// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UseSerialArgs {
    pub organization_id: u64,
    pub serial_id: u64,
}

impl From<UseSerialArgs> for super::Reducer {
    fn from(args: UseSerialArgs) -> Self {
        Self::UseSerial {
            organization_id: args.organization_id,
            serial_id: args.serial_id,
        }
    }
}

impl __sdk::InModule for UseSerialArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `use_serial`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait use_serial {
    /// Request that the remote module invoke the reducer `use_serial` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`use_serial:use_serial_then`] to run a callback after the reducer completes.
    fn use_serial(&self, organization_id: u64, serial_id: u64) -> __sdk::Result<()> {
        self.use_serial_then(organization_id, serial_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `use_serial` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn use_serial_then(
        &self,
        organization_id: u64,
        serial_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl use_serial for super::RemoteReducers {
    fn use_serial_then(
        &self,
        organization_id: u64,
        serial_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UseSerialArgs {
                organization_id,
                serial_id,
            },
            callback,
        )
    }
}
