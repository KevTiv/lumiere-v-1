// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddProposalSourceDocArgs {
    pub proposal_id: u64,
    pub name: String,
    pub content: String,
    pub doc_type: String,
    pub word_count: u32,
}

impl From<AddProposalSourceDocArgs> for super::Reducer {
    fn from(args: AddProposalSourceDocArgs) -> Self {
        Self::AddProposalSourceDoc {
            proposal_id: args.proposal_id,
            name: args.name,
            content: args.content,
            doc_type: args.doc_type,
            word_count: args.word_count,
        }
    }
}

impl __sdk::InModule for AddProposalSourceDocArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_proposal_source_doc`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_proposal_source_doc {
    /// Request that the remote module invoke the reducer `add_proposal_source_doc` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_proposal_source_doc:add_proposal_source_doc_then`] to run a callback after the reducer completes.
    fn add_proposal_source_doc(
        &self,
        proposal_id: u64,
        name: String,
        content: String,
        doc_type: String,
        word_count: u32,
    ) -> __sdk::Result<()> {
        self.add_proposal_source_doc_then(
            proposal_id,
            name,
            content,
            doc_type,
            word_count,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `add_proposal_source_doc` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_proposal_source_doc_then(
        &self,
        proposal_id: u64,
        name: String,
        content: String,
        doc_type: String,
        word_count: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl add_proposal_source_doc for super::RemoteReducers {
    fn add_proposal_source_doc_then(
        &self,
        proposal_id: u64,
        name: String,
        content: String,
        doc_type: String,
        word_count: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddProposalSourceDocArgs {
                proposal_id,
                name,
                content,
                doc_type,
                word_count,
            },
            callback,
        )
    }
}
