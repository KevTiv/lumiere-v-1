// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ActivatePosConfigArgs {
    pub organization_id: u64,
    pub config_id: u64,
}

impl From<ActivatePosConfigArgs> for super::Reducer {
    fn from(args: ActivatePosConfigArgs) -> Self {
        Self::ActivatePosConfig {
            organization_id: args.organization_id,
            config_id: args.config_id,
        }
    }
}

impl __sdk::InModule for ActivatePosConfigArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `activate_pos_config`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait activate_pos_config {
    /// Request that the remote module invoke the reducer `activate_pos_config` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`activate_pos_config:activate_pos_config_then`] to run a callback after the reducer completes.
    fn activate_pos_config(&self, organization_id: u64, config_id: u64) -> __sdk::Result<()> {
        self.activate_pos_config_then(organization_id, config_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `activate_pos_config` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn activate_pos_config_then(
        &self,
        organization_id: u64,
        config_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl activate_pos_config for super::RemoteReducers {
    fn activate_pos_config_then(
        &self,
        organization_id: u64,
        config_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ActivatePosConfigArgs {
                organization_id,
                config_id,
            },
            callback,
        )
    }
}
