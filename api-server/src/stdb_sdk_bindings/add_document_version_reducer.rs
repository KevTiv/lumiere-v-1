// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::add_document_version_params_type::AddDocumentVersionParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddDocumentVersionArgs {
    pub organization_id: u64,
    pub document_id: u64,
    pub params: AddDocumentVersionParams,
}

impl From<AddDocumentVersionArgs> for super::Reducer {
    fn from(args: AddDocumentVersionArgs) -> Self {
        Self::AddDocumentVersion {
            organization_id: args.organization_id,
            document_id: args.document_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for AddDocumentVersionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_document_version`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_document_version {
    /// Request that the remote module invoke the reducer `add_document_version` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_document_version:add_document_version_then`] to run a callback after the reducer completes.
    fn add_document_version(
        &self,
        organization_id: u64,
        document_id: u64,
        params: AddDocumentVersionParams,
    ) -> __sdk::Result<()> {
        self.add_document_version_then(organization_id, document_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `add_document_version` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_document_version_then(
        &self,
        organization_id: u64,
        document_id: u64,
        params: AddDocumentVersionParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl add_document_version for super::RemoteReducers {
    fn add_document_version_then(
        &self,
        organization_id: u64,
        document_id: u64,
        params: AddDocumentVersionParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddDocumentVersionArgs {
                organization_id,
                document_id,
                params,
            },
            callback,
        )
    }
}
