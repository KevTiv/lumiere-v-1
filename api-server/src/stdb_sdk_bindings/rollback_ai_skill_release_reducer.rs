// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RollbackAiSkillReleaseArgs {
    pub organization_id: u64,
    pub skill_id: u64,
    pub target_release_id: u64,
    pub reason: String,
}

impl From<RollbackAiSkillReleaseArgs> for super::Reducer {
    fn from(args: RollbackAiSkillReleaseArgs) -> Self {
        Self::RollbackAiSkillRelease {
            organization_id: args.organization_id,
            skill_id: args.skill_id,
            target_release_id: args.target_release_id,
            reason: args.reason,
        }
    }
}

impl __sdk::InModule for RollbackAiSkillReleaseArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `rollback_ai_skill_release`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait rollback_ai_skill_release {
    /// Request that the remote module invoke the reducer `rollback_ai_skill_release` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`rollback_ai_skill_release:rollback_ai_skill_release_then`] to run a callback after the reducer completes.
    fn rollback_ai_skill_release(
        &self,
        organization_id: u64,
        skill_id: u64,
        target_release_id: u64,
        reason: String,
    ) -> __sdk::Result<()> {
        self.rollback_ai_skill_release_then(
            organization_id,
            skill_id,
            target_release_id,
            reason,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `rollback_ai_skill_release` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn rollback_ai_skill_release_then(
        &self,
        organization_id: u64,
        skill_id: u64,
        target_release_id: u64,
        reason: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl rollback_ai_skill_release for super::RemoteReducers {
    fn rollback_ai_skill_release_then(
        &self,
        organization_id: u64,
        skill_id: u64,
        target_release_id: u64,
        reason: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RollbackAiSkillReleaseArgs {
                organization_id,
                skill_id,
                target_release_id,
                reason,
            },
            callback,
        )
    }
}
