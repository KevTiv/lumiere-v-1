// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct FinishWorkorderArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub workorder_id: u64,
}

impl From<FinishWorkorderArgs> for super::Reducer {
    fn from(args: FinishWorkorderArgs) -> Self {
        Self::FinishWorkorder {
            organization_id: args.organization_id,
            company_id: args.company_id,
            workorder_id: args.workorder_id,
        }
    }
}

impl __sdk::InModule for FinishWorkorderArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `finish_workorder`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait finish_workorder {
    /// Request that the remote module invoke the reducer `finish_workorder` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`finish_workorder:finish_workorder_then`] to run a callback after the reducer completes.
    fn finish_workorder(
        &self,
        organization_id: u64,
        company_id: u64,
        workorder_id: u64,
    ) -> __sdk::Result<()> {
        self.finish_workorder_then(organization_id, company_id, workorder_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `finish_workorder` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn finish_workorder_then(
        &self,
        organization_id: u64,
        company_id: u64,
        workorder_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl finish_workorder for super::RemoteReducers {
    fn finish_workorder_then(
        &self,
        organization_id: u64,
        company_id: u64,
        workorder_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            FinishWorkorderArgs {
                organization_id,
                company_id,
                workorder_id,
            },
            callback,
        )
    }
}
