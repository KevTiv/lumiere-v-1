// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::grant_subscription_entitlement_params_type::GrantSubscriptionEntitlementParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct GrantSubscriptionEntitlementArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub subscription_id: u64,
    pub params: GrantSubscriptionEntitlementParams,
}

impl From<GrantSubscriptionEntitlementArgs> for super::Reducer {
    fn from(args: GrantSubscriptionEntitlementArgs) -> Self {
        Self::GrantSubscriptionEntitlement {
            organization_id: args.organization_id,
            company_id: args.company_id,
            subscription_id: args.subscription_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for GrantSubscriptionEntitlementArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `grant_subscription_entitlement`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait grant_subscription_entitlement {
    /// Request that the remote module invoke the reducer `grant_subscription_entitlement` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`grant_subscription_entitlement:grant_subscription_entitlement_then`] to run a callback after the reducer completes.
    fn grant_subscription_entitlement(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: GrantSubscriptionEntitlementParams,
    ) -> __sdk::Result<()> {
        self.grant_subscription_entitlement_then(
            organization_id,
            company_id,
            subscription_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `grant_subscription_entitlement` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn grant_subscription_entitlement_then(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: GrantSubscriptionEntitlementParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl grant_subscription_entitlement for super::RemoteReducers {
    fn grant_subscription_entitlement_then(
        &self,
        organization_id: u64,
        company_id: u64,
        subscription_id: u64,
        params: GrantSubscriptionEntitlementParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            GrantSubscriptionEntitlementArgs {
                organization_id,
                company_id,
                subscription_id,
                params,
            },
            callback,
        )
    }
}
