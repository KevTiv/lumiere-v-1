// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_working_calendar_params_type::UpdateWorkingCalendarParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateWorkingCalendarArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub calendar_id: u64,
    pub params: UpdateWorkingCalendarParams,
}

impl From<UpdateWorkingCalendarArgs> for super::Reducer {
    fn from(args: UpdateWorkingCalendarArgs) -> Self {
        Self::UpdateWorkingCalendar {
            organization_id: args.organization_id,
            company_id: args.company_id,
            calendar_id: args.calendar_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateWorkingCalendarArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_working_calendar`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_working_calendar {
    /// Request that the remote module invoke the reducer `update_working_calendar` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_working_calendar:update_working_calendar_then`] to run a callback after the reducer completes.
    fn update_working_calendar(
        &self,
        organization_id: u64,
        company_id: u64,
        calendar_id: u64,
        params: UpdateWorkingCalendarParams,
    ) -> __sdk::Result<()> {
        self.update_working_calendar_then(
            organization_id,
            company_id,
            calendar_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_working_calendar` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_working_calendar_then(
        &self,
        organization_id: u64,
        company_id: u64,
        calendar_id: u64,
        params: UpdateWorkingCalendarParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_working_calendar for super::RemoteReducers {
    fn update_working_calendar_then(
        &self,
        organization_id: u64,
        company_id: u64,
        calendar_id: u64,
        params: UpdateWorkingCalendarParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateWorkingCalendarArgs {
                organization_id,
                company_id,
                calendar_id,
                params,
            },
            callback,
        )
    }
}
