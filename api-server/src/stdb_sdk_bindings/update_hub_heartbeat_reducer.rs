// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateHubHeartbeatArgs {
    pub organization_id: u64,
    pub hub_id: u64,
    pub ip_address: Option<String>,
    pub firmware_version: Option<String>,
    pub connectivity_quality: Option<String>,
}

impl From<UpdateHubHeartbeatArgs> for super::Reducer {
    fn from(args: UpdateHubHeartbeatArgs) -> Self {
        Self::UpdateHubHeartbeat {
            organization_id: args.organization_id,
            hub_id: args.hub_id,
            ip_address: args.ip_address,
            firmware_version: args.firmware_version,
            connectivity_quality: args.connectivity_quality,
        }
    }
}

impl __sdk::InModule for UpdateHubHeartbeatArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_hub_heartbeat`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_hub_heartbeat {
    /// Request that the remote module invoke the reducer `update_hub_heartbeat` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_hub_heartbeat:update_hub_heartbeat_then`] to run a callback after the reducer completes.
    fn update_hub_heartbeat(
        &self,
        organization_id: u64,
        hub_id: u64,
        ip_address: Option<String>,
        firmware_version: Option<String>,
        connectivity_quality: Option<String>,
    ) -> __sdk::Result<()> {
        self.update_hub_heartbeat_then(
            organization_id,
            hub_id,
            ip_address,
            firmware_version,
            connectivity_quality,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_hub_heartbeat` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_hub_heartbeat_then(
        &self,
        organization_id: u64,
        hub_id: u64,
        ip_address: Option<String>,
        firmware_version: Option<String>,
        connectivity_quality: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_hub_heartbeat for super::RemoteReducers {
    fn update_hub_heartbeat_then(
        &self,
        organization_id: u64,
        hub_id: u64,
        ip_address: Option<String>,
        firmware_version: Option<String>,
        connectivity_quality: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateHubHeartbeatArgs {
                organization_id,
                hub_id,
                ip_address,
                firmware_version,
                connectivity_quality,
            },
            callback,
        )
    }
}
