// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddProposalCommentArgs {
    pub proposal_id: u64,
    pub section_id: u64,
    pub content: String,
    pub parent_id: Option<u64>,
    pub author_name: String,
}

impl From<AddProposalCommentArgs> for super::Reducer {
    fn from(args: AddProposalCommentArgs) -> Self {
        Self::AddProposalComment {
            proposal_id: args.proposal_id,
            section_id: args.section_id,
            content: args.content,
            parent_id: args.parent_id,
            author_name: args.author_name,
        }
    }
}

impl __sdk::InModule for AddProposalCommentArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_proposal_comment`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_proposal_comment {
    /// Request that the remote module invoke the reducer `add_proposal_comment` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_proposal_comment:add_proposal_comment_then`] to run a callback after the reducer completes.
    fn add_proposal_comment(
        &self,
        proposal_id: u64,
        section_id: u64,
        content: String,
        parent_id: Option<u64>,
        author_name: String,
    ) -> __sdk::Result<()> {
        self.add_proposal_comment_then(
            proposal_id,
            section_id,
            content,
            parent_id,
            author_name,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `add_proposal_comment` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_proposal_comment_then(
        &self,
        proposal_id: u64,
        section_id: u64,
        content: String,
        parent_id: Option<u64>,
        author_name: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl add_proposal_comment for super::RemoteReducers {
    fn add_proposal_comment_then(
        &self,
        proposal_id: u64,
        section_id: u64,
        content: String,
        parent_id: Option<u64>,
        author_name: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddProposalCommentArgs {
                proposal_id,
                section_id,
                content,
                parent_id,
                author_name,
            },
            callback,
        )
    }
}
