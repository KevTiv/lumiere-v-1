// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::upsert_workflow_edge_params_type::UpsertWorkflowEdgeParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertWorkflowEdgeArgs {
    pub organization_id: u64,
    pub workflow_version_id: u64,
    pub expected_revision: u64,
    pub params: UpsertWorkflowEdgeParams,
}

impl From<UpsertWorkflowEdgeArgs> for super::Reducer {
    fn from(args: UpsertWorkflowEdgeArgs) -> Self {
        Self::UpsertWorkflowEdge {
            organization_id: args.organization_id,
            workflow_version_id: args.workflow_version_id,
            expected_revision: args.expected_revision,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpsertWorkflowEdgeArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_workflow_edge`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_workflow_edge {
    /// Request that the remote module invoke the reducer `upsert_workflow_edge` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_workflow_edge:upsert_workflow_edge_then`] to run a callback after the reducer completes.
    fn upsert_workflow_edge(
        &self,
        organization_id: u64,
        workflow_version_id: u64,
        expected_revision: u64,
        params: UpsertWorkflowEdgeParams,
    ) -> __sdk::Result<()> {
        self.upsert_workflow_edge_then(
            organization_id,
            workflow_version_id,
            expected_revision,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_workflow_edge` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_workflow_edge_then(
        &self,
        organization_id: u64,
        workflow_version_id: u64,
        expected_revision: u64,
        params: UpsertWorkflowEdgeParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_workflow_edge for super::RemoteReducers {
    fn upsert_workflow_edge_then(
        &self,
        organization_id: u64,
        workflow_version_id: u64,
        expected_revision: u64,
        params: UpsertWorkflowEdgeParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertWorkflowEdgeArgs {
                organization_id,
                workflow_version_id,
                expected_revision,
                params,
            },
            callback,
        )
    }
}
