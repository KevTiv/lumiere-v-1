// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::discount_policy_type::DiscountPolicy;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdatePricelistArgs {
    pub organization_id: u64,
    pub pricelist_id: u64,
    pub name: Option<String>,
    pub currency_id: Option<u64>,
    pub discount_policy: Option<DiscountPolicy>,
    pub is_active: Option<bool>,
}

impl From<UpdatePricelistArgs> for super::Reducer {
    fn from(args: UpdatePricelistArgs) -> Self {
        Self::UpdatePricelist {
            organization_id: args.organization_id,
            pricelist_id: args.pricelist_id,
            name: args.name,
            currency_id: args.currency_id,
            discount_policy: args.discount_policy,
            is_active: args.is_active,
        }
    }
}

impl __sdk::InModule for UpdatePricelistArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_pricelist`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_pricelist {
    /// Request that the remote module invoke the reducer `update_pricelist` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_pricelist:update_pricelist_then`] to run a callback after the reducer completes.
    fn update_pricelist(
        &self,
        organization_id: u64,
        pricelist_id: u64,
        name: Option<String>,
        currency_id: Option<u64>,
        discount_policy: Option<DiscountPolicy>,
        is_active: Option<bool>,
    ) -> __sdk::Result<()> {
        self.update_pricelist_then(
            organization_id,
            pricelist_id,
            name,
            currency_id,
            discount_policy,
            is_active,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_pricelist` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_pricelist_then(
        &self,
        organization_id: u64,
        pricelist_id: u64,
        name: Option<String>,
        currency_id: Option<u64>,
        discount_policy: Option<DiscountPolicy>,
        is_active: Option<bool>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_pricelist for super::RemoteReducers {
    fn update_pricelist_then(
        &self,
        organization_id: u64,
        pricelist_id: u64,
        name: Option<String>,
        currency_id: Option<u64>,
        discount_policy: Option<DiscountPolicy>,
        is_active: Option<bool>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdatePricelistArgs {
                organization_id,
                pricelist_id,
                name,
                currency_id,
                discount_policy,
                is_active,
            },
            callback,
        )
    }
}
