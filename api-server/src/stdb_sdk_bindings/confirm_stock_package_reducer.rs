// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ConfirmStockPackageArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub package_id: u64,
}

impl From<ConfirmStockPackageArgs> for super::Reducer {
    fn from(args: ConfirmStockPackageArgs) -> Self {
        Self::ConfirmStockPackage {
            organization_id: args.organization_id,
            company_id: args.company_id,
            package_id: args.package_id,
        }
    }
}

impl __sdk::InModule for ConfirmStockPackageArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `confirm_stock_package`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait confirm_stock_package {
    /// Request that the remote module invoke the reducer `confirm_stock_package` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`confirm_stock_package:confirm_stock_package_then`] to run a callback after the reducer completes.
    fn confirm_stock_package(
        &self,
        organization_id: u64,
        company_id: u64,
        package_id: u64,
    ) -> __sdk::Result<()> {
        self.confirm_stock_package_then(organization_id, company_id, package_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `confirm_stock_package` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn confirm_stock_package_then(
        &self,
        organization_id: u64,
        company_id: u64,
        package_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl confirm_stock_package for super::RemoteReducers {
    fn confirm_stock_package_then(
        &self,
        organization_id: u64,
        company_id: u64,
        package_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ConfirmStockPackageArgs {
                organization_id,
                company_id,
                package_id,
            },
            callback,
        )
    }
}
