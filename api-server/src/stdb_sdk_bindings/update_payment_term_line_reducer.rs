// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::payment_term_value_type::PaymentTermValue;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdatePaymentTermLineArgs {
    pub organization_id: u64,
    pub line_id: u64,
    pub value: Option<PaymentTermValue>,
    pub value_amount: Option<f64>,
    pub days: Option<u32>,
    pub months: Option<u32>,
    pub days_after_end_of_month: Option<bool>,
    pub sequence: Option<u32>,
}

impl From<UpdatePaymentTermLineArgs> for super::Reducer {
    fn from(args: UpdatePaymentTermLineArgs) -> Self {
        Self::UpdatePaymentTermLine {
            organization_id: args.organization_id,
            line_id: args.line_id,
            value: args.value,
            value_amount: args.value_amount,
            days: args.days,
            months: args.months,
            days_after_end_of_month: args.days_after_end_of_month,
            sequence: args.sequence,
        }
    }
}

impl __sdk::InModule for UpdatePaymentTermLineArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_payment_term_line`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_payment_term_line {
    /// Request that the remote module invoke the reducer `update_payment_term_line` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_payment_term_line:update_payment_term_line_then`] to run a callback after the reducer completes.
    fn update_payment_term_line(
        &self,
        organization_id: u64,
        line_id: u64,
        value: Option<PaymentTermValue>,
        value_amount: Option<f64>,
        days: Option<u32>,
        months: Option<u32>,
        days_after_end_of_month: Option<bool>,
        sequence: Option<u32>,
    ) -> __sdk::Result<()> {
        self.update_payment_term_line_then(
            organization_id,
            line_id,
            value,
            value_amount,
            days,
            months,
            days_after_end_of_month,
            sequence,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_payment_term_line` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_payment_term_line_then(
        &self,
        organization_id: u64,
        line_id: u64,
        value: Option<PaymentTermValue>,
        value_amount: Option<f64>,
        days: Option<u32>,
        months: Option<u32>,
        days_after_end_of_month: Option<bool>,
        sequence: Option<u32>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_payment_term_line for super::RemoteReducers {
    fn update_payment_term_line_then(
        &self,
        organization_id: u64,
        line_id: u64,
        value: Option<PaymentTermValue>,
        value_amount: Option<f64>,
        days: Option<u32>,
        months: Option<u32>,
        days_after_end_of_month: Option<bool>,
        sequence: Option<u32>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdatePaymentTermLineArgs {
                organization_id,
                line_id,
                value,
                value_amount,
                days,
                months,
                days_after_end_of_month,
                sequence,
            },
            callback,
        )
    }
}
