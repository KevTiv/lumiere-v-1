// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::tax_deadline_status_type::TaxDeadlineStatus;
use super::tax_deadline_type::TaxDeadline;
use super::tax_deadline_type_type::TaxDeadlineType;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `tax_deadline`.
///
/// Obtain a handle from the [`TaxDeadlineTableAccess::tax_deadline`] method on [`super::RemoteTables`],
/// like `ctx.db.tax_deadline()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.tax_deadline().on_insert(...)`.
pub struct TaxDeadlineTableHandle<'ctx> {
    imp: __sdk::TableHandle<TaxDeadline>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `tax_deadline`.
///
/// Implemented for [`super::RemoteTables`].
pub trait TaxDeadlineTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`TaxDeadlineTableHandle`], which mediates access to the table `tax_deadline`.
    fn tax_deadline(&self) -> TaxDeadlineTableHandle<'_>;
}

impl TaxDeadlineTableAccess for super::RemoteTables {
    fn tax_deadline(&self) -> TaxDeadlineTableHandle<'_> {
        TaxDeadlineTableHandle {
            imp: self.imp.get_table::<TaxDeadline>("tax_deadline"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct TaxDeadlineInsertCallbackId(__sdk::CallbackId);
pub struct TaxDeadlineDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for TaxDeadlineTableHandle<'ctx> {
    type Row = TaxDeadline;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TaxDeadline> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = TaxDeadlineInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> TaxDeadlineInsertCallbackId {
        TaxDeadlineInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: TaxDeadlineInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = TaxDeadlineDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> TaxDeadlineDeleteCallbackId {
        TaxDeadlineDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: TaxDeadlineDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct TaxDeadlineUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for TaxDeadlineTableHandle<'ctx> {
    type UpdateCallbackId = TaxDeadlineUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> TaxDeadlineUpdateCallbackId {
        TaxDeadlineUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: TaxDeadlineUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `tax_deadline`,
/// which allows point queries on the field of the same name
/// via the [`TaxDeadlineIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.tax_deadline().id().find(...)`.
pub struct TaxDeadlineIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<TaxDeadline, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> TaxDeadlineTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `tax_deadline`.
    pub fn id(&self) -> TaxDeadlineIdUnique<'ctx> {
        TaxDeadlineIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> TaxDeadlineIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TaxDeadline> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<TaxDeadline>("tax_deadline");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<TaxDeadline>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<TaxDeadline>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `TaxDeadline`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait tax_deadlineQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `TaxDeadline`.
    fn tax_deadline(&self) -> __sdk::__query_builder::Table<TaxDeadline>;
}

impl tax_deadlineQueryTableAccess for __sdk::QueryTableAccessor {
    fn tax_deadline(&self) -> __sdk::__query_builder::Table<TaxDeadline> {
        __sdk::__query_builder::Table::new("tax_deadline")
    }
}
