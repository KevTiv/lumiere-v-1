// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::user_credential_type::UserCredential;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `user_credential`.
///
/// Obtain a handle from the [`UserCredentialTableAccess::user_credential`] method on [`super::RemoteTables`],
/// like `ctx.db.user_credential()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.user_credential().on_insert(...)`.
pub struct UserCredentialTableHandle<'ctx> {
    imp: __sdk::TableHandle<UserCredential>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `user_credential`.
///
/// Implemented for [`super::RemoteTables`].
pub trait UserCredentialTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`UserCredentialTableHandle`], which mediates access to the table `user_credential`.
    fn user_credential(&self) -> UserCredentialTableHandle<'_>;
}

impl UserCredentialTableAccess for super::RemoteTables {
    fn user_credential(&self) -> UserCredentialTableHandle<'_> {
        UserCredentialTableHandle {
            imp: self.imp.get_table::<UserCredential>("user_credential"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct UserCredentialInsertCallbackId(__sdk::CallbackId);
pub struct UserCredentialDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for UserCredentialTableHandle<'ctx> {
    type Row = UserCredential;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = UserCredential> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = UserCredentialInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> UserCredentialInsertCallbackId {
        UserCredentialInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: UserCredentialInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = UserCredentialDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> UserCredentialDeleteCallbackId {
        UserCredentialDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: UserCredentialDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct UserCredentialUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for UserCredentialTableHandle<'ctx> {
    type UpdateCallbackId = UserCredentialUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> UserCredentialUpdateCallbackId {
        UserCredentialUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: UserCredentialUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `user_credential`,
/// which allows point queries on the field of the same name
/// via the [`UserCredentialIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.user_credential().id().find(...)`.
pub struct UserCredentialIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<UserCredential, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> UserCredentialTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `user_credential`.
    pub fn id(&self) -> UserCredentialIdUnique<'ctx> {
        UserCredentialIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> UserCredentialIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<UserCredential> {
        self.imp.find(col_val)
    }
}

/// Access to the `email` unique index on the table `user_credential`,
/// which allows point queries on the field of the same name
/// via the [`UserCredentialEmailUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.user_credential().email().find(...)`.
pub struct UserCredentialEmailUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<UserCredential, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> UserCredentialTableHandle<'ctx> {
    /// Get a handle on the `email` unique index on the table `user_credential`.
    pub fn email(&self) -> UserCredentialEmailUnique<'ctx> {
        UserCredentialEmailUnique {
            imp: self.imp.get_unique_constraint::<String>("email"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> UserCredentialEmailUnique<'ctx> {
    /// Find the subscribed row whose `email` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<UserCredential> {
        self.imp.find(col_val)
    }
}

/// Access to the `identity` unique index on the table `user_credential`,
/// which allows point queries on the field of the same name
/// via the [`UserCredentialIdentityUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.user_credential().identity().find(...)`.
pub struct UserCredentialIdentityUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<UserCredential, __sdk::Identity>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> UserCredentialTableHandle<'ctx> {
    /// Get a handle on the `identity` unique index on the table `user_credential`.
    pub fn identity(&self) -> UserCredentialIdentityUnique<'ctx> {
        UserCredentialIdentityUnique {
            imp: self
                .imp
                .get_unique_constraint::<__sdk::Identity>("identity"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> UserCredentialIdentityUnique<'ctx> {
    /// Find the subscribed row whose `identity` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &__sdk::Identity) -> Option<UserCredential> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<UserCredential>("user_credential");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
    _table.add_unique_constraint::<String>("email", |row| &row.email);
    _table.add_unique_constraint::<__sdk::Identity>("identity", |row| &row.identity);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<UserCredential>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<UserCredential>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `UserCredential`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait user_credentialQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `UserCredential`.
    fn user_credential(&self) -> __sdk::__query_builder::Table<UserCredential>;
}

impl user_credentialQueryTableAccess for __sdk::QueryTableAccessor {
    fn user_credential(&self) -> __sdk::__query_builder::Table<UserCredential> {
        __sdk::__query_builder::Table::new("user_credential")
    }
}
