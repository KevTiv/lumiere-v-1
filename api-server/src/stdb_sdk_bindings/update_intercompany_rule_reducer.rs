// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_intercompany_rule_params_type::UpdateIntercompanyRuleParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateIntercompanyRuleArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub rule_id: u64,
    pub params: UpdateIntercompanyRuleParams,
}

impl From<UpdateIntercompanyRuleArgs> for super::Reducer {
    fn from(args: UpdateIntercompanyRuleArgs) -> Self {
        Self::UpdateIntercompanyRule {
            organization_id: args.organization_id,
            company_id: args.company_id,
            rule_id: args.rule_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateIntercompanyRuleArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_intercompany_rule`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_intercompany_rule {
    /// Request that the remote module invoke the reducer `update_intercompany_rule` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_intercompany_rule:update_intercompany_rule_then`] to run a callback after the reducer completes.
    fn update_intercompany_rule(
        &self,
        organization_id: u64,
        company_id: u64,
        rule_id: u64,
        params: UpdateIntercompanyRuleParams,
    ) -> __sdk::Result<()> {
        self.update_intercompany_rule_then(organization_id, company_id, rule_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_intercompany_rule` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_intercompany_rule_then(
        &self,
        organization_id: u64,
        company_id: u64,
        rule_id: u64,
        params: UpdateIntercompanyRuleParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_intercompany_rule for super::RemoteReducers {
    fn update_intercompany_rule_then(
        &self,
        organization_id: u64,
        company_id: u64,
        rule_id: u64,
        params: UpdateIntercompanyRuleParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateIntercompanyRuleArgs {
                organization_id,
                company_id,
                rule_id,
                params,
            },
            callback,
        )
    }
}
