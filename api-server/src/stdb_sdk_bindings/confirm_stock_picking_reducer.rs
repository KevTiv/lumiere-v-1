// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::company_scope_params_type::CompanyScopeParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ConfirmStockPickingArgs {
    pub organization_id: u64,
    pub picking_id: u64,
    pub params: CompanyScopeParams,
}

impl From<ConfirmStockPickingArgs> for super::Reducer {
    fn from(args: ConfirmStockPickingArgs) -> Self {
        Self::ConfirmStockPicking {
            organization_id: args.organization_id,
            picking_id: args.picking_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for ConfirmStockPickingArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `confirm_stock_picking`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait confirm_stock_picking {
    /// Request that the remote module invoke the reducer `confirm_stock_picking` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`confirm_stock_picking:confirm_stock_picking_then`] to run a callback after the reducer completes.
    fn confirm_stock_picking(
        &self,
        organization_id: u64,
        picking_id: u64,
        params: CompanyScopeParams,
    ) -> __sdk::Result<()> {
        self.confirm_stock_picking_then(organization_id, picking_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `confirm_stock_picking` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn confirm_stock_picking_then(
        &self,
        organization_id: u64,
        picking_id: u64,
        params: CompanyScopeParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl confirm_stock_picking for super::RemoteReducers {
    fn confirm_stock_picking_then(
        &self,
        organization_id: u64,
        picking_id: u64,
        params: CompanyScopeParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ConfirmStockPickingArgs {
                organization_id,
                picking_id,
                params,
            },
            callback,
        )
    }
}
