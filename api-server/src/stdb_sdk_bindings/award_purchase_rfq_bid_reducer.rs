// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AwardPurchaseRfqBidArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub rfq_id: u64,
    pub bid_id: u64,
}

impl From<AwardPurchaseRfqBidArgs> for super::Reducer {
    fn from(args: AwardPurchaseRfqBidArgs) -> Self {
        Self::AwardPurchaseRfqBid {
            organization_id: args.organization_id,
            company_id: args.company_id,
            rfq_id: args.rfq_id,
            bid_id: args.bid_id,
        }
    }
}

impl __sdk::InModule for AwardPurchaseRfqBidArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `award_purchase_rfq_bid`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait award_purchase_rfq_bid {
    /// Request that the remote module invoke the reducer `award_purchase_rfq_bid` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`award_purchase_rfq_bid:award_purchase_rfq_bid_then`] to run a callback after the reducer completes.
    fn award_purchase_rfq_bid(
        &self,
        organization_id: u64,
        company_id: u64,
        rfq_id: u64,
        bid_id: u64,
    ) -> __sdk::Result<()> {
        self.award_purchase_rfq_bid_then(organization_id, company_id, rfq_id, bid_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `award_purchase_rfq_bid` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn award_purchase_rfq_bid_then(
        &self,
        organization_id: u64,
        company_id: u64,
        rfq_id: u64,
        bid_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl award_purchase_rfq_bid for super::RemoteReducers {
    fn award_purchase_rfq_bid_then(
        &self,
        organization_id: u64,
        company_id: u64,
        rfq_id: u64,
        bid_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AwardPurchaseRfqBidArgs {
                organization_id,
                company_id,
                rfq_id,
                bid_id,
            },
            callback,
        )
    }
}
