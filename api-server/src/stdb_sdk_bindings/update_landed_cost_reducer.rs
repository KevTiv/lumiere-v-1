// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_landed_cost_params_type::UpdateLandedCostParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateLandedCostArgs {
    pub organization_id: u64,
    pub landed_cost_id: u64,
    pub params: UpdateLandedCostParams,
}

impl From<UpdateLandedCostArgs> for super::Reducer {
    fn from(args: UpdateLandedCostArgs) -> Self {
        Self::UpdateLandedCost {
            organization_id: args.organization_id,
            landed_cost_id: args.landed_cost_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateLandedCostArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_landed_cost`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_landed_cost {
    /// Request that the remote module invoke the reducer `update_landed_cost` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_landed_cost:update_landed_cost_then`] to run a callback after the reducer completes.
    fn update_landed_cost(
        &self,
        organization_id: u64,
        landed_cost_id: u64,
        params: UpdateLandedCostParams,
    ) -> __sdk::Result<()> {
        self.update_landed_cost_then(organization_id, landed_cost_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_landed_cost` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_landed_cost_then(
        &self,
        organization_id: u64,
        landed_cost_id: u64,
        params: UpdateLandedCostParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_landed_cost for super::RemoteReducers {
    fn update_landed_cost_then(
        &self,
        organization_id: u64,
        landed_cost_id: u64,
        params: UpdateLandedCostParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateLandedCostArgs {
                organization_id,
                landed_cost_id,
                params,
            },
            callback,
        )
    }
}
