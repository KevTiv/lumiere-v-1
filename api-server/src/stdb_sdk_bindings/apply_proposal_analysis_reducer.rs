// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::apply_proposal_analysis_params_type::ApplyProposalAnalysisParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ApplyProposalAnalysisArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub proposal_id: u64,
    pub params: ApplyProposalAnalysisParams,
}

impl From<ApplyProposalAnalysisArgs> for super::Reducer {
    fn from(args: ApplyProposalAnalysisArgs) -> Self {
        Self::ApplyProposalAnalysis {
            organization_id: args.organization_id,
            company_id: args.company_id,
            proposal_id: args.proposal_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for ApplyProposalAnalysisArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `apply_proposal_analysis`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait apply_proposal_analysis {
    /// Request that the remote module invoke the reducer `apply_proposal_analysis` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`apply_proposal_analysis:apply_proposal_analysis_then`] to run a callback after the reducer completes.
    fn apply_proposal_analysis(
        &self,
        organization_id: u64,
        company_id: u64,
        proposal_id: u64,
        params: ApplyProposalAnalysisParams,
    ) -> __sdk::Result<()> {
        self.apply_proposal_analysis_then(
            organization_id,
            company_id,
            proposal_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `apply_proposal_analysis` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn apply_proposal_analysis_then(
        &self,
        organization_id: u64,
        company_id: u64,
        proposal_id: u64,
        params: ApplyProposalAnalysisParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl apply_proposal_analysis for super::RemoteReducers {
    fn apply_proposal_analysis_then(
        &self,
        organization_id: u64,
        company_id: u64,
        proposal_id: u64,
        params: ApplyProposalAnalysisParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ApplyProposalAnalysisArgs {
                organization_id,
                company_id,
                proposal_id,
                params,
            },
            callback,
        )
    }
}
