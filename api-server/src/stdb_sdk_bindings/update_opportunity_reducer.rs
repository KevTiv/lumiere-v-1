// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_opportunity_params_type::UpdateOpportunityParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateOpportunityArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub opportunity_id: u64,
    pub params: UpdateOpportunityParams,
}

impl From<UpdateOpportunityArgs> for super::Reducer {
    fn from(args: UpdateOpportunityArgs) -> Self {
        Self::UpdateOpportunity {
            organization_id: args.organization_id,
            company_id: args.company_id,
            opportunity_id: args.opportunity_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateOpportunityArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_opportunity`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_opportunity {
    /// Request that the remote module invoke the reducer `update_opportunity` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_opportunity:update_opportunity_then`] to run a callback after the reducer completes.
    fn update_opportunity(
        &self,
        organization_id: u64,
        company_id: u64,
        opportunity_id: u64,
        params: UpdateOpportunityParams,
    ) -> __sdk::Result<()> {
        self.update_opportunity_then(
            organization_id,
            company_id,
            opportunity_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_opportunity` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_opportunity_then(
        &self,
        organization_id: u64,
        company_id: u64,
        opportunity_id: u64,
        params: UpdateOpportunityParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_opportunity for super::RemoteReducers {
    fn update_opportunity_then(
        &self,
        organization_id: u64,
        company_id: u64,
        opportunity_id: u64,
        params: UpdateOpportunityParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateOpportunityArgs {
                organization_id,
                company_id,
                opportunity_id,
                params,
            },
            callback,
        )
    }
}
