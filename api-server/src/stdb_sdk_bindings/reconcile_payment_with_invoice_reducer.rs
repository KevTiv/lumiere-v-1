// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ReconcilePaymentWithInvoiceArgs {
    pub organization_id: u64,
    pub payment_move_id: u64,
    pub invoice_move_id: u64,
}

impl From<ReconcilePaymentWithInvoiceArgs> for super::Reducer {
    fn from(args: ReconcilePaymentWithInvoiceArgs) -> Self {
        Self::ReconcilePaymentWithInvoice {
            organization_id: args.organization_id,
            payment_move_id: args.payment_move_id,
            invoice_move_id: args.invoice_move_id,
        }
    }
}

impl __sdk::InModule for ReconcilePaymentWithInvoiceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `reconcile_payment_with_invoice`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait reconcile_payment_with_invoice {
    /// Request that the remote module invoke the reducer `reconcile_payment_with_invoice` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`reconcile_payment_with_invoice:reconcile_payment_with_invoice_then`] to run a callback after the reducer completes.
    fn reconcile_payment_with_invoice(
        &self,
        organization_id: u64,
        payment_move_id: u64,
        invoice_move_id: u64,
    ) -> __sdk::Result<()> {
        self.reconcile_payment_with_invoice_then(
            organization_id,
            payment_move_id,
            invoice_move_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `reconcile_payment_with_invoice` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn reconcile_payment_with_invoice_then(
        &self,
        organization_id: u64,
        payment_move_id: u64,
        invoice_move_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl reconcile_payment_with_invoice for super::RemoteReducers {
    fn reconcile_payment_with_invoice_then(
        &self,
        organization_id: u64,
        payment_move_id: u64,
        invoice_move_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ReconcilePaymentWithInvoiceArgs {
                organization_id,
                payment_move_id,
                invoice_move_id,
            },
            callback,
        )
    }
}
