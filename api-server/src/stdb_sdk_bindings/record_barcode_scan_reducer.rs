// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::record_barcode_scan_params_type::RecordBarcodeScanParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecordBarcodeScanArgs {
    pub organization_id: u64,
    pub params: RecordBarcodeScanParams,
}

impl From<RecordBarcodeScanArgs> for super::Reducer {
    fn from(args: RecordBarcodeScanArgs) -> Self {
        Self::RecordBarcodeScan {
            organization_id: args.organization_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for RecordBarcodeScanArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `record_barcode_scan`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait record_barcode_scan {
    /// Request that the remote module invoke the reducer `record_barcode_scan` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`record_barcode_scan:record_barcode_scan_then`] to run a callback after the reducer completes.
    fn record_barcode_scan(
        &self,
        organization_id: u64,
        params: RecordBarcodeScanParams,
    ) -> __sdk::Result<()> {
        self.record_barcode_scan_then(organization_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `record_barcode_scan` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn record_barcode_scan_then(
        &self,
        organization_id: u64,
        params: RecordBarcodeScanParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl record_barcode_scan for super::RemoteReducers {
    fn record_barcode_scan_then(
        &self,
        organization_id: u64,
        params: RecordBarcodeScanParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecordBarcodeScanArgs {
                organization_id,
                params,
            },
            callback,
        )
    }
}
