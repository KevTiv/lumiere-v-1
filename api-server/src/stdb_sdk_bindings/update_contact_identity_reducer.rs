// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_contact_identity_params_type::UpdateContactIdentityParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateContactIdentityArgs {
    pub organization_id: u64,
    pub identity_id: u64,
    pub params: UpdateContactIdentityParams,
}

impl From<UpdateContactIdentityArgs> for super::Reducer {
    fn from(args: UpdateContactIdentityArgs) -> Self {
        Self::UpdateContactIdentity {
            organization_id: args.organization_id,
            identity_id: args.identity_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateContactIdentityArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_contact_identity`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_contact_identity {
    /// Request that the remote module invoke the reducer `update_contact_identity` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_contact_identity:update_contact_identity_then`] to run a callback after the reducer completes.
    fn update_contact_identity(
        &self,
        organization_id: u64,
        identity_id: u64,
        params: UpdateContactIdentityParams,
    ) -> __sdk::Result<()> {
        self.update_contact_identity_then(organization_id, identity_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_contact_identity` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_contact_identity_then(
        &self,
        organization_id: u64,
        identity_id: u64,
        params: UpdateContactIdentityParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_contact_identity for super::RemoteReducers {
    fn update_contact_identity_then(
        &self,
        organization_id: u64,
        identity_id: u64,
        params: UpdateContactIdentityParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateContactIdentityArgs {
                organization_id,
                identity_id,
                params,
            },
            callback,
        )
    }
}
