// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ApproveLeaveArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub leave_id: u64,
}

impl From<ApproveLeaveArgs> for super::Reducer {
    fn from(args: ApproveLeaveArgs) -> Self {
        Self::ApproveLeave {
            organization_id: args.organization_id,
            company_id: args.company_id,
            leave_id: args.leave_id,
        }
    }
}

impl __sdk::InModule for ApproveLeaveArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `approve_leave`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait approve_leave {
    /// Request that the remote module invoke the reducer `approve_leave` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`approve_leave:approve_leave_then`] to run a callback after the reducer completes.
    fn approve_leave(
        &self,
        organization_id: u64,
        company_id: u64,
        leave_id: u64,
    ) -> __sdk::Result<()> {
        self.approve_leave_then(organization_id, company_id, leave_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `approve_leave` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn approve_leave_then(
        &self,
        organization_id: u64,
        company_id: u64,
        leave_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl approve_leave for super::RemoteReducers {
    fn approve_leave_then(
        &self,
        organization_id: u64,
        company_id: u64,
        leave_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ApproveLeaveArgs {
                organization_id,
                company_id,
                leave_id,
            },
            callback,
        )
    }
}
