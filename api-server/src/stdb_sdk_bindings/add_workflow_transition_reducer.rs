// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::add_workflow_transition_params_type::AddWorkflowTransitionParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddWorkflowTransitionArgs {
    pub organization_id: u64,
    pub workflow_id: u64,
    pub activity_from: u64,
    pub activity_to: u64,
    pub params: AddWorkflowTransitionParams,
}

impl From<AddWorkflowTransitionArgs> for super::Reducer {
    fn from(args: AddWorkflowTransitionArgs) -> Self {
        Self::AddWorkflowTransition {
            organization_id: args.organization_id,
            workflow_id: args.workflow_id,
            activity_from: args.activity_from,
            activity_to: args.activity_to,
            params: args.params,
        }
    }
}

impl __sdk::InModule for AddWorkflowTransitionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_workflow_transition`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_workflow_transition {
    /// Request that the remote module invoke the reducer `add_workflow_transition` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_workflow_transition:add_workflow_transition_then`] to run a callback after the reducer completes.
    fn add_workflow_transition(
        &self,
        organization_id: u64,
        workflow_id: u64,
        activity_from: u64,
        activity_to: u64,
        params: AddWorkflowTransitionParams,
    ) -> __sdk::Result<()> {
        self.add_workflow_transition_then(
            organization_id,
            workflow_id,
            activity_from,
            activity_to,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `add_workflow_transition` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_workflow_transition_then(
        &self,
        organization_id: u64,
        workflow_id: u64,
        activity_from: u64,
        activity_to: u64,
        params: AddWorkflowTransitionParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl add_workflow_transition for super::RemoteReducers {
    fn add_workflow_transition_then(
        &self,
        organization_id: u64,
        workflow_id: u64,
        activity_from: u64,
        activity_to: u64,
        params: AddWorkflowTransitionParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddWorkflowTransitionArgs {
                organization_id,
                workflow_id,
                activity_from,
                activity_to,
                params,
            },
            callback,
        )
    }
}
