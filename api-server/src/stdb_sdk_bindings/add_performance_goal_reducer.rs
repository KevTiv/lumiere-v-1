// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::add_performance_goal_params_type::AddPerformanceGoalParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddPerformanceGoalArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub cycle_id: u64,
    pub params: AddPerformanceGoalParams,
}

impl From<AddPerformanceGoalArgs> for super::Reducer {
    fn from(args: AddPerformanceGoalArgs) -> Self {
        Self::AddPerformanceGoal {
            organization_id: args.organization_id,
            company_id: args.company_id,
            cycle_id: args.cycle_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for AddPerformanceGoalArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_performance_goal`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_performance_goal {
    /// Request that the remote module invoke the reducer `add_performance_goal` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_performance_goal:add_performance_goal_then`] to run a callback after the reducer completes.
    fn add_performance_goal(
        &self,
        organization_id: u64,
        company_id: u64,
        cycle_id: u64,
        params: AddPerformanceGoalParams,
    ) -> __sdk::Result<()> {
        self.add_performance_goal_then(organization_id, company_id, cycle_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `add_performance_goal` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_performance_goal_then(
        &self,
        organization_id: u64,
        company_id: u64,
        cycle_id: u64,
        params: AddPerformanceGoalParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl add_performance_goal for super::RemoteReducers {
    fn add_performance_goal_then(
        &self,
        organization_id: u64,
        company_id: u64,
        cycle_id: u64,
        params: AddPerformanceGoalParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddPerformanceGoalArgs {
                organization_id,
                company_id,
                cycle_id,
                params,
            },
            callback,
        )
    }
}
