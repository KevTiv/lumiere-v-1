// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ReserveSerialArgs {
    pub organization_id: u64,
    pub serial_id: u64,
}

impl From<ReserveSerialArgs> for super::Reducer {
    fn from(args: ReserveSerialArgs) -> Self {
        Self::ReserveSerial {
            organization_id: args.organization_id,
            serial_id: args.serial_id,
        }
    }
}

impl __sdk::InModule for ReserveSerialArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `reserve_serial`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait reserve_serial {
    /// Request that the remote module invoke the reducer `reserve_serial` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`reserve_serial:reserve_serial_then`] to run a callback after the reducer completes.
    fn reserve_serial(&self, organization_id: u64, serial_id: u64) -> __sdk::Result<()> {
        self.reserve_serial_then(organization_id, serial_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `reserve_serial` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn reserve_serial_then(
        &self,
        organization_id: u64,
        serial_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl reserve_serial for super::RemoteReducers {
    fn reserve_serial_then(
        &self,
        organization_id: u64,
        serial_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ReserveSerialArgs {
                organization_id,
                serial_id,
            },
            callback,
        )
    }
}
