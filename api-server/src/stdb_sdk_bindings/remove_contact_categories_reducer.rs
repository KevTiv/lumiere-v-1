// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RemoveContactCategoriesArgs {
    pub organization_id: u64,
    pub contact_id: u64,
    pub category_ids: Vec<u64>,
}

impl From<RemoveContactCategoriesArgs> for super::Reducer {
    fn from(args: RemoveContactCategoriesArgs) -> Self {
        Self::RemoveContactCategories {
            organization_id: args.organization_id,
            contact_id: args.contact_id,
            category_ids: args.category_ids,
        }
    }
}

impl __sdk::InModule for RemoveContactCategoriesArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `remove_contact_categories`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait remove_contact_categories {
    /// Request that the remote module invoke the reducer `remove_contact_categories` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`remove_contact_categories:remove_contact_categories_then`] to run a callback after the reducer completes.
    fn remove_contact_categories(
        &self,
        organization_id: u64,
        contact_id: u64,
        category_ids: Vec<u64>,
    ) -> __sdk::Result<()> {
        self.remove_contact_categories_then(organization_id, contact_id, category_ids, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `remove_contact_categories` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn remove_contact_categories_then(
        &self,
        organization_id: u64,
        contact_id: u64,
        category_ids: Vec<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl remove_contact_categories for super::RemoteReducers {
    fn remove_contact_categories_then(
        &self,
        organization_id: u64,
        contact_id: u64,
        category_ids: Vec<u64>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RemoveContactCategoriesArgs {
                organization_id,
                contact_id,
                category_ids,
            },
            callback,
        )
    }
}
