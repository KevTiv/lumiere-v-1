// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::tax_deadline_status_job_type::TaxDeadlineStatusJob;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `tax_deadline_status_job`.
///
/// Obtain a handle from the [`TaxDeadlineStatusJobTableAccess::tax_deadline_status_job`] method on [`super::RemoteTables`],
/// like `ctx.db.tax_deadline_status_job()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.tax_deadline_status_job().on_insert(...)`.
pub struct TaxDeadlineStatusJobTableHandle<'ctx> {
    imp: __sdk::TableHandle<TaxDeadlineStatusJob>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `tax_deadline_status_job`.
///
/// Implemented for [`super::RemoteTables`].
pub trait TaxDeadlineStatusJobTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`TaxDeadlineStatusJobTableHandle`], which mediates access to the table `tax_deadline_status_job`.
    fn tax_deadline_status_job(&self) -> TaxDeadlineStatusJobTableHandle<'_>;
}

impl TaxDeadlineStatusJobTableAccess for super::RemoteTables {
    fn tax_deadline_status_job(&self) -> TaxDeadlineStatusJobTableHandle<'_> {
        TaxDeadlineStatusJobTableHandle {
            imp: self
                .imp
                .get_table::<TaxDeadlineStatusJob>("tax_deadline_status_job"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct TaxDeadlineStatusJobInsertCallbackId(__sdk::CallbackId);
pub struct TaxDeadlineStatusJobDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for TaxDeadlineStatusJobTableHandle<'ctx> {
    type Row = TaxDeadlineStatusJob;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = TaxDeadlineStatusJob> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = TaxDeadlineStatusJobInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> TaxDeadlineStatusJobInsertCallbackId {
        TaxDeadlineStatusJobInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: TaxDeadlineStatusJobInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = TaxDeadlineStatusJobDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> TaxDeadlineStatusJobDeleteCallbackId {
        TaxDeadlineStatusJobDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: TaxDeadlineStatusJobDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct TaxDeadlineStatusJobUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for TaxDeadlineStatusJobTableHandle<'ctx> {
    type UpdateCallbackId = TaxDeadlineStatusJobUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> TaxDeadlineStatusJobUpdateCallbackId {
        TaxDeadlineStatusJobUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: TaxDeadlineStatusJobUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `scheduled_id` unique index on the table `tax_deadline_status_job`,
/// which allows point queries on the field of the same name
/// via the [`TaxDeadlineStatusJobScheduledIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.tax_deadline_status_job().scheduled_id().find(...)`.
pub struct TaxDeadlineStatusJobScheduledIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<TaxDeadlineStatusJob, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> TaxDeadlineStatusJobTableHandle<'ctx> {
    /// Get a handle on the `scheduled_id` unique index on the table `tax_deadline_status_job`.
    pub fn scheduled_id(&self) -> TaxDeadlineStatusJobScheduledIdUnique<'ctx> {
        TaxDeadlineStatusJobScheduledIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("scheduled_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> TaxDeadlineStatusJobScheduledIdUnique<'ctx> {
    /// Find the subscribed row whose `scheduled_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<TaxDeadlineStatusJob> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<TaxDeadlineStatusJob>("tax_deadline_status_job");
    _table.add_unique_constraint::<u64>("scheduled_id", |row| &row.scheduled_id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<TaxDeadlineStatusJob>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<TaxDeadlineStatusJob>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `TaxDeadlineStatusJob`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait tax_deadline_status_jobQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `TaxDeadlineStatusJob`.
    fn tax_deadline_status_job(&self) -> __sdk::__query_builder::Table<TaxDeadlineStatusJob>;
}

impl tax_deadline_status_jobQueryTableAccess for __sdk::QueryTableAccessor {
    fn tax_deadline_status_job(&self) -> __sdk::__query_builder::Table<TaxDeadlineStatusJob> {
        __sdk::__query_builder::Table::new("tax_deadline_status_job")
    }
}
