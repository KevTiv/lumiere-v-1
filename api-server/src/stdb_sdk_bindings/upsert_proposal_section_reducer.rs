// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertProposalSectionArgs {
    pub proposal_id: u64,
    pub section_id: u64,
    pub title: String,
    pub content: String,
    pub status: String,
    pub sequence: u32,
    pub ai_suggestion: Option<String>,
}

impl From<UpsertProposalSectionArgs> for super::Reducer {
    fn from(args: UpsertProposalSectionArgs) -> Self {
        Self::UpsertProposalSection {
            proposal_id: args.proposal_id,
            section_id: args.section_id,
            title: args.title,
            content: args.content,
            status: args.status,
            sequence: args.sequence,
            ai_suggestion: args.ai_suggestion,
        }
    }
}

impl __sdk::InModule for UpsertProposalSectionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_proposal_section`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_proposal_section {
    /// Request that the remote module invoke the reducer `upsert_proposal_section` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_proposal_section:upsert_proposal_section_then`] to run a callback after the reducer completes.
    fn upsert_proposal_section(
        &self,
        proposal_id: u64,
        section_id: u64,
        title: String,
        content: String,
        status: String,
        sequence: u32,
        ai_suggestion: Option<String>,
    ) -> __sdk::Result<()> {
        self.upsert_proposal_section_then(
            proposal_id,
            section_id,
            title,
            content,
            status,
            sequence,
            ai_suggestion,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_proposal_section` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_proposal_section_then(
        &self,
        proposal_id: u64,
        section_id: u64,
        title: String,
        content: String,
        status: String,
        sequence: u32,
        ai_suggestion: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_proposal_section for super::RemoteReducers {
    fn upsert_proposal_section_then(
        &self,
        proposal_id: u64,
        section_id: u64,
        title: String,
        content: String,
        status: String,
        sequence: u32,
        ai_suggestion: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertProposalSectionArgs {
                proposal_id,
                section_id,
                title,
                content,
                status,
                sequence,
                ai_suggestion,
            },
            callback,
        )
    }
}
