// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdatePosTerminalArgs {
    pub terminal_id: u64,
    pub status: String,
    pub daily_revenue: f64,
    pub open_orders: u32,
}

impl From<UpdatePosTerminalArgs> for super::Reducer {
    fn from(args: UpdatePosTerminalArgs) -> Self {
        Self::UpdatePosTerminal {
            terminal_id: args.terminal_id,
            status: args.status,
            daily_revenue: args.daily_revenue,
            open_orders: args.open_orders,
        }
    }
}

impl __sdk::InModule for UpdatePosTerminalArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_pos_terminal`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_pos_terminal {
    /// Request that the remote module invoke the reducer `update_pos_terminal` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_pos_terminal:update_pos_terminal_then`] to run a callback after the reducer completes.
    fn update_pos_terminal(
        &self,
        terminal_id: u64,
        status: String,
        daily_revenue: f64,
        open_orders: u32,
    ) -> __sdk::Result<()> {
        self.update_pos_terminal_then(terminal_id, status, daily_revenue, open_orders, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_pos_terminal` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_pos_terminal_then(
        &self,
        terminal_id: u64,
        status: String,
        daily_revenue: f64,
        open_orders: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_pos_terminal for super::RemoteReducers {
    fn update_pos_terminal_then(
        &self,
        terminal_id: u64,
        status: String,
        daily_revenue: f64,
        open_orders: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdatePosTerminalArgs {
                terminal_id,
                status,
                daily_revenue,
                open_orders,
            },
            callback,
        )
    }
}
