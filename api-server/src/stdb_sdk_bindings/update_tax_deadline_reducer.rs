// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_tax_deadline_params_type::UpdateTaxDeadlineParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateTaxDeadlineArgs {
    pub organization_id: u64,
    pub deadline_id: u64,
    pub params: UpdateTaxDeadlineParams,
}

impl From<UpdateTaxDeadlineArgs> for super::Reducer {
    fn from(args: UpdateTaxDeadlineArgs) -> Self {
        Self::UpdateTaxDeadline {
            organization_id: args.organization_id,
            deadline_id: args.deadline_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateTaxDeadlineArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_tax_deadline`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_tax_deadline {
    /// Request that the remote module invoke the reducer `update_tax_deadline` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_tax_deadline:update_tax_deadline_then`] to run a callback after the reducer completes.
    fn update_tax_deadline(
        &self,
        organization_id: u64,
        deadline_id: u64,
        params: UpdateTaxDeadlineParams,
    ) -> __sdk::Result<()> {
        self.update_tax_deadline_then(organization_id, deadline_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_tax_deadline` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_tax_deadline_then(
        &self,
        organization_id: u64,
        deadline_id: u64,
        params: UpdateTaxDeadlineParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_tax_deadline for super::RemoteReducers {
    fn update_tax_deadline_then(
        &self,
        organization_id: u64,
        deadline_id: u64,
        params: UpdateTaxDeadlineParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateTaxDeadlineArgs {
                organization_id,
                deadline_id,
                params,
            },
            callback,
        )
    }
}
