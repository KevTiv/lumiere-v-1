// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CancelAiAgentRunArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub run_id: u64,
    pub reason: Option<String>,
}

impl From<CancelAiAgentRunArgs> for super::Reducer {
    fn from(args: CancelAiAgentRunArgs) -> Self {
        Self::CancelAiAgentRun {
            organization_id: args.organization_id,
            company_id: args.company_id,
            run_id: args.run_id,
            reason: args.reason,
        }
    }
}

impl __sdk::InModule for CancelAiAgentRunArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `cancel_ai_agent_run`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait cancel_ai_agent_run {
    /// Request that the remote module invoke the reducer `cancel_ai_agent_run` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`cancel_ai_agent_run:cancel_ai_agent_run_then`] to run a callback after the reducer completes.
    fn cancel_ai_agent_run(
        &self,
        organization_id: u64,
        company_id: u64,
        run_id: u64,
        reason: Option<String>,
    ) -> __sdk::Result<()> {
        self.cancel_ai_agent_run_then(organization_id, company_id, run_id, reason, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `cancel_ai_agent_run` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn cancel_ai_agent_run_then(
        &self,
        organization_id: u64,
        company_id: u64,
        run_id: u64,
        reason: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl cancel_ai_agent_run for super::RemoteReducers {
    fn cancel_ai_agent_run_then(
        &self,
        organization_id: u64,
        company_id: u64,
        run_id: u64,
        reason: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CancelAiAgentRunArgs {
                organization_id,
                company_id,
                run_id,
                reason,
            },
            callback,
        )
    }
}
