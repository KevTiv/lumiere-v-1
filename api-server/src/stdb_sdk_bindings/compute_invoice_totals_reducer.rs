// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ComputeInvoiceTotalsArgs {
    pub organization_id: u64,
    pub move_id: u64,
}

impl From<ComputeInvoiceTotalsArgs> for super::Reducer {
    fn from(args: ComputeInvoiceTotalsArgs) -> Self {
        Self::ComputeInvoiceTotals {
            organization_id: args.organization_id,
            move_id: args.move_id,
        }
    }
}

impl __sdk::InModule for ComputeInvoiceTotalsArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `compute_invoice_totals`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait compute_invoice_totals {
    /// Request that the remote module invoke the reducer `compute_invoice_totals` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`compute_invoice_totals:compute_invoice_totals_then`] to run a callback after the reducer completes.
    fn compute_invoice_totals(&self, organization_id: u64, move_id: u64) -> __sdk::Result<()> {
        self.compute_invoice_totals_then(organization_id, move_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `compute_invoice_totals` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn compute_invoice_totals_then(
        &self,
        organization_id: u64,
        move_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl compute_invoice_totals for super::RemoteReducers {
    fn compute_invoice_totals_then(
        &self,
        organization_id: u64,
        move_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ComputeInvoiceTotalsArgs {
                organization_id,
                move_id,
            },
            callback,
        )
    }
}
