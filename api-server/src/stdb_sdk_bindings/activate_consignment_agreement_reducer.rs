// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ActivateConsignmentAgreementArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub agreement_id: u64,
}

impl From<ActivateConsignmentAgreementArgs> for super::Reducer {
    fn from(args: ActivateConsignmentAgreementArgs) -> Self {
        Self::ActivateConsignmentAgreement {
            organization_id: args.organization_id,
            company_id: args.company_id,
            agreement_id: args.agreement_id,
        }
    }
}

impl __sdk::InModule for ActivateConsignmentAgreementArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `activate_consignment_agreement`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait activate_consignment_agreement {
    /// Request that the remote module invoke the reducer `activate_consignment_agreement` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`activate_consignment_agreement:activate_consignment_agreement_then`] to run a callback after the reducer completes.
    fn activate_consignment_agreement(
        &self,
        organization_id: u64,
        company_id: u64,
        agreement_id: u64,
    ) -> __sdk::Result<()> {
        self.activate_consignment_agreement_then(
            organization_id,
            company_id,
            agreement_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `activate_consignment_agreement` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn activate_consignment_agreement_then(
        &self,
        organization_id: u64,
        company_id: u64,
        agreement_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl activate_consignment_agreement for super::RemoteReducers {
    fn activate_consignment_agreement_then(
        &self,
        organization_id: u64,
        company_id: u64,
        agreement_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ActivateConsignmentAgreementArgs {
                organization_id,
                company_id,
                agreement_id,
            },
            callback,
        )
    }
}
