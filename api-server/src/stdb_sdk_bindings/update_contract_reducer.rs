// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_contract_params_type::UpdateContractParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateContractArgs {
    pub organization_id: u64,
    pub contract_id: u64,
    pub params: UpdateContractParams,
}

impl From<UpdateContractArgs> for super::Reducer {
    fn from(args: UpdateContractArgs) -> Self {
        Self::UpdateContract {
            organization_id: args.organization_id,
            contract_id: args.contract_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateContractArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_contract`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_contract {
    /// Request that the remote module invoke the reducer `update_contract` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_contract:update_contract_then`] to run a callback after the reducer completes.
    fn update_contract(
        &self,
        organization_id: u64,
        contract_id: u64,
        params: UpdateContractParams,
    ) -> __sdk::Result<()> {
        self.update_contract_then(organization_id, contract_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_contract` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_contract_then(
        &self,
        organization_id: u64,
        contract_id: u64,
        params: UpdateContractParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_contract for super::RemoteReducers {
    fn update_contract_then(
        &self,
        organization_id: u64,
        contract_id: u64,
        params: UpdateContractParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateContractArgs {
                organization_id,
                contract_id,
                params,
            },
            callback,
        )
    }
}
