// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::receive_consignment_stock_params_type::ReceiveConsignmentStockParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ReceiveConsignmentStockArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub params: ReceiveConsignmentStockParams,
}

impl From<ReceiveConsignmentStockArgs> for super::Reducer {
    fn from(args: ReceiveConsignmentStockArgs) -> Self {
        Self::ReceiveConsignmentStock {
            organization_id: args.organization_id,
            company_id: args.company_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for ReceiveConsignmentStockArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `receive_consignment_stock`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait receive_consignment_stock {
    /// Request that the remote module invoke the reducer `receive_consignment_stock` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`receive_consignment_stock:receive_consignment_stock_then`] to run a callback after the reducer completes.
    fn receive_consignment_stock(
        &self,
        organization_id: u64,
        company_id: u64,
        params: ReceiveConsignmentStockParams,
    ) -> __sdk::Result<()> {
        self.receive_consignment_stock_then(organization_id, company_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `receive_consignment_stock` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn receive_consignment_stock_then(
        &self,
        organization_id: u64,
        company_id: u64,
        params: ReceiveConsignmentStockParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl receive_consignment_stock for super::RemoteReducers {
    fn receive_consignment_stock_then(
        &self,
        organization_id: u64,
        company_id: u64,
        params: ReceiveConsignmentStockParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ReceiveConsignmentStockArgs {
                organization_id,
                company_id,
                params,
            },
            callback,
        )
    }
}
