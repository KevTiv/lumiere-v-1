// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SetIotThresholdArgs {
    pub organization_id: u64,
    pub device_id: u64,
    pub sensor_type: String,
    pub min_value: Option<f64>,
    pub max_value: Option<f64>,
    pub severity: String,
}

impl From<SetIotThresholdArgs> for super::Reducer {
    fn from(args: SetIotThresholdArgs) -> Self {
        Self::SetIotThreshold {
            organization_id: args.organization_id,
            device_id: args.device_id,
            sensor_type: args.sensor_type,
            min_value: args.min_value,
            max_value: args.max_value,
            severity: args.severity,
        }
    }
}

impl __sdk::InModule for SetIotThresholdArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `set_iot_threshold`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait set_iot_threshold {
    /// Request that the remote module invoke the reducer `set_iot_threshold` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`set_iot_threshold:set_iot_threshold_then`] to run a callback after the reducer completes.
    fn set_iot_threshold(
        &self,
        organization_id: u64,
        device_id: u64,
        sensor_type: String,
        min_value: Option<f64>,
        max_value: Option<f64>,
        severity: String,
    ) -> __sdk::Result<()> {
        self.set_iot_threshold_then(
            organization_id,
            device_id,
            sensor_type,
            min_value,
            max_value,
            severity,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `set_iot_threshold` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn set_iot_threshold_then(
        &self,
        organization_id: u64,
        device_id: u64,
        sensor_type: String,
        min_value: Option<f64>,
        max_value: Option<f64>,
        severity: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl set_iot_threshold for super::RemoteReducers {
    fn set_iot_threshold_then(
        &self,
        organization_id: u64,
        device_id: u64,
        sensor_type: String,
        min_value: Option<f64>,
        max_value: Option<f64>,
        severity: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SetIotThresholdArgs {
                organization_id,
                device_id,
                sensor_type,
                min_value,
                max_value,
                severity,
            },
            callback,
        )
    }
}
