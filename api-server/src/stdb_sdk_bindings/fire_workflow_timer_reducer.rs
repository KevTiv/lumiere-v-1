// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::fire_workflow_timer_params_type::FireWorkflowTimerParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct FireWorkflowTimerArgs {
    pub organization_id: u64,
    pub params: FireWorkflowTimerParams,
}

impl From<FireWorkflowTimerArgs> for super::Reducer {
    fn from(args: FireWorkflowTimerArgs) -> Self {
        Self::FireWorkflowTimer {
            organization_id: args.organization_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for FireWorkflowTimerArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `fire_workflow_timer`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait fire_workflow_timer {
    /// Request that the remote module invoke the reducer `fire_workflow_timer` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`fire_workflow_timer:fire_workflow_timer_then`] to run a callback after the reducer completes.
    fn fire_workflow_timer(
        &self,
        organization_id: u64,
        params: FireWorkflowTimerParams,
    ) -> __sdk::Result<()> {
        self.fire_workflow_timer_then(organization_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `fire_workflow_timer` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn fire_workflow_timer_then(
        &self,
        organization_id: u64,
        params: FireWorkflowTimerParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl fire_workflow_timer for super::RemoteReducers {
    fn fire_workflow_timer_then(
        &self,
        organization_id: u64,
        params: FireWorkflowTimerParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            FireWorkflowTimerArgs {
                organization_id,
                params,
            },
            callback,
        )
    }
}
