// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ApproveSupplierIntakeArgs {
    pub organization_id: u64,
    pub intake_id: u64,
    pub partner_id: u64,
}

impl From<ApproveSupplierIntakeArgs> for super::Reducer {
    fn from(args: ApproveSupplierIntakeArgs) -> Self {
        Self::ApproveSupplierIntake {
            organization_id: args.organization_id,
            intake_id: args.intake_id,
            partner_id: args.partner_id,
        }
    }
}

impl __sdk::InModule for ApproveSupplierIntakeArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `approve_supplier_intake`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait approve_supplier_intake {
    /// Request that the remote module invoke the reducer `approve_supplier_intake` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`approve_supplier_intake:approve_supplier_intake_then`] to run a callback after the reducer completes.
    fn approve_supplier_intake(
        &self,
        organization_id: u64,
        intake_id: u64,
        partner_id: u64,
    ) -> __sdk::Result<()> {
        self.approve_supplier_intake_then(organization_id, intake_id, partner_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `approve_supplier_intake` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn approve_supplier_intake_then(
        &self,
        organization_id: u64,
        intake_id: u64,
        partner_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl approve_supplier_intake for super::RemoteReducers {
    fn approve_supplier_intake_then(
        &self,
        organization_id: u64,
        intake_id: u64,
        partner_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ApproveSupplierIntakeArgs {
                organization_id,
                intake_id,
                partner_id,
            },
            callback,
        )
    }
}
