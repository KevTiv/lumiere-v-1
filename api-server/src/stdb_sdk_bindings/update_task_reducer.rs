// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_task_params_type::UpdateTaskParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateTaskArgs {
    pub organization_id: u64,
    pub task_id: u64,
    pub params: UpdateTaskParams,
}

impl From<UpdateTaskArgs> for super::Reducer {
    fn from(args: UpdateTaskArgs) -> Self {
        Self::UpdateTask {
            organization_id: args.organization_id,
            task_id: args.task_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateTaskArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_task`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_task {
    /// Request that the remote module invoke the reducer `update_task` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_task:update_task_then`] to run a callback after the reducer completes.
    fn update_task(
        &self,
        organization_id: u64,
        task_id: u64,
        params: UpdateTaskParams,
    ) -> __sdk::Result<()> {
        self.update_task_then(organization_id, task_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_task` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_task_then(
        &self,
        organization_id: u64,
        task_id: u64,
        params: UpdateTaskParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_task for super::RemoteReducers {
    fn update_task_then(
        &self,
        organization_id: u64,
        task_id: u64,
        params: UpdateTaskParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateTaskArgs {
                organization_id,
                task_id,
                params,
            },
            callback,
        )
    }
}
