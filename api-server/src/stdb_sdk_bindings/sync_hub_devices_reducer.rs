// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::device_sync_entry_type::DeviceSyncEntry;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SyncHubDevicesArgs {
    pub organization_id: u64,
    pub hub_id: u64,
    pub detected: Vec<DeviceSyncEntry>,
}

impl From<SyncHubDevicesArgs> for super::Reducer {
    fn from(args: SyncHubDevicesArgs) -> Self {
        Self::SyncHubDevices {
            organization_id: args.organization_id,
            hub_id: args.hub_id,
            detected: args.detected,
        }
    }
}

impl __sdk::InModule for SyncHubDevicesArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `sync_hub_devices`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait sync_hub_devices {
    /// Request that the remote module invoke the reducer `sync_hub_devices` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`sync_hub_devices:sync_hub_devices_then`] to run a callback after the reducer completes.
    fn sync_hub_devices(
        &self,
        organization_id: u64,
        hub_id: u64,
        detected: Vec<DeviceSyncEntry>,
    ) -> __sdk::Result<()> {
        self.sync_hub_devices_then(organization_id, hub_id, detected, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `sync_hub_devices` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn sync_hub_devices_then(
        &self,
        organization_id: u64,
        hub_id: u64,
        detected: Vec<DeviceSyncEntry>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl sync_hub_devices for super::RemoteReducers {
    fn sync_hub_devices_then(
        &self,
        organization_id: u64,
        hub_id: u64,
        detected: Vec<DeviceSyncEntry>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SyncHubDevicesArgs {
                organization_id,
                hub_id,
                detected,
            },
            callback,
        )
    }
}
