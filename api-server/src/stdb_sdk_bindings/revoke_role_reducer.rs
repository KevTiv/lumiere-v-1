// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RevokeRoleArgs {
    pub organization_id: u64,
    pub assignment_id: u64,
}

impl From<RevokeRoleArgs> for super::Reducer {
    fn from(args: RevokeRoleArgs) -> Self {
        Self::RevokeRole {
            organization_id: args.organization_id,
            assignment_id: args.assignment_id,
        }
    }
}

impl __sdk::InModule for RevokeRoleArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `revoke_role`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait revoke_role {
    /// Request that the remote module invoke the reducer `revoke_role` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`revoke_role:revoke_role_then`] to run a callback after the reducer completes.
    fn revoke_role(&self, organization_id: u64, assignment_id: u64) -> __sdk::Result<()> {
        self.revoke_role_then(organization_id, assignment_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `revoke_role` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn revoke_role_then(
        &self,
        organization_id: u64,
        assignment_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl revoke_role for super::RemoteReducers {
    fn revoke_role_then(
        &self,
        organization_id: u64,
        assignment_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RevokeRoleArgs {
                organization_id,
                assignment_id,
            },
            callback,
        )
    }
}
