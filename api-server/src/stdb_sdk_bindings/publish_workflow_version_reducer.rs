// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PublishWorkflowVersionArgs {
    pub organization_id: u64,
    pub workflow_version_id: u64,
    pub expected_revision: u64,
}

impl From<PublishWorkflowVersionArgs> for super::Reducer {
    fn from(args: PublishWorkflowVersionArgs) -> Self {
        Self::PublishWorkflowVersion {
            organization_id: args.organization_id,
            workflow_version_id: args.workflow_version_id,
            expected_revision: args.expected_revision,
        }
    }
}

impl __sdk::InModule for PublishWorkflowVersionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `publish_workflow_version`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait publish_workflow_version {
    /// Request that the remote module invoke the reducer `publish_workflow_version` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`publish_workflow_version:publish_workflow_version_then`] to run a callback after the reducer completes.
    fn publish_workflow_version(
        &self,
        organization_id: u64,
        workflow_version_id: u64,
        expected_revision: u64,
    ) -> __sdk::Result<()> {
        self.publish_workflow_version_then(
            organization_id,
            workflow_version_id,
            expected_revision,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `publish_workflow_version` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn publish_workflow_version_then(
        &self,
        organization_id: u64,
        workflow_version_id: u64,
        expected_revision: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl publish_workflow_version for super::RemoteReducers {
    fn publish_workflow_version_then(
        &self,
        organization_id: u64,
        workflow_version_id: u64,
        expected_revision: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PublishWorkflowVersionArgs {
                organization_id,
                workflow_version_id,
                expected_revision,
            },
            callback,
        )
    }
}
