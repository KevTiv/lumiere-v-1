// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RejectAiActionDraftArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub draft_id: u64,
    pub reason: String,
}

impl From<RejectAiActionDraftArgs> for super::Reducer {
    fn from(args: RejectAiActionDraftArgs) -> Self {
        Self::RejectAiActionDraft {
            organization_id: args.organization_id,
            company_id: args.company_id,
            draft_id: args.draft_id,
            reason: args.reason,
        }
    }
}

impl __sdk::InModule for RejectAiActionDraftArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `reject_ai_action_draft`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait reject_ai_action_draft {
    /// Request that the remote module invoke the reducer `reject_ai_action_draft` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`reject_ai_action_draft:reject_ai_action_draft_then`] to run a callback after the reducer completes.
    fn reject_ai_action_draft(
        &self,
        organization_id: u64,
        company_id: u64,
        draft_id: u64,
        reason: String,
    ) -> __sdk::Result<()> {
        self.reject_ai_action_draft_then(organization_id, company_id, draft_id, reason, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `reject_ai_action_draft` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn reject_ai_action_draft_then(
        &self,
        organization_id: u64,
        company_id: u64,
        draft_id: u64,
        reason: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl reject_ai_action_draft for super::RemoteReducers {
    fn reject_ai_action_draft_then(
        &self,
        organization_id: u64,
        company_id: u64,
        draft_id: u64,
        reason: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RejectAiActionDraftArgs {
                organization_id,
                company_id,
                draft_id,
                reason,
            },
            callback,
        )
    }
}
