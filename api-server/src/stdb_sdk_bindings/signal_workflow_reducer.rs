// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::signal_workflow_params_type::SignalWorkflowParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SignalWorkflowArgs {
    pub organization_id: u64,
    pub params: SignalWorkflowParams,
}

impl From<SignalWorkflowArgs> for super::Reducer {
    fn from(args: SignalWorkflowArgs) -> Self {
        Self::SignalWorkflow {
            organization_id: args.organization_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for SignalWorkflowArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `signal_workflow`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait signal_workflow {
    /// Request that the remote module invoke the reducer `signal_workflow` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`signal_workflow:signal_workflow_then`] to run a callback after the reducer completes.
    fn signal_workflow(
        &self,
        organization_id: u64,
        params: SignalWorkflowParams,
    ) -> __sdk::Result<()> {
        self.signal_workflow_then(organization_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `signal_workflow` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn signal_workflow_then(
        &self,
        organization_id: u64,
        params: SignalWorkflowParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl signal_workflow for super::RemoteReducers {
    fn signal_workflow_then(
        &self,
        organization_id: u64,
        params: SignalWorkflowParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SignalWorkflowArgs {
                organization_id,
                params,
            },
            callback,
        )
    }
}
