// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_barcode_rule_params_type::UpdateBarcodeRuleParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateBarcodeRuleArgs {
    pub organization_id: u64,
    pub rule_id: u64,
    pub params: UpdateBarcodeRuleParams,
}

impl From<UpdateBarcodeRuleArgs> for super::Reducer {
    fn from(args: UpdateBarcodeRuleArgs) -> Self {
        Self::UpdateBarcodeRule {
            organization_id: args.organization_id,
            rule_id: args.rule_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateBarcodeRuleArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_barcode_rule`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_barcode_rule {
    /// Request that the remote module invoke the reducer `update_barcode_rule` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_barcode_rule:update_barcode_rule_then`] to run a callback after the reducer completes.
    fn update_barcode_rule(
        &self,
        organization_id: u64,
        rule_id: u64,
        params: UpdateBarcodeRuleParams,
    ) -> __sdk::Result<()> {
        self.update_barcode_rule_then(organization_id, rule_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_barcode_rule` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_barcode_rule_then(
        &self,
        organization_id: u64,
        rule_id: u64,
        params: UpdateBarcodeRuleParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_barcode_rule for super::RemoteReducers {
    fn update_barcode_rule_then(
        &self,
        organization_id: u64,
        rule_id: u64,
        params: UpdateBarcodeRuleParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateBarcodeRuleArgs {
                organization_id,
                rule_id,
                params,
            },
            callback,
        )
    }
}
