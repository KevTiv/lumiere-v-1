// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ComputeBomCostArgs {
    pub organization_id: u64,
    pub bom_id: u64,
}

impl From<ComputeBomCostArgs> for super::Reducer {
    fn from(args: ComputeBomCostArgs) -> Self {
        Self::ComputeBomCost {
            organization_id: args.organization_id,
            bom_id: args.bom_id,
        }
    }
}

impl __sdk::InModule for ComputeBomCostArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `compute_bom_cost`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait compute_bom_cost {
    /// Request that the remote module invoke the reducer `compute_bom_cost` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`compute_bom_cost:compute_bom_cost_then`] to run a callback after the reducer completes.
    fn compute_bom_cost(&self, organization_id: u64, bom_id: u64) -> __sdk::Result<()> {
        self.compute_bom_cost_then(organization_id, bom_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `compute_bom_cost` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn compute_bom_cost_then(
        &self,
        organization_id: u64,
        bom_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl compute_bom_cost for super::RemoteReducers {
    fn compute_bom_cost_then(
        &self,
        organization_id: u64,
        bom_id: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ComputeBomCostArgs {
                organization_id,
                bom_id,
            },
            callback,
        )
    }
}
