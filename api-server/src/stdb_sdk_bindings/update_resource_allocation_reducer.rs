// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_resource_allocation_params_type::UpdateResourceAllocationParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateResourceAllocationArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub allocation_id: u64,
    pub params: UpdateResourceAllocationParams,
}

impl From<UpdateResourceAllocationArgs> for super::Reducer {
    fn from(args: UpdateResourceAllocationArgs) -> Self {
        Self::UpdateResourceAllocation {
            organization_id: args.organization_id,
            company_id: args.company_id,
            allocation_id: args.allocation_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateResourceAllocationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_resource_allocation`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_resource_allocation {
    /// Request that the remote module invoke the reducer `update_resource_allocation` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_resource_allocation:update_resource_allocation_then`] to run a callback after the reducer completes.
    fn update_resource_allocation(
        &self,
        organization_id: u64,
        company_id: u64,
        allocation_id: u64,
        params: UpdateResourceAllocationParams,
    ) -> __sdk::Result<()> {
        self.update_resource_allocation_then(
            organization_id,
            company_id,
            allocation_id,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_resource_allocation` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_resource_allocation_then(
        &self,
        organization_id: u64,
        company_id: u64,
        allocation_id: u64,
        params: UpdateResourceAllocationParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_resource_allocation for super::RemoteReducers {
    fn update_resource_allocation_then(
        &self,
        organization_id: u64,
        company_id: u64,
        allocation_id: u64,
        params: UpdateResourceAllocationParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateResourceAllocationArgs {
                organization_id,
                company_id,
                allocation_id,
                params,
            },
            callback,
        )
    }
}
