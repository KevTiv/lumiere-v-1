// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::contact_verification_state_type::ContactVerificationState;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct VerifyContactIdentityArgs {
    pub organization_id: u64,
    pub identity_id: u64,
    pub state: ContactVerificationState,
}

impl From<VerifyContactIdentityArgs> for super::Reducer {
    fn from(args: VerifyContactIdentityArgs) -> Self {
        Self::VerifyContactIdentity {
            organization_id: args.organization_id,
            identity_id: args.identity_id,
            state: args.state,
        }
    }
}

impl __sdk::InModule for VerifyContactIdentityArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `verify_contact_identity`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait verify_contact_identity {
    /// Request that the remote module invoke the reducer `verify_contact_identity` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`verify_contact_identity:verify_contact_identity_then`] to run a callback after the reducer completes.
    fn verify_contact_identity(
        &self,
        organization_id: u64,
        identity_id: u64,
        state: ContactVerificationState,
    ) -> __sdk::Result<()> {
        self.verify_contact_identity_then(organization_id, identity_id, state, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `verify_contact_identity` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn verify_contact_identity_then(
        &self,
        organization_id: u64,
        identity_id: u64,
        state: ContactVerificationState,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl verify_contact_identity for super::RemoteReducers {
    fn verify_contact_identity_then(
        &self,
        organization_id: u64,
        identity_id: u64,
        state: ContactVerificationState,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            VerifyContactIdentityArgs {
                organization_id,
                identity_id,
                state,
            },
            callback,
        )
    }
}
