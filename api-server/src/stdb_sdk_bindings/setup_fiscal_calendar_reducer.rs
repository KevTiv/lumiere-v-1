// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::setup_fiscal_calendar_params_type::SetupFiscalCalendarParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SetupFiscalCalendarArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub params: SetupFiscalCalendarParams,
}

impl From<SetupFiscalCalendarArgs> for super::Reducer {
    fn from(args: SetupFiscalCalendarArgs) -> Self {
        Self::SetupFiscalCalendar {
            organization_id: args.organization_id,
            company_id: args.company_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for SetupFiscalCalendarArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `setup_fiscal_calendar`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait setup_fiscal_calendar {
    /// Request that the remote module invoke the reducer `setup_fiscal_calendar` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`setup_fiscal_calendar:setup_fiscal_calendar_then`] to run a callback after the reducer completes.
    fn setup_fiscal_calendar(
        &self,
        organization_id: u64,
        company_id: u64,
        params: SetupFiscalCalendarParams,
    ) -> __sdk::Result<()> {
        self.setup_fiscal_calendar_then(organization_id, company_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `setup_fiscal_calendar` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn setup_fiscal_calendar_then(
        &self,
        organization_id: u64,
        company_id: u64,
        params: SetupFiscalCalendarParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl setup_fiscal_calendar for super::RemoteReducers {
    fn setup_fiscal_calendar_then(
        &self,
        organization_id: u64,
        company_id: u64,
        params: SetupFiscalCalendarParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SetupFiscalCalendarArgs {
                organization_id,
                company_id,
                params,
            },
            callback,
        )
    }
}
