// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddProposalLineItemArgs {
    pub proposal_id: u64,
    pub section_id: Option<u64>,
    pub product_id: u64,
    pub product_name: String,
    pub quantity: f64,
    pub price_unit: f64,
    pub discount: f64,
    pub notes: Option<String>,
}

impl From<AddProposalLineItemArgs> for super::Reducer {
    fn from(args: AddProposalLineItemArgs) -> Self {
        Self::AddProposalLineItem {
            proposal_id: args.proposal_id,
            section_id: args.section_id,
            product_id: args.product_id,
            product_name: args.product_name,
            quantity: args.quantity,
            price_unit: args.price_unit,
            discount: args.discount,
            notes: args.notes,
        }
    }
}

impl __sdk::InModule for AddProposalLineItemArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_proposal_line_item`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_proposal_line_item {
    /// Request that the remote module invoke the reducer `add_proposal_line_item` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_proposal_line_item:add_proposal_line_item_then`] to run a callback after the reducer completes.
    fn add_proposal_line_item(
        &self,
        proposal_id: u64,
        section_id: Option<u64>,
        product_id: u64,
        product_name: String,
        quantity: f64,
        price_unit: f64,
        discount: f64,
        notes: Option<String>,
    ) -> __sdk::Result<()> {
        self.add_proposal_line_item_then(
            proposal_id,
            section_id,
            product_id,
            product_name,
            quantity,
            price_unit,
            discount,
            notes,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `add_proposal_line_item` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_proposal_line_item_then(
        &self,
        proposal_id: u64,
        section_id: Option<u64>,
        product_id: u64,
        product_name: String,
        quantity: f64,
        price_unit: f64,
        discount: f64,
        notes: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl add_proposal_line_item for super::RemoteReducers {
    fn add_proposal_line_item_then(
        &self,
        proposal_id: u64,
        section_id: Option<u64>,
        product_id: u64,
        product_name: String,
        quantity: f64,
        price_unit: f64,
        discount: f64,
        notes: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddProposalLineItemArgs {
                proposal_id,
                section_id,
                product_id,
                product_name,
                quantity,
                price_unit,
                discount,
                notes,
            },
            callback,
        )
    }
}
