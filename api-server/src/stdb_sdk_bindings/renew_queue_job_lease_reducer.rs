// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::renew_queue_lease_params_type::RenewQueueLeaseParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RenewQueueJobLeaseArgs {
    pub organization_id: u64,
    pub job_id: u64,
    pub params: RenewQueueLeaseParams,
}

impl From<RenewQueueJobLeaseArgs> for super::Reducer {
    fn from(args: RenewQueueJobLeaseArgs) -> Self {
        Self::RenewQueueJobLease {
            organization_id: args.organization_id,
            job_id: args.job_id,
            params: args.params,
        }
    }
}

impl __sdk::InModule for RenewQueueJobLeaseArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `renew_queue_job_lease`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait renew_queue_job_lease {
    /// Request that the remote module invoke the reducer `renew_queue_job_lease` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`renew_queue_job_lease:renew_queue_job_lease_then`] to run a callback after the reducer completes.
    fn renew_queue_job_lease(
        &self,
        organization_id: u64,
        job_id: u64,
        params: RenewQueueLeaseParams,
    ) -> __sdk::Result<()> {
        self.renew_queue_job_lease_then(organization_id, job_id, params, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `renew_queue_job_lease` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn renew_queue_job_lease_then(
        &self,
        organization_id: u64,
        job_id: u64,
        params: RenewQueueLeaseParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl renew_queue_job_lease for super::RemoteReducers {
    fn renew_queue_job_lease_then(
        &self,
        organization_id: u64,
        job_id: u64,
        params: RenewQueueLeaseParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RenewQueueJobLeaseArgs {
                organization_id,
                job_id,
                params,
            },
            callback,
        )
    }
}
