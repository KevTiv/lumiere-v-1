// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::tax_deadline_status_job_type::TaxDeadlineStatusJob;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateTaxDeadlinesArgs {
    pub job: TaxDeadlineStatusJob,
}

impl From<UpdateTaxDeadlinesArgs> for super::Reducer {
    fn from(args: UpdateTaxDeadlinesArgs) -> Self {
        Self::UpdateTaxDeadlines { job: args.job }
    }
}

impl __sdk::InModule for UpdateTaxDeadlinesArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_tax_deadlines`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_tax_deadlines {
    /// Request that the remote module invoke the reducer `update_tax_deadlines` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_tax_deadlines:update_tax_deadlines_then`] to run a callback after the reducer completes.
    fn update_tax_deadlines(&self, job: TaxDeadlineStatusJob) -> __sdk::Result<()> {
        self.update_tax_deadlines_then(job, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_tax_deadlines` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_tax_deadlines_then(
        &self,
        job: TaxDeadlineStatusJob,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_tax_deadlines for super::RemoteReducers {
    fn update_tax_deadlines_then(
        &self,
        job: TaxDeadlineStatusJob,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(UpdateTaxDeadlinesArgs { job }, callback)
    }
}
