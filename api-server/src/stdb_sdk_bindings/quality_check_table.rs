// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::quality_check_type::QualityCheck;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `quality_check`.
///
/// Obtain a handle from the [`QualityCheckTableAccess::quality_check`] method on [`super::RemoteTables`],
/// like `ctx.db.quality_check()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.quality_check().on_insert(...)`.
pub struct QualityCheckTableHandle<'ctx> {
    imp: __sdk::TableHandle<QualityCheck>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `quality_check`.
///
/// Implemented for [`super::RemoteTables`].
pub trait QualityCheckTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`QualityCheckTableHandle`], which mediates access to the table `quality_check`.
    fn quality_check(&self) -> QualityCheckTableHandle<'_>;
}

impl QualityCheckTableAccess for super::RemoteTables {
    fn quality_check(&self) -> QualityCheckTableHandle<'_> {
        QualityCheckTableHandle {
            imp: self.imp.get_table::<QualityCheck>("quality_check"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct QualityCheckInsertCallbackId(__sdk::CallbackId);
pub struct QualityCheckDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for QualityCheckTableHandle<'ctx> {
    type Row = QualityCheck;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = QualityCheck> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = QualityCheckInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> QualityCheckInsertCallbackId {
        QualityCheckInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: QualityCheckInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = QualityCheckDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> QualityCheckDeleteCallbackId {
        QualityCheckDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: QualityCheckDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct QualityCheckUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for QualityCheckTableHandle<'ctx> {
    type UpdateCallbackId = QualityCheckUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> QualityCheckUpdateCallbackId {
        QualityCheckUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: QualityCheckUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `quality_check`,
/// which allows point queries on the field of the same name
/// via the [`QualityCheckIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.quality_check().id().find(...)`.
pub struct QualityCheckIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<QualityCheck, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> QualityCheckTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `quality_check`.
    pub fn id(&self) -> QualityCheckIdUnique<'ctx> {
        QualityCheckIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> QualityCheckIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<QualityCheck> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<QualityCheck>("quality_check");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<QualityCheck>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<QualityCheck>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `QualityCheck`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait quality_checkQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `QualityCheck`.
    fn quality_check(&self) -> __sdk::__query_builder::Table<QualityCheck>;
}

impl quality_checkQueryTableAccess for __sdk::QueryTableAccessor {
    fn quality_check(&self) -> __sdk::__query_builder::Table<QualityCheck> {
        __sdk::__query_builder::Table::new("quality_check")
    }
}
