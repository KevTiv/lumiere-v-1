// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::drive_conflict_policy_type::DriveConflictPolicy;
use super::sync_direction_type::SyncDirection;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateGoogleDriveConnectionArgs {
    pub organization_id: u64,
    pub company_id: Option<u64>,
    pub name: String,
    pub account_email: String,
    pub account_id: String,
    pub credentials_reference: String,
    pub root_folder_id: Option<String>,
    pub shared_drive_id: Option<String>,
    pub sync_enabled: bool,
    pub webhook_enabled: bool,
    pub webhook_url: Option<String>,
    pub webhook_secret_reference: Option<String>,
    pub sync_direction: SyncDirection,
    pub conflict_policy: Option<DriveConflictPolicy>,
    pub sync_frequency_minutes: u32,
    pub allowed_file_types: Vec<String>,
    pub max_file_size_mb: u32,
}

impl From<CreateGoogleDriveConnectionArgs> for super::Reducer {
    fn from(args: CreateGoogleDriveConnectionArgs) -> Self {
        Self::CreateGoogleDriveConnection {
            organization_id: args.organization_id,
            company_id: args.company_id,
            name: args.name,
            account_email: args.account_email,
            account_id: args.account_id,
            credentials_reference: args.credentials_reference,
            root_folder_id: args.root_folder_id,
            shared_drive_id: args.shared_drive_id,
            sync_enabled: args.sync_enabled,
            webhook_enabled: args.webhook_enabled,
            webhook_url: args.webhook_url,
            webhook_secret_reference: args.webhook_secret_reference,
            sync_direction: args.sync_direction,
            conflict_policy: args.conflict_policy,
            sync_frequency_minutes: args.sync_frequency_minutes,
            allowed_file_types: args.allowed_file_types,
            max_file_size_mb: args.max_file_size_mb,
        }
    }
}

impl __sdk::InModule for CreateGoogleDriveConnectionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_google_drive_connection`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_google_drive_connection {
    /// Request that the remote module invoke the reducer `create_google_drive_connection` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_google_drive_connection:create_google_drive_connection_then`] to run a callback after the reducer completes.
    fn create_google_drive_connection(
        &self,
        organization_id: u64,
        company_id: Option<u64>,
        name: String,
        account_email: String,
        account_id: String,
        credentials_reference: String,
        root_folder_id: Option<String>,
        shared_drive_id: Option<String>,
        sync_enabled: bool,
        webhook_enabled: bool,
        webhook_url: Option<String>,
        webhook_secret_reference: Option<String>,
        sync_direction: SyncDirection,
        conflict_policy: Option<DriveConflictPolicy>,
        sync_frequency_minutes: u32,
        allowed_file_types: Vec<String>,
        max_file_size_mb: u32,
    ) -> __sdk::Result<()> {
        self.create_google_drive_connection_then(
            organization_id,
            company_id,
            name,
            account_email,
            account_id,
            credentials_reference,
            root_folder_id,
            shared_drive_id,
            sync_enabled,
            webhook_enabled,
            webhook_url,
            webhook_secret_reference,
            sync_direction,
            conflict_policy,
            sync_frequency_minutes,
            allowed_file_types,
            max_file_size_mb,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_google_drive_connection` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_google_drive_connection_then(
        &self,
        organization_id: u64,
        company_id: Option<u64>,
        name: String,
        account_email: String,
        account_id: String,
        credentials_reference: String,
        root_folder_id: Option<String>,
        shared_drive_id: Option<String>,
        sync_enabled: bool,
        webhook_enabled: bool,
        webhook_url: Option<String>,
        webhook_secret_reference: Option<String>,
        sync_direction: SyncDirection,
        conflict_policy: Option<DriveConflictPolicy>,
        sync_frequency_minutes: u32,
        allowed_file_types: Vec<String>,
        max_file_size_mb: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_google_drive_connection for super::RemoteReducers {
    fn create_google_drive_connection_then(
        &self,
        organization_id: u64,
        company_id: Option<u64>,
        name: String,
        account_email: String,
        account_id: String,
        credentials_reference: String,
        root_folder_id: Option<String>,
        shared_drive_id: Option<String>,
        sync_enabled: bool,
        webhook_enabled: bool,
        webhook_url: Option<String>,
        webhook_secret_reference: Option<String>,
        sync_direction: SyncDirection,
        conflict_policy: Option<DriveConflictPolicy>,
        sync_frequency_minutes: u32,
        allowed_file_types: Vec<String>,
        max_file_size_mb: u32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateGoogleDriveConnectionArgs {
                organization_id,
                company_id,
                name,
                account_email,
                account_id,
                credentials_reference,
                root_folder_id,
                shared_drive_id,
                sync_enabled,
                webhook_enabled,
                webhook_url,
                webhook_secret_reference,
                sync_direction,
                conflict_policy,
                sync_frequency_minutes,
                allowed_file_types,
                max_file_size_mb,
            },
            callback,
        )
    }
}
