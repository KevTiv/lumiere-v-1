// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::update_ai_chat_session_title_params_type::UpdateAiChatSessionTitleParams;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateAiChatSessionTitleArgs {
    pub organization_id: u64,
    pub company_id: u64,
    pub session_key: String,
    pub params: UpdateAiChatSessionTitleParams,
}

impl From<UpdateAiChatSessionTitleArgs> for super::Reducer {
    fn from(args: UpdateAiChatSessionTitleArgs) -> Self {
        Self::UpdateAiChatSessionTitle {
            organization_id: args.organization_id,
            company_id: args.company_id,
            session_key: args.session_key,
            params: args.params,
        }
    }
}

impl __sdk::InModule for UpdateAiChatSessionTitleArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_ai_chat_session_title`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_ai_chat_session_title {
    /// Request that the remote module invoke the reducer `update_ai_chat_session_title` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_ai_chat_session_title:update_ai_chat_session_title_then`] to run a callback after the reducer completes.
    fn update_ai_chat_session_title(
        &self,
        organization_id: u64,
        company_id: u64,
        session_key: String,
        params: UpdateAiChatSessionTitleParams,
    ) -> __sdk::Result<()> {
        self.update_ai_chat_session_title_then(
            organization_id,
            company_id,
            session_key,
            params,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `update_ai_chat_session_title` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_ai_chat_session_title_then(
        &self,
        organization_id: u64,
        company_id: u64,
        session_key: String,
        params: UpdateAiChatSessionTitleParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_ai_chat_session_title for super::RemoteReducers {
    fn update_ai_chat_session_title_then(
        &self,
        organization_id: u64,
        company_id: u64,
        session_key: String,
        params: UpdateAiChatSessionTitleParams,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateAiChatSessionTitleArgs {
                organization_id,
                company_id,
                session_key,
                params,
            },
            callback,
        )
    }
}
