-- Auto-generated by lumiere-codegen. Do not edit.
-- Source table: audit_log  →  Cold table: cold_audit_log

CREATE TABLE IF NOT EXISTS cold_audit_log (
    id NUMERIC(20,0) NOT NULL,
    organization_id NUMERIC(20,0) NOT NULL,
    company_id NUMERIC(20,0),
    table_name TEXT NOT NULL,
    record_id NUMERIC(20,0) NOT NULL,
    action TEXT NOT NULL,
    old_values TEXT,
    new_values TEXT,
    changed_fields JSONB NOT NULL,
    user_identity BYTEA NOT NULL,
    session_id NUMERIC(20,0),
    ip_address TEXT,
    user_agent TEXT,
    timestamp BIGINT NOT NULL,
    metadata TEXT,
    archived_at TIMESTAMPTZ NOT NULL DEFAULT now(),
    payload_checksum TEXT NOT NULL,
    CONSTRAINT cold_audit_log_pkey PRIMARY KEY (id)
);

CREATE INDEX IF NOT EXISTS cold_audit_log_organization_id ON cold_audit_log (organization_id);
CREATE INDEX IF NOT EXISTS cold_audit_log_table_name ON cold_audit_log (table_name);
CREATE INDEX IF NOT EXISTS cold_audit_log_archived_at ON cold_audit_log (archived_at);
