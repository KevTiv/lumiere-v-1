/**
 * Loose row shapes for proposal UI (avoids pulling strict generated table types into `@lumiere/ui`).
 * Loose shapes for proposal UI; prefer `generated/types` where strict typing is needed.
 */
/* eslint-disable @typescript-eslint/no-explicit-any */
export type ProposalSourceDoc = Record<string, unknown>
export type ProposalLineItem = Record<string, unknown>
export type ProposalPresence = Record<string, unknown>
export type ProposalComment = Record<string, unknown>
