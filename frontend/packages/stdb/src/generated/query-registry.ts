// Hand-written proxy — the generated content moved to @lumiere/contracts.
// Kept at this path so existing import specifiers don't need to change.
export * from "@lumiere/contracts/generated/query-registry";
