#!/usr/bin/env node
// Kept as a discoverable CI entry point. The generator performs validation
// after loading the checked-in artifact and fails closed on drift.
import "./generate-subscription-census.mjs";
